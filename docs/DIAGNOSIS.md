# DIAGNOSIS — 全书诊断（重构前）

> 方法：只诊断，不重写。对照 `BOOK_SPEC.md`、`GLOSSARY.md`、章节正文与数字清单。
> 评分：1–10（真实性 / 深度 / 可操作 / 叙事 / 一致性）。证据强度：强 / 中 / 弱。

## 总判

| 维度 | 分 | 说明 |
|---|---|---|
| 结构完整性 | 9 | 九部分、33 章、地基三件套、遗留债务闭环清晰 |
| 叙事张力 | 8 | 案例章冲突强；部分理论章「正确但偏空」 |
| 证据密度 | 7→8 | 虚构案例仍自洽；**已写入可核实公开锚点（DORA 2025/2024、NIST AI RMF、GitHub×Accenture、Nygard ADR）** |
| 配图 | 2→9 | **实测（张数由 `scripts/check_figures.py --print` 打印，本行不抄总数；最近一次为手稿 43 文件 + 站点页 3 张）：逐页由第七条守卫验「md 围栏数 = 已渲染并量到字号的图数」，等式两侧的读数由该守卫自己打印；每章 2–5 张、无豁免；节点 ≤12、图号按阅读顺序递增、图注编号全书唯一，由 `scripts/check_figures.py` 实测把关；位图 15 张（封面 + 4 案例头图 + 9 部分卷首 + 1 遗留旧封面）** |
| 可发布美观 | 6→9 | **零 CDN 自托管子集字体（Noto Serif/Sans SC 四款 woff2）、九部分卷首艺术页、深浅双主题切换（默认浅色）、阅读进度条、宽屏右侧 TOC、返回顶部、封面重做** |
| 术语一致 | 8 | 已落 GLOSSARY；第 26 章案例角色名已与正文对齐 |

**结论：不必推倒重写。** 重构重点：① 补图与视觉层（已完成，含字体与交互）；② 理论章收紧「空泛正确」（最薄 13 章已扩写 +36%～+77%，含可抄配置/度量/检查清单，数字全部取自 ch05-数字清单.md）；③ 统一元数据与导航（已完成）；④ 对外声明案例边界（首页/指南已强化）。

### 已完成清单（P0 + P1 + 定位补齐 + 2026-09 出版级补强）

- 编辑部五件套 + 诊断 + 图清单
- Docsify 自托管 Mermaid
- 封面 + 四案例头图
- 关键理论章与四案例治理机制图
- 第 4 章「最小可用工程栈 90 天」
- 第 8 章 import-linter 可落地配置
- 第 26 章四案例映射角色名修正
- 公开证据档案（DORA / NIST / GitHub 研究 / Nygard ADR）
- **定位章《这本书是什么》**
- **90 天总路线图**
- **附录 J AI 系统工程最小面**
- **附录 K 一线最小实践包**
- 四案例章首「叙事边界」硬化声明
- **（2026-09）全部理论章补到 ≥2 图、支柱/案例章 ≥3 图；修复 3 处不可解析的旧 Mermaid 语法**
- **（2026-09）九个部分卷首艺术页 + 新封面图 + 侧边栏分组**
- **（2026-09）零 CDN 自托管子集字体：`scripts/build_fonts.py` 生成 4 款 Noto SC woff2，charset 覆盖全站实际用字**
- **（2026-09）主题升格：深浅双主题切换（默认浅色）、阅读进度条、宽屏右侧 TOC、返回顶部、封面/首页重排**
- **（2026-09）最薄 13 个理论/实践章扩写 +36%～+77%（可抄骨架、落地步骤、度量表、检查清单；数字全部引用 ch05-数字清单.md 既有事实）**
- **（2026-09）浏览器实测：控制台零错误、全部图片/字体资源 200、390px 视口无横向破版**
- **（2026-09-24）配图欠账收口**：先前记录里"附录按 STYLE_GUIDE 豁免"的说法经查证**不存在于 BOOK_SPEC/STYLE_GUIDE**，属发明豁免——已改为补齐实物：附录 A/I/J/K 各 2 张、《这本书是什么》等定位章达标、前置三件套（时间线 2 / 角色卡 3 / 数字清单 2）补齐，42 个 `chNN-` 章文件全部落在 2–3 张。
- **（2026-09-24）图注顺序更正**：全书实测「围栏在前、加粗图注在后」占 98/101，先前 STYLE_GUIDE 的模板写反了；已修正模板，并把模板里内层 mermaid 围栏改用四反引号外层围栏包住，避免示例被当成真图计数。
- **（2026-09-24）渲染破损修复**：本节上一版在正文里裸写了三个反引号的围栏串，Docsify 会把后半页吞进代码块（实测页面只剩前半、`## 下一轮` 变成裸文本）——已改为不含围栏串的说法，并新增 `scripts/check_markdown.py` 作为结构闸（围栏串只允许出现在行首、每文件围栏必须成对）。
- **（2026-09-24）超 12 个可读对象的四张图已收口**（重画而非放宽规则）：`docs/manuscript/README.md` 13→12、角色卡图 PS-1（该章前缀本轮由 R- 改为 PS-，见下方图号唯一性条目）16→8+8 拆分、第 2 章图 2-1 14→12（去掉中心汇聚节点，改为六对一一对应，与图注一致）。
- **（2026-09-24）图号按阅读顺序递增**：7 个章文件（ch01/ch06/ch34–ch37/ch39）的图号原为"编号与位置错位"，已按出现顺序重排；守卫实测全书 `图号 != 1..n` 为 0。
- **（2026-09-24）BOOK_SPEC §9 补齐可执行定义**：写明"节点 = 画布上的可读对象"及 subgraph/`style` 行不计、思维导图分支与叶项分别计、甘特按任务行计；明确"每章 2–5 张，含定位章、前置账本与附录，无豁免条款"；注明由 `scripts/check_figures.py` 校验。
- **（2026-09-24）两条守卫脚本落地并做过变异自检**：`scripts/check_figures.py`（图数 2–5、图注 1:1、图号递增、节点 ≤12，输出 `手稿 43 个文件 / 104 张图；站点页 3 张；合计 107 张`）、`scripts/check_links.py`（输出 `本地引用去重后 97 条 URL。死链 = 0。`）。两者均以假数据触发具名失败、退出码 1，清理后退出码 0；`FIGURE_LIST.md` 的表格由 `check_figures.py --print` 生成，不再手抄。
- **（2026-09-24）新增第三条守卫（`scripts/check_markdown.py`）**：管围栏结构——① 正文里不得裸写围栏串（三个及以上反引号），② 每文件围栏必须成对，③ 根目录与 `docs/` 的五份规范/元数据副本必须逐字节一致。输出：`73 个 md 文件：围栏全部成对，正文无裸围栏串；5 对根/docs 副本逐字节一致`。三条判据各做过变异（探针文件 + 改坏一份 GLOSSARY 副本），三条都能具名报红、退出码 1；复位后退出码 0。
- **（2026-09-24）真实破损两例（由这条守卫与浏览器实测共同抓到）**：① 本节上一版在正文里裸写了三个反引号的围栏串，页面上把围栏当字符漏出来；② 根副本与 `docs/` 副本曾各自演化——`BOOK_SPEC.md` 的 §9 只有根副本是新的、`STYLE_GUIDE.md` 的图注模板只有 docs 副本是新的，同一本书里存在两个版本的配图规则且都自称正确。已双向对齐并由第三条守卫锁死。
- **（2026-09-24）复核方法学更正——浏览器缓存会伪造「已验证」**：静态站用 `python3 -m http.server` 起时不带 `Cache-Control`，Chrome 会把 `.md` 留在 HTTP 缓存里；本轮首次"逐页实测"读到的其实是改前的旧页（旧 `FIGURE_LIST` 的三反引号模板把后半页吞成裸文本，看上去像新改动坏了）。定稿复核的正确顺序是：先对每个 `.md` 发 `fetch(url, {cache:'reload'})` 清缓存 → 整页 `reload()` 清 Docsify 内存缓存 → 再逐路由渲染。**终验以清缓存后的扫描为准。**
- **（2026-09-24）`FIGURE_LIST.md` 与实物对齐**：总数改为实测 107，删除"附录豁免"的发明条款，位图清单更正为 14 张被引用 + 1 张遗留旧封面。
- **（2026-09-24）甘特图被压成 300px 的排版缺陷修复**：自托管的 mermaid 的甘特渲染器按**宿主父元素的 `offsetWidth`** 定宽；本主题的 `.mermaid` 容器由插件在 `doneEach` 里动态生成，那一刻量到的是包裹层而不是正文列宽，实测恒为 300px——四案例时间线（本书门面图）在 632px 正文列里只剩一小块，刻度文字约 4～5px。修法是在 `mermaid.initialize` 里显式喂 `gantt.useWidth = 正文列宽`（`ainseGanttWidth()`：量 `.markdown-section` 内首个 `p/li/h2/td` 的 `offsetWidth`，夹在 300–1200，只在量不到时兜底 720）。修复后实测：1280px 视口下 ch03/ch02 甘特 `viewBox 0 0 632 316`、渲染 632px（正文列满宽），ch09 `632×244`；390px 视口下 `0 0 300 316`、渲染 296px、缩放 0.99，无横向溢出（`scrollWidth 386 == clientWidth 386`）。
- **（2026-09-24）终验（清缓存后）实测数字**：62 条路由逐页渲染——107 个 mermaid 块 `mermaid.parse` 0 失败、围栏泄漏 0、空页 0、渲染后本地链接死链 0；三条 python 守卫全绿（图 107 / 引用 97 URL 死链 0 / 73 个 md 结构 + 5 对副本一致），退出码均为 0。
- **（2026-09-24）测量方法学补充——隐形标签页会造出假阳性**：自动化标签页 `window.innerWidth === 0`、`document.hidden === true`，任何按宽度推导的量（`getBoundingClientRect`、`offsetWidth`）都不可信；本轮首次报出的 3 个"SVG 宽 0"即是此假阳性。宽度类复检改用**固定尺寸的隐藏同源 iframe**（1280px / 390px）后再读数。
- **（2026-09-24）致命缺陷「整页不可点击」根因与修法**：`theme.css` 给 `section.cover` 无条件写了 `display:flex !important`，而 Docsify 收起封面只摘 `show` 类、节点留在 DOM——非封面路由上封面仍以 `position:fixed + 100vh + z-index:999` 铺满 1280×900，命中测试实证 5/5 采样点 `elementFromPoint` 全部落在 `.cover` 上。读者看到的正是"点什么都没反应"。修法：`section.cover:not(.show){display:none !important}`；由第四条守卫 `check_overlays.py`（层叠语义）+ 第五条守卫 `check_interactions.py`（真实命中）双向锁死。
- **（2026-09-24）方法学更正（本轮最重要的一条）——DOM 读数看不见遮挡类缺陷**：改坏之前，"封面已收起"在 `innerText`、`offsetHeight`、`.cover.show` 类名三类读数上全都表现正常，此前所有"浏览器已验证"的结论因此集体放行一个整页失效的缺陷。此后交互结论只认两类证据：**该点的 `elementFromPoint` 命中的是目标本身或其后代**，以及**真点一次之后路由/状态确实变化**。读数只用于定位，不用于下交互结论。
- **（2026-09-24）探针自身的四个假阳性来源（都是量法错，不是站点缺陷）**：① `theme.css` 开了 `scroll-behavior:smooth`，滚完立刻量拿到的是动画途中的矩形——量前一律 `scrollIntoView({behavior:'instant'})`；② 把视口外元素的中心"夹"进视口再 `elementFromPoint`，测的其实是邻居，命中点必须是真实中心，中心出视口就判不可点；③ 比较 `href` 与 `location.hash` 不同口径（文件型 href 是原文、`?id=` 锚点已在 href 里编码，方向正好相反），曾报出"期望 X，实测 X"的自相矛盾，改为两侧都 `decodeURIComponent` 再比；④ 让封面进遍历清单会连锁放大成逐路由假失败，守卫改为**中止并给一条具名前置原因**，而不是放大 N 条。
- **（2026-09-24）侧边栏二次重画导致"点错链接"（守卫自己的竞态）**：Docsify 换页后不是一次画完——正文先出现，`subMaxLevel` 的 `?id=` 子链接要等标题渲染完再注入，侧边栏随后几百毫秒内被整轮换掉。守卫因此报出"打标的是 `#/guide?id=…`，10s 后哈希却是 `#/manuscript/README`"。修法三条：点前先等 **DOM 签名（侧边栏长度 + 正文长度）连续 0.35s 不变**；把"要点的这一个"用属性钉死（`.sidebar li:not(.active) > a` 这类相对选择器在等待期间会换人），坐标、命中确认、点击全部只认被钉住的节点；点前最后一次 `elementFromPoint` 确认，且只认"它自己或它的后代"接到这一点——命中祖先不算，因为事件捕获路径止于祖先，目标 `<a>` 的处理器根本不会触发。
- **（2026-09-24）命中测试与真实点击的分工**：被判"该点被别的东西吃掉"时，守卫**仍然按读者的视角真点一次**，把两种证据并成一条失败项；若命中测试说点不到而真点却生效了，同样报红（探针与现实的冲突本身就是缺陷）。变异 M3（`#btn-theme{pointer-events:none}`）正是靠这条双证据链被按机制捕获的，而不是靠一句泛词判据。
- **（2026-09-24）mermaid 未捕获 promise 拒绝（`Uncaught (in promise) … Object`）根因**：`mermaid.run()` 返回 Promise，插件里三处都用 `try/catch` 包它——`try/catch` 拦不住异步拒绝，控制台必现红字。真正咬人的是三条渲染路径（`doneEach` 首绘、封面收起后的补渲染、主题切换重绘）会**并行**对同一批节点动手，其中两方还会先把节点 `textContent` 重置回源码。修法：全站一条串行队列（一次只让一份图在飞），出队时若 `location.hash` 已切走就整批作废（不再往旧 DOM 上画），还原源码也挪到出队时做；失败**仍用 `console.error` 点名「哪条路径、哪一页、哪张图」**——吞成静默或降级成 `warn` 等于给第五条守卫拆闸。修后复测数字见本节末「终验」条。
- **（2026-09-24）图注与正文强调句同形，读者分不清谁是谁**：全书 107 条图注在稿件里写成 `**图 4-1｜四支柱** — 说明…`，渲染后与正文里的普通加粗句一模一样。修法是在渲染层挂号（`figureCaption` 插件的 `afterEach` 给 `<p><strong>图 X-Y…` 挂 `.figure-caption` 类），版式全部交给 `theme.css`（左侧细线 + 小一号 + 图号用强调色），**稿件零改动、零 CDN**。静态核对：以「图」开头的行 109 条，挂号正则命中 107 条（与 `check_figures.py` 的 107 张图一一对上），余下 2 条是 `FIGURE_LIST.md` / `STYLE_GUIDE.md` 里的模板示例 `图 N-M`——刻意不命中，示例不该长成正文的样子。
- **（2026-09-24，本轮改写第 61 条）「390 顶栏不可点是设计」是一条错误的放行理由**：原文写着"Docsify 的移动端样式表把 `.app-nav` 设成 `display:none`"。查证结果：`theme.css` 里那条 `.app-nav{display:none !important}` 在 **`@media print`** 块内，与窄屏无关；390 实测计算样式 `display:flex`、`position:fixed`、高 60px。也就是说当时手机读者看到的不是"隐藏"，而是**一条挤坏、点不准的顶栏**——守卫却以"设计上不存在"为由把它放过去了。教训写进方法学：**放行理由必须是被量过的**，"这个控件按设计不存在"要附一条计算样式读数，否则它只是给缺陷起的名字。现在 390 档既真点抽屉路径（开关 → 抽屉内链接 → 收起），也真点顶栏（第 3 项「全书总览」，中心点恒在屏内）。
- **（2026-09-24）窄屏顶栏折行/溢出的根因与修法**：`.app-nav` 是 `position:fixed` + `height:var(--navbar-h)`（60px）的**硬高度**条，`li` 默认 `flex-shrink:1` 且链接没有 `white-space:nowrap`，五个中文项被压扁后在条内折成 2 行并溢出裁切（实测「主页」`rect [12, 0, 39, 59]`、行数 2）。修法是四件一起：`ul{flex-wrap:nowrap; overflow-x:auto}`、`li{flex:0 0 auto}`、`a{white-space:nowrap}`，外加把父条的 `justify-content` 从 `flex-end` 改成 `flex-start`——**内容超宽时靠右对齐会把最后一个链接推到滚不进来的位置**，这是"能滚但滚不到"的隐藏坑。修后实测：`ul` 内容宽 428px / 可视 366px，横向可滚，五项全部单行且落在 60px 条内。
- **（2026-09-24）窄屏 mermaid：不能用一个 CSS 定值解决两类图**：390 视口里正文列约 342px，mermaid 给 `svg` 写的是内联 `max-width = 自己的 viewBox 宽`，于是 viewBox 1045 的横向流程图被等比压成 308px（缩放 0.37，节点文字 4～5px）。第一版修法 `@media (max-width:768px){.mermaid svg{min-width:600px}}` **造出新缺陷**：它把本来只有 302px 宽的甘特也拉到 600px（缩放 2.0，字号失控）。终版按「每张图自己的 viewBox 宽」逐图补 `min-width`（`ainseGuardDiagramWidth()`，挂在唯一那条串行渲染队列的 `.then()` 上，首绘/补渲染/主题重绘三条路径一起覆盖），甘特则另走窄屏感知的 `useWidth`（`<768` 时按 600 定宽，交给横滚，不再把月份刻度硬塞进 300px）。
- **（2026-09-24）居中 flex 遇上超宽子元素 = 滚不回来的左溢出**：`.mermaid` 是 `justify-content:center` 的 flex，子 `svg` 比滚动容器宽时，居中把左半张推到容器左侧之外，而 `scrollLeft` 不能为负——实测图左缘 x=-216（容器左缘 24），**左半张永久丢失且没有任何报错**。修法是在窄屏改 `justify-content:flex-start`。这类缺陷命中测试看不见（图不在点击清单里），只能靠几何闸量「viewBox 宽 / 渲染宽 / 左缘」。
- **（2026-09-24）本站真正的横向滚动容器是 `.content`，不是 `documentElement` 这个盒子**：第六条守卫第一次做变异自检时，「正文强行加宽到 1400px」这条变异**零报红**——因为判据量的是 `documentElement.scrollWidth - innerWidth`，实测恒为 0，而破版实际发生在 `.content`（绝对定位的滚动层）里。补上 `contentEl.scrollWidth - clientWidth` 后同一变异立刻报红「正文层 .content 横向溢出 1010px」。教训：**判据量错盒 = 一条永远绿的闸**；每条溢出类判据要写清它量的是哪个滚动容器，并用变异证明那个容器确实是读者看到破版的那一个。
- **（2026-09-24）第六条守卫 `check_mobile.py`（窄屏几何闸）与其五条变异**：命中测试看不见"折行"和"缩糊"——顶栏链接 `rect` 高 59、中心 y=29 全在 60px 条内，命中测试全绿，读者看到的却是两行挤在一起的半截字。所以几何判据独立成闸：顶栏单行且在条内、每张 mermaid 缩放 ≥0.95、图左缘不越容器、`.content` 与文档均无横滚。变异 A（去 `nowrap`）→「跑出固定条」20 条、B（可压扁）→「折成 2 行」20 条、C（撤掉宽图左对齐）→「居中溢出滚不回来」6 条、D（强撑正文）→「正文层 .content 横向溢出」4 条、E（定宽守卫失配）→「等比缩成糊图」5 条，退出码 0。变异 C 的锚**必须是两条规则一起删**：`.is-wide` 那条与 `@media (max-width:768px)` 里那条是同一机制的两半（桌面靠挂号、窄屏靠媒体查询），只删一条时另一半原地生效，读数为 0 红——"一条判据为什么没红"要先问"是不是还有一份代码在替它顶班"。
- **（2026-09-24）变异锚点必须唯一：`replace(old, new, 1)` 只吃第一处**：上面那条 E 第一次跑是**未被捕获**（守卫判 0 条报红）。原因不在判据，而在变异本身：锚点 `window.matchMedia('(max-width: 768px)')` 在 `index.html` 里有两处，`count=1` 的替换打到了前一处 `ainseGanttWidth()` 上，窄屏定宽守卫原地不动，图当然不糊。这类"假自检失败"比假绿更难发现，因为它看起来像在指责判据太松。修法：锚点写成整行以消歧，并让 `apply_mutation` 在 `text.count(old) != 1` 时**中止并报名**——歧义要在打补丁之前死掉，而不是靠读数反推。
- **（2026-09-24）探针自身还会撒三个谎（都表现为"验证通过"）**：① 拼 URL 时留下双斜杠，请求 404 但页面照样渲染完，读数一切正常；② 读颜色时丢掉 alpha 通道，`rgba(255,255,255,0)` 被当成纯白通过了对比度判据；③ 用 `localStorage` 直接写主题键再刷新来"验证深色模式"——那绕过了 `#btn-theme` 的真实点击路径，按钮坏了也能全绿。此后主题类结论只认真点按钮后的读数（第五条守卫的 `click_chain` 即按此写）。
- **（2026-09-24）选择器匹配不到节点时"0 失败"是空集，不是通过**：一次视觉探针用 `.part-num` 量部分卷首编号，返回 0 处问题——真相是该类名在渲染结果里根本不存在（页面上没有任何节点被量到）。守卫此后必须**自报覆盖集**（量了几个、什么选择器），空集要显式报"零覆盖"而不是绿。同理，`elementFromPoint` 在视口外返回 `null` 属于"不可测"，不得计入"点不动"——390 顶栏第 5 项「搜索」中心点在横滚区外，按 offscreen 跳过并写明，改点中心点恒在屏内的第 3 项。
- **（2026-09-24）`docs/README.md` 总览页上原样显示出一条 `---` 这行分隔线**：`<div>` 起的 HTML 块要遇到**空行**才结束，紧跟标签行的 `---` 被当作字面量渲染。这类破损不报错、不影响死链、DOM 里也"看得见"，因此补进第三条守卫 `check_markdown.py`：上一行是 HTML 标签行、本行是 `---`/`#`/`>`/表格/列表且中间没有空行 → 报红。
- **（2026-09-24）窄屏宽表用 `display:block` 换滚动，是把表格语义换成了可视滚动**：`@media (max-width:768px)` 里过去写 `table{display:block; overflow-x:auto}`——读者能滚，但 `display:block` 会把 `<table>` 从可访问性树里抹掉（读屏不再报行/列，`cols/rows` 结构消失）。改成渲染层挂号：`index.html` 的 `tableScroll` 插件在 `afterEach` 给每个 `<table>` 套一层 `.table-scroll`（滚动交给它），表格自己保持 `display:table`。实测（390 与 1280 各 4 页）：`display=table`、父层 `table-scroll`、`overflow-x:auto`；390 下 21 张表有 9 张需要滚动（最宽溢出 277px），1280 下 0 张；`.content` 与文档横向溢出仍为 0。
- **（2026-09-24）"溢出"判据必须区分「滚出正文列」和「被滚动容器接住」**：加了包裹层后，视觉探针在 390 报了 10 处"溢出内容区"（表格页 6 处）。逐条查：报的都是 `.table-scroll` 内部按内容撑开的表格，容器本身贴齐正文列、`.content` 读数为 0——是判据没建模滚动容器，不是破版。修法是给豁免加条件（祖先里确有 `overflow-x:auto/scroll` 且**该祖先自己**在正文列内），并且**豁免只对后代生效、容器自身仍被检查**（`.table-scroll` 已加进被检选择器）。独立支撑来自另一条守卫：第六条守卫健量 `.content` 与文档的横滚，且它的变异 D 证明这条读数会报红——不是把缺陷改名成特性。
- **（2026-09-24）会打印失败却退出码 0 的探针，本身就是一条假闸**：上述 4 页报红的那一轮，脚本 `rc` 仍是 0（它只打印不判分）。这等于给读者发"全绿"的假凭据。修法：把每页的 `fails` 存进 `report`，末尾按 `失败/作废` 条数给退出码。此后任何探针脚本进复核链之前，先问一句"它红的时候退出码是几"。
- **（2026-09-24）可读性地板：桌面档才是主战场，窄屏定宽常量解决不了它**：390 档修好之后重测 1280，41 张图缩放仍 <0.85，最差是案例四 ch37 的 `viewBox` 宽 1491 → 渲染 638px（缩放 0.428），节点「CTO 阿坤」有效字号 6.85px，全站 15 张图低于 11px。根因是先前那条定宽只挂在 `@media (max-width:768px)` 上，而正文栏在 1280 也只有 638px——**「窄」是相对正文栏的，不是相对视口的**。修法改成逐图求解：`有效字号 = 节点自然字号 × 渲染缩放`，要让地板 ≥11px 就需要 `min-width = viewBox宽 × 自然字号 / 11`，超出正文栏的部分交给 `.mermaid-block` 横向滚动，同时给该图宿主挂 `.is-wide` 改左对齐（居中 flex 会把左半张推到 `scrollLeft` 滚不回的负偏移）。**不用 CSS 定值**：定值会把 302px 的甘特也拉到 600px（缩放 2.0，字号失控），这条先前踩过一次。
- **（2026-09-24）滚动暗示层要挂在「不滚动」的容器外面**：实测全站需要横向滚动的容器 352 处、视觉暗示覆盖率 0——读者在 390 档面对的是一张溢出 1167px 的图，界面上没有任何一处说"还能往右滚"。做法是给每个滚动容器套一层 `.scroll-frame`（**不滚动**的那一层），左右各挂一片 `.scroll-veil`：`mask-image` 做渐隐、`::after` 用内联 SVG 遮罩画方向箭头、`pointer-events:none`、状态类 `has-left/has-right` 由 `scroll`/`resize` 同步。两条容易忽略的：① 渐隐伪元素若挂在**滚动容器自己**身上，它会跟着内容一起滚走，暗示在滚的第一帧就消失；② 渐隐的底色要按层选——表格压在页面底色上、图有自己的卡底，用 `--c-veil` 分开给（图那层用 `--c-bg-soft`，否则边缘露出一条白边）。`pointer-events` 不是可选项：它压着的就是图节点与表格单元格，第七道守卫在右缘逐点做 `elementFromPoint` 命中测试。
- **（2026-09-24）「该不该显示」只问状态类，不问 computed opacity，也不问量测时的坐标**：交互闸把暗示层纳入真实点击清单后，连续两轮报"候选 0 处"——判据一次都没执行，而"没执行的判据"和"通过的判据"输出完全一样。第一版用 `getComputedStyle().opacity` 当条件：与滚动/换类同一次求值里读到的是 `.18s` 渐变的**起始值**，永远读不到 1。第二版改用"坐标在视口内"筛候选：同样 0 次真点，因为坐标是在同一次求值里量的，而控件那段之前调过 `settle()` 滚过侧边栏，正文里暗示层的 y 已经换人（实测 ch37 那条 y=7345，远超 900 视口）。终版只交**状态类 + 容器序号**，真点前按序号重新定位、重新量坐标，并补一条硬失败：「命中测试量到 N 点、该侧有状态类 M 点，真实点击却 0 次 ⇒ 本档结论不成立」。教训：**坐标是易腐读数，跨不过一次求值；能跨页传递的定位要用序号，不用像素。**
- **（2026-09-24）像素复核否掉了一次「顺手改 CSS」**：390 档表格截图上，右缘 25px 内文字被抹平、列 11–13 出现深色像素，起初判成"渐隐没涂到内容上"。两条读数对账后推翻：计算样式里 `--c-veil` = rgb(247,248,250)、几何与 `mask-image` 都正确，那些深色像素其实是**靛蓝方向箭头本体**。结论是按设计工作，**未改一行 CSS**。截图类疑点必须"计算样式 + 像素"双读数，不能看图像坏了就动手——那会把一条正确的实现改坏，还得重跑全套浏览器守卫。
- **（2026-09-24）侧边栏两处裸文本分组标签（截图复核抓到，DOM 读数看不见）**：`地基（四案例并列）` 与 `配套附录` 在 `_sidebar.md` 里是裸文本条目，渲染后不带 `<strong>`，落位 x≈10、与同级的组标签样式脱节。改为 `**…**` 走既有的 `.sidebar-nav li > strong` 规则，稿件层零 CSS 改动。这类"排版层级丢了"的缺陷只在截图上看得见：DOM 里它就是个合法的 `<li><a>`，命中测试与几何闸全绿。
- **（2026-09-24）`--limit 12` 的跑批打印「交互闸通过（全量）」**：范围标签只列了 `--no-chain` / `--no-walk` 两个开关，漏了 limit——一条**承诺覆盖面**的文案没跟着覆盖面变量走，读的人据此以为 62 条全走了。修法是把 limit 也写进 skipped 标签。同理，变异自检的 needle 不能写死：A 那条先前写「低于 11px 下限」，失败文案用的是常量 `MIN_EFFECTIVE_FONT`（渲染成 `11.0`），变异真的把地板打红 13 条、自检却因 needle 对不上判成"未被捕获"；现由常量生成。
- **（2026-09-24）「这一页该有几张图」的独立口径要按 CommonMark 嵌套规则数围栏**：覆盖集自证最初用「行首三个反引号 + mermaid」的正则去数，把 `STYLE_GUIDE.md` / `FIGURE_LIST.md` 里被四反引号外层围栏包住的**图注模板示例**也数成了应有图，全站体检因此报 8 条"有图没画出来"——缺陷在量具不在书（Docsify 永远不会把示例源码变成图）。终版按「同种字符、长度不短于自己、行尾无内容」才算闭合来配对围栏，只统计真正会渲染的那条三反引号 mermaid 围栏。（本条自己踩了一次同样的坑：初版在这里裸写了三个反引号的正则字面量，被第三条守卫当场报红——规则对写规则的人同样生效。）
- **（2026-09-24）本轮终验（可读性地板 + 滚动暗示落地之后的全量重跑，逐条带退出码）**：第七条守卫 `check_legibility.py --viewports 1280,1440,390 --themes light,dark --screenshot` **rc=0**——61 条正文路由 × 3 视口 × 2 主题 = **366 次真浏览器采样、366 张截图**逐图复核，枚举两口径互证一致（DOM 62 / `_sidebar.md` 62），读数目录开跑前先删；其变异自检 **rc=0**（A 撤地板→「低于 11.0px 下限」13 条、B 不建暗示层→「没有 .scroll-frame」44 条、C 渐隐吃点击→「挡住了命中测试」4 条、D 去 scroll 监听→「状态没跟着滚动更新」44 条、E 宽图不挂 is-wide→「没挂号 is-wide」13 条，子进程退出码复核均为 1）。第六条窄屏几何闸 **rc=0**（4 代表页 × 390），变异 A–E **rc=0**（20 / 20 / 6 / 4 / 5 条）。第五条交互闸全量 **rc=0**——62 条路由 × 1280/390，命中点 620、可点控件 5608、真实点击 85 次（新增暗示层穿透读数：1280 档节点 616 / 命中 76 点 / 该侧有状态类 38 点 / 真点 2/2，390 档节点 1232 / 命中 364 点 / 有状态类 220 点 / 真点 2/2，接收者均非暗示层），0 遮挡 / 0 点不动 / 0 状态不变 / 0 控制台错误；变异 M1–M5 **rc=0**（M1 复活封面遮罩 50s→267 条、M2 全屏透明遮罩 498s→259 条、M3 主题按钮吃掉点击 25s→3 条、M4 侧边栏点不动 21s→6 条、M5 暗示层 `pointer-events:auto` 23s→4 条）。截图类证据另加 132 张逐容器特写（起点/中点/终点 × 三档 × 浅深，状态类/opacity/pointer-events 零失配，rc=0）。第一条图闸、第二条死链闸、第三条结构闸、第四条遮罩层叠闸在本节元数据改动后重跑，退出码见下条。
- **（2026-09-24）元数据落笔之后重跑的静态四闸（读数取自本节最终文本）**：第一条图闸 **rc=0**（107 张、每章 2–5 张、图号递增、节点 ≤12）、第二条死链闸 **rc=0**（本地引用去重 97 条 URL，死链 0）、第四条遮罩层叠闸 **rc=0**（枚举到 1 个占满视口的 fixed 遮罩，状态化隐藏规则在层叠上能赢）、第三条结构闸**先 rc=1 后 rc=0**——它第一次跑抓到的是**本节自己**：上一版在这里裸写了三个反引号的正则字面量，Docsify 会把后半页吞进代码块，正是本条守卫管的那类破损（同一处根/docs 两份副本各报一条，所以是 2 处而非 1 处）。改写为「三个反引号」的文字描述后四闸全绿，73 个 md 文件围栏成对、无裸围栏、HTML 块后无未隔空语法、5 对根/docs 副本逐字节一致。这条失败是本轮最好的一条证据：**守卫不看写的是谁的稿子，包括写守卫说明的那篇。**
- **（2026-09-24）量测之后的任何编辑都作废该量测（含只改注释）**：本轮因此把顺序固定为「站点代码冻结 → 浏览器类守卫（第五/六/七条 + 变异自检 + 截图复核）→ 元数据（DIAGNOSIS / FIGURE_LIST 双副本）→ 静态类守卫（第一/二/三条）→ 提交」。这里有一条自己差点写错的判断：先前一版理由写着"元数据不在侧边栏里、不是被审计路由"——查 `docs/_sidebar.md:64-65`，全书诊断与配图清单**就是**侧边栏条目，因此**是**被审计的 62 条路由之一。所以准确的表述是：元数据编辑发生在浏览器读数之后，会让那两份页面的交互读数过期，必须重跑；本轮的做法是把元数据一次写完、只重跑真正读 md 的静态守卫，并在提交前用 `check_markdown.py` 的副本一致性判据兜住"两份演化"。写理由也要有出处——这条本身就是"注释里的断言要有字段来源"的又一次复发。

### 配色口径（2026-09-24 第二轮：浅档由纯白＋高饱和靛蓝改为纸感色板）

- **（2026-09-24）"颜色只有一个出处"这条要有可核对的缺口清单，不能只报一个总数**：`theme.css` 浅档 `:root` 定义 **76 个令牌**，深档 `html[data-theme='dark']` 覆盖 **45 个**，剩下 **31 个无深档对应**——这 31 个如果只写成"31 个待补"就是假缺陷清单，逐条列名后归因是两类：**21 个 `--c-plate*`** 是图表与题图的固定彩版（浅深两档同一张纸，故意不翻，翻了会和墨黑正文对不上），**10 个** 是 `--radius*` / `--sidebar-w` / `--navbar-h` / `--content-max` / `--font-*` 尺度与字体令牌（本来不是颜色）。所以**深档漏盖 0 个**。另有一条容易误判：`--cover-bg` 也无深档覆盖，但它的值是 `linear-gradient(…var(--c-sheet-hi)…)`，靠 `var()` 间接取色，自动跟档——"无覆盖"不等于"不跟主题"，判据得看值的形状而不是看键在不在。
- **（2026-09-24）数令牌的口径要用守卫自己的解析器，不要用顺手写的正则**：本轮为了写上面那段，先用"行首两个连字符"的正则去数，得到 69 / 69 且"零缺口"——盘上真实是 76 / 45。原因是令牌块里有一行写两条声明（如 `--c-plate-red: #f0dfd9; --c-plate-red-line: #a03b31;`），行首正则一行只吃到一个。两处误差方向相反且互相掩盖：浅档被少数 7 个、深档被多数 24 个，凑出"两档一一对应、没有缺口"这个**恰好是我想证明的结论**。改成直接 `import check_palette` 调它的 `parse_tokens()` 后才拿到 76/45/31。凡"覆盖率／对账"类读数，量具必须和被检对象共用同一份解析，否则报告的是正则的能力不是书的状况。
- **（2026-09-24）字面量色值的例外只剩两类，第三类是我主动撤掉的**：全站扫描下来，令牌块之外仍有字面量色的行是 `mask-image` 的黑白遮罩 4 行（`#000` 在那里是 alpha 通道不是涂料）与 `@media print` 的强制黑白 1 行（`docs/theme.css:1013`，印刷该省墨），第八条闸对这两类按属性/按介质放行，其余一律报红——**静态违规实测 0 条**。被撤掉的第三类是 `.token`（prism 代码高亮）豁免：它当初写着"这是第三方调色板，不动"，实际效果是让深档一整块八字面量代码色合法存在，纸感改版跑完**一条红都没报**。豁免注释是闸的洞，不是闸的边界条件。
- **（2026-09-24）图的 480 条颜色指令要逐条回指令牌，而不是「看着像」**：mermaid 主题配置里的每一条取色（`docs/index.html` 的 `t('--c-plate-*')`）都要求"解析后的字面值 ∈ `--c-plate*` 浅档值集"，实测 **480 条全中**；`index.html` 里为 mermaid 准备的兜底表 **16 项 / 读取 16 个令牌**，逐项与令牌值相等。**这条兜底表本身是第五道风险**：它一旦手编漂移，就会在 theme.css 加载失败的瞬间把图刷成旧色板，所以判据不是"有兜底"而是"兜底 == 令牌"。
- **（2026-09-24）第八条闸有一块看不见的地方，必须写在件内而不是留给读者猜**：真浏览器里的对比度读数是从 DOM 的 computed style 取的，**图片对外只有一个盒子，`<img>` 引用的 SVG 内部它取不到**。九张手工版画题图因此不由第八条闸背书，颜色纪律改由生成器自己的 `--check` 把住：现读 `theme.css` 的 `--c-plate*`（板外色在构造上不可能）、XML 必须能解析、不得含 `<text>` 节点；窄屏可读性由 390 档截图人工复核。**一个绿的读数只覆盖它真的看到的范围**——这条边界此前只存在于口头。
- **（2026-09-24）改线宽之前先把显示尺度量出来，别拿 viewBox 当尺寸**：题图第一版按 `viewBox` 1200 宽的直觉写笔宽，落到页面实测最细的一根只有 **0.447px**，浅档纸底上直接断线。真实显示尺度是用 CDP 量的：`.chapter-plate img` 在 **1280 与 1440 两档都是 670px 宽**（正文列有 `max-width`，视口更宽不等于图更大），**390 档 340px**。于是按 670/1200 的倒数给全部笔宽、虚线段长、排线笔宽与点刻半径统一预放大 **1.792 倍**（图案格间距不放大——那是空间量不是涂料量）。**补完之后再截图看着「没变」**，这一次没有下"补丁失效"的结论：先去盘上 grep 产物的 `stroke-width` 直方图，确认已是 2.33/3.76/1.43 这一档，才判出"线宽确实修好了，剩下的毛病是构图"。截图会被显示器缩放和视觉预期骗，产物字节不会。
- **（2026-09-24）试版产物不许落在发货目录里**：`plate_engine.py --only` 曾把 `probe-*.svg` 直接写进 `docs/assets/plates/`，一时该目录 11 个文件（含一张 138 KB 的试验稿），而站点是零构建直发——它会被真的下载。现在试版落 `/tmp/plates_probe`，发货目录实测**恰好 9 张**。顺手一条同源教训：`--only` 那次改完图，我按旧预览判定"没生效"，浪费了一轮；预览文件和仓库文件不是同一个东西。

### 逐章扩写质检（2026-09-24 第二轮：无锚图清零 + 数字对账 + 工具卡分档）

- **（2026-09-24）一条新判据在让书变绿之前，必须先让它在书上变红一次，并且先量它管得住多大范围**：第一条守卫这轮加了两条——正文每处「图 X-Y」引用要能落到全书某条真图注上（悬空引用），以及每条图注必须在本文件正文里被指一次（无锚图）。第一次跑 **rc=1、报 95 条具名问题**，这就是它该有的样子：一条 0 违规的新判据只说明口径没读者。收口后 6 个并行子代理逐文件补锚句（只准加正文句子，禁改图、图注、标题、表格），当前读数 **正文图引用 107 条 / 全书图注 102 条 / 悬空 0 条 / 无锚 0 条**。但"95→0"这个数字本身要先怀疑：`REF` 正则最初只吃 `[0-9]{1,2}-[0-9]{1,2}`，案例章的 `C1-1` 与卷首的罗马前缀**整类对象根本不进分母**——那 12 张图不是"没锚"，是"闸看不见它们"。放宽到 `[A-Za-z0-9]{1,4}-[0-9]{1,2}` 之后，**每类前缀各锁一条已知答案 fixture**（数字章、`C1-x`、罗马、混合），否则下一次放宽还会再吞掉一类。
- **（2026-09-24）变异自检会自己造假红：改动排除集的变异不是变异，是换题**：`--selftest` 第一版对照 C 用「`图 ` → `见图 `」全书替换来证明口径不依赖字面前缀，结果引用数从 8 涨到 87 而图注数没变——因为替换把图注行本身从 `CAP_LINE` 排除集里放了出来，图注开始被当成引用计。**判据**：对真产物做变异时，变异不允许改变该判据的排除集；要证明口径覆盖某类对象，用**已知答案的 fixture**（四条，含期望的引用数/问题数），不要用全书正则替换。另外基准读数也要能自证：`n_ref == 0` 时 `--selftest` 直接判红，"抓到 0 条"在这个口径里意味着闸失效，不意味着书干净。
- **（2026-09-24）AI 扩写的数字必须逐行对账，而且表格行是重灾区**：本轮所有新增行按 `git show HEAD:<file>` 取原文，逐行抽出数字，凡基线里不存在的数字就要求指回 `ch05-数字清单.md` 或台账；跳过围栏内部与 mermaid `style/fill/stroke`（那是配色轮的字面量，不是数字）。**结果：无出处数字 0 条。** 同一条流程第一次跑漏掉了表格行——上一版被证实的编造恰好活在一行表格的阈值里，所以这一轮的抽取显式含表格。顺带由编辑代理抓到两处**跨文件**矛盾并改正：案例一时间线上的治理组成立年月与总时间线不一致（`2024-05` → `2024-06`），第 33 章思维导图把 kill switch 写成"双人"而第 32 章的事实是人工冻结单人可执行、上线才是双签。**要登记一条缺口：八条守卫没有一条覆盖跨文件叙事一致性**——这两处是人抓的，不是闸抓的，下轮要么立闸要么别声称有。
- **（2026-09-24）工具卡分三档，且"C 档"必须是可见的**：第 4 章台账从"15 件工具停在提到名字"改为**按证据档位索引**——**A 本机实跑**（OpenAPI/protoc/nginx：连报错原文都是跑出来的）——**此句当日即为错账**，三件工具本机都跑不起来，2026-09-25 的「推送前的自检抓出台账自己造假」一条已把三行降为 B 并把取证写清、**B 文档或源码逐字、本机未复跑**（import-linter、Semgrep、Trivy、Prometheus、Grafana、Redis、Kafka、LangChain）、**C 不出卡**（oasdiff/buf 判据行、Sentry 整张卡）。B 档的判据行不只引网页：摘要行与退出码常量是从 `raw.githubusercontent.com` 上的**源码与文档 markdown** 逐字核对的（`Contracts: N kept, M broken.`、`OK/FINDINGS/FATAL=0/1/2`、`Total: 1 (UNKNOWN: 0, …)`、`Default: "trivy.yaml"`），出处与当日读数写在 `references.md` 的 B 档表。LangChain 卡**故意把"跑完看哪条输出"留空**并说明原因（官方无样例、拒绝凭印象拟一段重试日志）——按卡自己的口径，那一格空着比填上更像卡。
- **（2026-09-24）计数列会被写它的人自己作废，所以撤掉**：第 4 章原表的「章数 / 次数」是当天测的，当天就被本轮的卡片编辑改掉了（Redis 从 4 章变 5 章，因为第 21 章现在真的在讲 Redis）。**清单里任何"当下快照式"的计数，都会在同类对象落地那天静默腐烂**——这次的腐化源是我自己的扩写。做法：删掉计数列，改成对读者有行动价值的东西（落点 + 档位），并在口径声明里写明"刻意不放计数，要自己数请排除本台账"。同一条纪律顺手抓出另一处错账：Foundry 卡的落点原写第 20 章、备注"有合约代码，缺 `foundry.toml`"，实测第 20 章（风控）正文没有一处合约测试内容。**而我第一次改账时把它换成"第 19 章五层门禁"，同样没验——`grep -c '合约\|Solidity' ch24` 返回 0，第 19 章讲的是软件/数据/策略/交易/风控，跟合约无关。** 实情是 Foundry 全书只在案例三点名，**本书没有合约测试的机制章**；卡因此留在案例三，并在第 4 章台账的口径声明里显式记为"一个例外"。教训：**改一条错账要用与被改账同一套证据（对正文做词界搜索）去验新值，不能凭"哪一章听起来像"替换——否则只是把错账换个写法。**

- **（2026-09-24）Foundry 卡：一手证据改掉了正文里的一条命令**，这是本轮第一次出现"核实动作直接纠错案例章正文"的情况：
  - 案例三闸门二原文写 `forge test --coverage`。逐字读完官方 `forge test --help` 的完整选项块，**里面没有 `--coverage` 这个选项**；coverage 是独立子命令（`forge --help` 里 `coverage  Generate coverage reports`）。正文已改成 `forge coverage`，并把这条差异写进卡里当作可自查的坑（`forge test --help | grep -i coverage`）。
  - 官方 `forge test --help` 明列 **`--allow-failure`：逐字 「Exit with code 0 even if a test fails」**，且有 `FOUNDRY_ALLOW_FAILURE` 环境变量版本。**这正好是本书反复讲的"闸门被静默解除"的实物样本**：只判 rc 的 CI 在这个开关下永久绿。卡里的判据因此定成 stdout 的 `Ran N test suite ... : A passed, B failed, C skipped (T total tests)` 那一行，并要求 `skipped=0`、`total==清单条数`（案例三自陈 56 条不变量↔56 条用例，这一格就该是 56）。
  - 版本口径出现"文档自己就不自洽"的读数：官方 `reference/versions` 页自曝整份 CLI reference 生成自 `forge 1.8.0-nightly`（2026-08-04）与 `anvil 1.8.2-dev`（2026-09-13），而稳定版线已到 `v1.8.3`（2026-09-15）。**结论写进卡：抄配置前以本机 `forge --version` 为准，不以文档为准**；同页给出的 `forge config`（打印三层合并后真正生效的配置）成为"归档哪一份配置"的判据。
  - 台账同步：新增 Mythril / Slither 两行并判 **C 档 · 无卡**（本机未装、本轮无可核对判据行）——**正文点了名却出不了卡的工具，要在台账里留行，而不是假装没点名。** Sentry 当时同判 C，**当日稍后升 B 出卡**（见本节末条）。
  - 顺带核出案例三闸门三的一处口径漏洞：`anvil --fork-url` 官方逐字是"fork the **latest** state of a live network"——**跑两周的模拟盘若不钉 `--fork-block-number`，基准每天在动，"< 15% 偏离"这条判据不成立**。卡里补了 `--fork-block-number` 与 `--state state.json` 两条可抄项。
- **（2026-09-24）复验翻出来的三条，全部按实留在卡面上而不是抹平**：① Kafka `retries` 的"官方缺省 2147483647"本机没取到 `Default:` 读数，那一行改成官方逐字的 `delivery.timeout.ms` 约束句并写明缺省数字未取——**「看起来对」的天文数正是最贵的那种错**。② `docs.semgrep.dev/writing-rules/*` 的必需键表本机 404，卡里降级为"按转述登记，落盘前以官方页为准"（同站的 `cli-reference` 页后来通了，退出码因此能与源码常量并列对照）。③ `api.github.com` 本轮后半段开始返回 **403 rate limit**，Trivy 的小版本先按实写成"未复核"，**当日改走 `releases.atom` 后取到 `v0.74.0`（2026-08-14）**——**「取不到」有一半是取数路径的问题，先怀疑口径再落成「未核实」。**
- **（2026-09-24 同日更正）Foundry 稳定版读数按实改过一次**：先前登记 `v1.8.2 / 2026-09-15`，逐 `<entry>` 重解析 `releases.atom` 后是 **`v1.8.3`（2026-09-15T12:49:47Z）**、上一条稳定版 `v1.8.2` 是 2026-09-14。错因是**在整份 feed 上顺序配对 `<title>` 与 `<updated>` 两个字段**，而这个 feed 里稳定版与 Nightly 交错排列，09-15 那条 Nightly 的时间戳被配给了错误的版本号 ⇒ **取证脚本自己的正则也是一条读数**：跨条目字段必须先按条目切分再配。
- **（2026-09-24）交互闸查出"1 条真失败"却在打印它时崩掉——守卫看不见自己的结论，比站点缺陷更严重**：退出码 1 的理由是 `UnicodeEncodeError: surrogates not allowed`，真失败的内容被吞了。三层修：① `scripts/cdp.py` 加 `safe_text()`（先把合法代理对拼回码位、再把拼不回去的孤立代理位置成占位、**最后**才截断）+ 把 `stdout/stderr` 的 error handler 改成 `replace` 作全局兜底；② 崩溃点其实在 **JS 侧**——顶栏 `innerText` 逐字是「主页 / 📖 阅读指南 / 📚 全书总览 / 🗺 四案例时间线 / 🔎 搜索」共 **34 个码元**，而 JS 里 `.slice(0, 20)` 正好切在 🔎 的前半（本机实测末位码元 = `0xd83d`），故新增 `clip()` 成对地切；③ Python 侧三处 CDP 文本截断点全部改走 `safe_text`。**正对照四条读数**：合法对 → 长度 2；切在代理对中间的 95 字截断 → 结果无代理位（长 92）；真孤立代理位 → `abc\ufffd`；未走 `safe_text` 的裸打印 → `x?y` 而不是崩。
- **（2026-09-24）那条被吞掉的真失败最后判定为守卫自己的盲区，修完覆盖率反而 +1**：现场是 `#/manuscript/ch10-…` 的侧栏入口，目标矩形 `[12,16,275,30]`、中心 `(150,31)`，接住事件的是 `nav.app-nav`（`fixed`，`[0,0,1280,60]`，`z=200`）。**几何上不可能的部分才是线索**：`.sidebar` 是 `fixed / top=60 / overflow-y:auto`，被卷到裁剪边界之上的子节点，`getBoundingClientRect()` 照样给出 `top=16`——而 `anchor_box` 的 `ok()` 只拿**屏幕**的界（`top>=0`）判"可见"，于是既没触发重滚，也把一个点不到的坐标放行。修法：**判可见要用被判定对象自己的滚动口的界**（逐级向上取 overflow 祖先的矩形求交）。修完 62 条路由 `累计失败 0`，且**真实点击 65 → 66**：ch10 那一次以前从未真被点过——**假失败遮蔽的是覆盖缺口**，这一类"守卫报错"不能只当成噪音消掉。
- **（2026-09-24）配色闸的 `@media print` 豁免已撤**：豁免等于在闸上留一个"往里塞什么都不判"的房间，而同一函数里那条 `.token` 豁免就是这么让深档八个字面量合法活了一整轮改版。做法：打印档改成 `--c-print-paper` / `--c-print-ink` 两个令牌（两档同值——纸上白纸黑墨是印刷约定，不随屏幕主题翻），规则侧回到零字面量；同时补一条**打在豁免原址的正对照**变异 P8（把 `#fff`/`#000` 塞回 `@media print`，必须被"字面量色值"判据抓红），否则没人知道那间房是真封了还是暂时没人往里放东西。`mask-image` 那条豁免保留并写明理由：**遮罩通道里的 `#000` 是"不透"不是颜色**，收进令牌反而读错语义。
- **（2026-09-24）Sentry 从 C 档升到 B 档，升档理由比卡本身值得记**：判 C 的原由是"官方文档没有可 grep 的输出样例"——这句**当场复核为真**（文档对 `sentry-cli info` 只有一句用途说明）。可 grep 的字面量在源码 `src/commands/info.rs` 里（`Sentry Server: `、`  Method: `、无凭据时取值就是 `Unauthorized`）。⇒ **"文档里没写"≠"取不到"，但也≠可以不算出处**；卡里因此写明"那是源码不是文档"。判据落点也顺带成形：`info` 跑通、看到 `Sentry Server` 那行都不算接上，**看到 `Method: Unauthorized` 才算看到真相**。


### 收口（2026-09-24 第二轮）：新增判据、目视复核与三处自纠

- **第三条守卫新增一条判据：相对 HEAD 的小节标题零丢失**（`scripts/check_markdown.py`）。动因不是理论风险而是**本轮实测连犯三次**：用编辑工具改稿时把下一块的头部一起吃掉——ch27 少一段围栏（原判据能接住）、ch26 少 `## 21.5`、ch05 少 `## 3.3`（后两类**不破围栏、不破链接、Docsify 照渲染**，只剩一张没有标题的孤儿表格，读者再也跳不到那一节）。口径：拿 `git show HEAD:<file>` 当基线，剥掉编号后比对标题集合，**改名与重编号不算丢失**（互相包含即视为存活），只有"整块查无此人"才报红；并带覆盖集自证——**取到基线的文件数 <60 直接中止**，防止"比对集为空"被读成通过。已知答案自检 4 条全过：删一节→报 1 条；改标题名→不报；原样→0 条；`J.9→J.11` 重编号→不报。当前读数：**67 个文件有基线、丢失 0 条**（三处误删已全部补回，这条读数本身即修复的证据）。
- **配色改动后的 3 档 × 浅/深 截图逐项复核已完成**（第八条闸 `--screenshot` 出 366 页次，人工读了 6 格矩阵的代表页：文本页 ch10 与宽表页 ch05）：浅档暖奶白纸底 + 墨黑正文 + 单一铜绿强调，无纯白、无高饱和靛蓝；深档是重调的暖黑而非反色（强调色提亮为浅铜绿、图版卡仍是"纸色"，形成台灯下的纸这一层语义）；1440 档阅读栏仍锁在约 720px 测度、两侧留白加宽，没有拉长的行；390 档顶栏横向滚动 + 抽屉与换档按钮分离。两处"看着像 bug"的读图结论按实记为设计：**图 N-1 右端节点被卡片裁切**是横向滚动的既定暗示（`›` 提示在位、第七条闸验过暗示层不吃点击），**题图在 390 下看不见小字**是因为 9 张 plate SVG 实测 `<text>` 元素数为 **0**（纯线面抽象图，无文字可糊）——顺手把"第七条闸是否漏量了 plate 里的字"这个疑点用计数关掉，而不是留给下一轮猜。
- **卡片数字的出处机检**：正则扫六张工具卡里所有带 %／笔／条／天／分钟的数，命中 6 处逐一回溯——`12 万`＝台账 X-0719（并把措辞对齐成台账原单位「12 万笔精度偏差撮合」）、`97.4%`＝台账第 64 行、`15%`／`100%`＝案例三闸门三与台账「合约审计覆盖率 100%（23 个合约）」、`3 条 skipped` 是假设说明不是事实、`180%`＝案例三正文逐字命中但**台账漏登**。按台账自己那条「先回写清单，再进正文」的规矩，**在 ch05 补了 3.2b 一节**（回测年化 180% / 夏普 2.8 / 最大回撤 12% / 样本内 R² 0.91 / 样本外 0.23、衰减 0.68），卡片引用从此有锚。
- **散文里的快照计数继续清除**：「正文点名的 **15 件**工具」「README 里那**九件**」「**一个例外**」这类句子全部改成指向台账本身或把类别写回条目身上——台账此刻确实 15 行，但第 16 行落地那天这些句子就会静默腐烂，而腐烂的账比没有账更危险。同一轮把 `FIGURE_LIST` 里「去重后 **97 条**本地引用全部 200」也改成"以命令打印的那一行为准"，并纠正了一条写反的用法：`check_links.py` **不自带服务器**，必须先起 `http.server`；顺带记下它的 fail-safe 方向（服务没起 ⇒ 全部引用报死链、退出 1，不会把"取不到"洗成"通过"）。
- **（2026-09-24）八闸回归 + 四套变异自检的退出码（一次串行跑完，避免抢 Chrome）**：静态——第一条 `check_figures` **0**、`--selftest` 四对照 **0**、第三条 `check_markdown`（含新的标题丢失判据）与 `--selftest` 4 fixture **0**、第二条 `check_links` **0**（须先起 `http.server 8080`）、`plate_engine --check` **0**。浏览器——第四条遮罩 **0**、第五条交互全量 **0**（62 路由 × 1280/390，命中点 620、控件 5663、真实点击 66 次）、第六条窄屏 **0**、第七条可读性 **0**（1280/390 × 浅/深）、第八条配色 **0**（366 页次、113412+6912 个样本、独立口径 106740 个可见文本节点、真点换档 3 次、图 630/630 渲染出 svg、耗时 162s）。变异——交互 M1–M5 **0**（270 / 261 / 4 / 6 / 7 条红，逐条命中具名判据且子进程 rc=1）、窄屏 A–E **0**（20 / 20 / 6 / 4 / 5）、可读性 A–E **0**（14 / 44 / 91 / 88 / 17）、配色 P1–P8 **0**（175 / 2 / 19 / 2 / 3 / 1 / 4 / 1 条红，P8 命中「字面量色值」1 条 ⇒ `@media print` 那间房确认封住）。**本轮把第五条从"报红但报不出内容"修成"报得出内容"之后，它的变异集仍然是全绿的对照系**——修具没有顺手放宽判据。
- **（2026-09-24）终验顺序说明（避免"量测后又编辑"）**：上面的浏览器读数取自配色与内容编辑冻结之后；此后仅剩 `DIAGNOSIS.md` / `FIGURE_LIST.md` 两份元数据自身的写入与根副本同步，因此**最终字节上又重跑了一遍八闸**，日志在 `/tmp/bat_final_*.log`（本机，跑完即随会话留存），退出码以日志为准；本行只登记"重跑发生过、口径同上"。重跑读数：**11 项全 0**（四条静态 + `check_markdown --selftest` + `check_figures --selftest` + 五条浏览器闸）；配色闸在最终字节上跑到 **366 页次 / 正文样本 114006 + 图形样本 6912 / 独立口径 107676 个可见文本节点（未覆盖 2004）/ 真点换档 3 次 / 图 630/630 渲染出 svg / 耗时 135s**，结论仍是「静态 0 条 + 浏览器 0 条 = 0 处配色不达标」。
- **「深档是重做不是反色」补上一条可复算的读数**：拿 `check_palette.parse_tokens` 的浅/深两表逐令牌比 RGB 反色，抽样的 8 个关键令牌**没有一个等于自己的反色**——`--c-bg` 浅 `#fbf8f2` → 深 `#1a1815`（暖黑），反色应是 `#04070d`（冷蓝黑）；`--c-accent` 浅 `#2f6154` → 深 `#8fbdb0`（同族提亮），反色会是 `#d09eab`（粉）；`--c-desk` `#efeae0` → `#121110`（反色 `#10151f`）。两档同值的令牌只有 3 个且都是刻意不翻的：`--c-badge-fg`（照片上压墨的序号牌）、`--c-print-paper` / `--c-print-ink`（印刷约定）。**「不是反色」这件事以前只有截图为证，现在有了一个能重算的比法。**
- **窄屏浮动控件压字：已按"并入顶栏两行网格"收口（2026-09-24 下一轮）**。原先的登记是：390 档 `.sidebar-toggle` 与 `#btn-theme` 都是 `position:fixed` 的角上方块，正文通栏滚动时中途那一行必被盖住——改前实测 `[24,776,44×44]` 与 `[334,740,40×40]` 两格命中测试都打在正文 `<li>` 上。**为什么不是补 `padding-bottom` 这一招**：那只能救末尾几行，救不了滚动途中的每一行；只要控件是悬浮在正文之上的方块，遮挡就是结构性的。做法：`@media (max-width:768px)` 把 `--navbar-h` 从 60px 抬到 84px，顶栏改两行——第一行 40px 是"☰ + `::before` 生成的书名 + ☀↑"控件带，第二行 44px 才是原来那条横向滚动的导航条；两个方块从"浮在正文上"变成"嵌在不透明顶栏里"，遮挡边界从压在字上的一条方块边变成一条干净的发丝线。改后实测：`nav [0,0,390,84]`、`.sidebar-toggle [8,2,36,36]`、`#btn-theme [306,2,36,36]` 全在第一行内（y<40），`.content` 从 y=84 起；在正文可见区撒 20px 网格命中测试（x 8→390、y 84→844 共 741 点），落在任何控件上的点数为 **0**。真实点击链照旧：☰ 两次点击 `ready → ready close → ready`、☀ 一次点击 `data-theme: null → dark`。当初担心的"挤掉导航宽度"没有发生：导航条仍在整条宽度上横向滚动（428px 内容 / 366px 可视），只是不再和正文抢同一层。
- **（2026-09-24）第八条闸补上"伪元素里的字"这一类对象，并给它自己的 RED**：书名并进顶栏用的是 `.app-nav::before { content: '…' }`——它**既不在 `sel` 枚举的元素清单里，也不在 TreeWalker 的文本节点口径里**，等于全站新增了一处没人判色的正文。先手量了一次（浅档 9.57:1、深档 9.81:1，14px/600），但手量数字会在任何人下一次改令牌时腐烂，所以把它做成判据：取样器新增伪元素通道（读 `getComputedStyle(el, '::before'|'::after')` 的 `content/color/fontSize`，底色仍走 `backdrop(el)` 合成），并配一条 390 档自证——顶栏在场（`navShown`）而伪元素读数为 0 即报红，封面页整条顶栏 `display:none` 不参与。变异集从 P1–P8 扩到 **P1–P9**，且 `MUT_VIEWS` 加上 390 档：只在 1280 跑变异的话 `::before` 根本不生成，**这条判据就从来没有过一次 RED**，绿读数什么也证明不了。实测 P9（把书名指到合法的 `--c-border` 发丝线色）报红 4 条、命中「pseudo::before」，其余八条不受影响；全量跑下来伪元素读数 122 条 = 61 条正文路由 × 浅/深，与"顶栏在场的 390 页次"逐格对上。
- **（2026-09-24 下一轮）改版式后的回归读数（浏览器类守卫全部在最终 `theme.css` 上重跑）**：静态——第一条 `check_figures` **0** 与 `--selftest` 四对照 **0**、第二条 `check_links` **0**（106 条引用、死链 0；须先起 `http.server 8080`）、第三条 `check_markdown` **0** 与 `--selftest` 4 fixture **0**、第四条遮罩 **0**。浏览器——第五条交互全量 **0**（62 路由 × 1280/390，命中点 620、控件 5666、真实点击 85 次，0 遮挡 / 0 点不动 / 0 状态不变 / 0 控制台错误）、第六条窄屏 **0**（4 代表页 × 390，顶栏两行仍单行不折）、第七条可读性 **0**、第八条配色 **0**（366 页次、伪元素 122 条）。变异——交互 M1–M5 **0**（271 / 261 / 5 / 6 / 7 条红，逐条命中具名判据且子进程 rc=1）、窄屏 A–E **0**（4 / 40 / 6 / 4 / 5）、可读性 A–E **0**（14 / 44 / 92 / 88 / 17）、配色 P1–P9 **0**（350 / 4 / 37 / 4 / 3 / 1 / 8 / 1 / 4 条红）。**这一轮的对照点**：M1/M2 的红数与上一轮同量级（270→271、261→261），说明顶栏改动没有把封面遮罩那两条判据改松；窄屏变异 A/B 的示例矩形已从 `条 [0,0,390,60]` 变成 `条 [0,0,390,84]`，是量具跟着版式走了而不是判据被放宽。

- **（2026-09-24）第九条守卫 `check_render_leaks.py` 落地，全站"写了语法没渲染"从 127 处清到 0**：第一轮浏览器全量扫描（61 页 / 14744 个可见文本节点 / 渲染出 `<strong>` 2532 个）报红 **127 处**，全部是可见文本里残留的字面 `**`。修法分三层，每层都用同一个闸复算：① 机械变换 **72 次**（把 ASCII 引号整体挪到加粗外侧，或把加粗内侧的成对引号换成「」），带两条自缚条件——改完 `*` 的颗数必须不变、改完静态失败对必须变少，否则那一行不动；② 逐句手写 **18 条**（`old` 在文件里必须唯一、`new` 过静态复核，任一条不合格整批不写）；③ 剩下的按闸的第二次报红逐条处理，见下条。收口读数记在本节末尾的回归行里。
- **（2026-09-24）立闸的机理描述自己先是半对的：一种形状查成了四种**：`check_render_leaks.py` 最初的注释写着"加粗内容以引号开头时 marked 不解析"。这只对四分之一——127 处里绝大多数是这个形状，但 ch19、ch27、`DIAGNOSIS.md` 自己各落在别的机理上。两条独立口径重建了规则：从 `docs/vendor/docsify.min.js` 里读出内嵌 marked 15.0.12 的 `_punctuation` 字符集（**只列 ASCII 符号**，中文标点、全角引号、破折号对它都是"字"）与 `strong.start` / `strong.endAst` 两条正则，再造 39 条写法在浏览器里逐条实测（16 条被吃进、23 条留下字面星号）——静态预测在**可比的 33 条上 33/33 命中**（另 6 条样本自身 `**` 不成对，不参与校准）。最终登记的失效形状四条：**R1** `：**"…"**`（`**` 前不是空白/ASCII 标点、后又紧跟 ASCII 引号 ⇒ 开不出来）、**R2** 段内落单的一个 `**`、**R3** `。**` 紧贴 `**坑` 形成四个星号连串（两对的闭合判定同时失败）、**R4** `**` 后面紧跟空白（`start` 的 `(?![\s])` 直接拒绝）。四种形状各造一条反例进 `--selftest`，配五条修好的正例同页互证。
- **（2026-09-24）定位工具自己漏了三种形状，是浏览器补抓出来的——静态口径只能当定位器不能当判据**：第一版静态扫描器按"逐行成对取 `**`"实现，于是 ① 行内奇数颗 `**` 被配对逻辑洗白（它压根不报"多了半颗"），② `** ` 后跟空白的开侧失败没进判定（正则里没抄 `(?![\s])`），③ 四颗星号连串被"只取恰好两颗"的规则整条跳过。三条各对应一处浏览器已报红的真缺陷（ch19:125/127、ch27:225、`DIAGNOSIS.md:111`），**漏检的表现不是报错而是报 0**——扫描器说"静态待核 0 行"，闸说"泄漏 6 处"。补齐后的严格版（奇数颗、开侧空白、`\*{4,}` 连串各一条判据）在同一份字节上读数与浏览器一致。附带查清一件事：ch19 那两处的段末落单 `**`，真相是**引号只开不闭**（`说："……炸。**` 应为 `说："……炸。"`），所以正解是把那颗星号改回引号而不是删掉强调；顺带给严格版加了一条"段内 ASCII 引号奇偶"的体检，全站扫出 **1 处** 同类漏闭（第 23 章章尾"全员受益"/"全员抵触"那句），已补。
- **（2026-09-24）`--selftest` 第一次跑就把我自己写的正例打红了**：九条对照的首版里，R3/G3 那一对为了省事只截半句（`要么把判据落在摘要行上。** **坑在这里**：…`），结果正例报泄漏。查下去不是判据太松，是这条正例自己**前半段的 `**` 没有开侧配对**——单独成段时它本来就是残缺语法。把两条都补成完整两句（各含两对加粗）后 9/9 通过。这条要留在账上的理由：**反例报红只证明闸不瞎，正例报红才证明对照集自己也被判据管着**；正例如果不会失败，它就是一条装饰。同时这一跑也确认了 `。**` 与 `**坑` 之间留一个空格的写法在浏览器里真的能落地——它现在是 `docs/STYLE_GUIDE.md`「加粗与引号」里的一条规矩。
- **（2026-09-24）第三条守卫的「零丢小节」判据把本轮唯一一次编辑事故当场报红**：给 `docs/STYLE_GUIDE.md` 插入「加粗与引号」一节时，我的替换串以 `## 第一人称` 作锚、新串里忘了把这条标题写回去——结果标题被吃掉，三条原正文变成上一节的孤儿。`check_markdown.py` 报：`docs/STYLE_GUIDE.md: HEAD 里的小节「人称」在工作树查无此人（改名不算丢失）→ 多半是编辑时把下一块的头部一起吃掉了`，**这一跑 17 项矩阵里唯一的非零退出就是它（`3a-markdown rc=1`）**。补回标题后单跑 `python3 scripts/check_markdown.py` → **rc=0**（73 个 md、围栏成对、5 对根/docs 副本逐字节一致、67 个文件相对 HEAD 小节零丢失）。这条判据是"删除必报、改名与重编号必不报"的（`--selftest` 4 条 fixture 同跑 **rc=0**），本轮是它第二次在真实编辑上命中——上一条命中是它刚立起来的那天。同一类事故我在 `docs/DIAGNOSIS.md` 插入本节时也犯过一次（吃掉「全量校验命令」那条三级标题），那次靠 `grep` 数围栏块位置自查发现，**没有依赖守卫**——记下来是因为"这条靠人"和"上一条靠闸"的差别正是闸该不该扩的依据。
- **（2026-09-24）修复过程的编辑纪律**：数字与事实串一律不动——所有变换只挪「引号 / 星号 / 空格」的位置，`ch05-数字清单.md` 里的既有事实逐条原样保留（例如"人均 PR +8.69%、PR 合并率 +15%、成功构建 +84%"改的是强调罩住的边界，不是数）。每一批写入前后各跑一次严格扫描，量测与写动作在同一条链上；元数据（本文件与 `FIGURE_LIST.md`）在站点代码与正文冻结之后才写，根副本用 `cp -a` 同步并以 `cmp` 对账。
- **（2026-09-24）九闸回归（17 项串行一次跑完，避免抢 Chrome）**：静态——第一条图闸 **0** 与 `--selftest` 四对照 **0**（107 张图、每章 2–5、图号递增、节点 ≤12、悬空 0）、第二条死链 **0**（本地引用去重 106 条 URL）、第三条结构闸 **1→0**（见上条）与 `--selftest` **0**、第四条遮罩 **0**（1 个占满视口的 fixed 遮罩，层叠上能被赢）。浏览器——第五条交互全量 **0**（62 路由 × 1280/390，命中点 620、控件 5664、真实点击 85 次）、第六条窄屏 **0**、第七条可读性 **0**、第八条配色 **0**（366 页次、正文/图形样本 115892+6912、独立口径 110796 个可见文本节点（未覆盖 2004）、伪元素生成的字 122 条）、**第九条渲染泄漏 0**（61 页 / 14859 个可见文本节点 / 渲染出 `<strong>` 2557 个 / 泄漏 0 处）与 `--selftest` 九条对照 **0**。变异——交互 M1–M5 **0**（270 / 261 / 5 / 6 / 7 条红，逐条命中具名判据且子进程 rc=1）、窄屏 A–E **0**（4 / 40 / 6 / 4 / 5）、可读性 A–E **0**（14 / 44 / 92 / 88 / 17）、配色 P1–P9 **0**（350 / 4 / 37 / 4 / 3 / 1 / 8 / 1 / 4 条红）。另有 `plate_engine --check` **0**（9 张题图）。**对照点**：M1/M2 与 P1–P9 的红数与上一轮逐条同数（上一轮 271 / 261 与 350 / 4 / 37 / 4 / 3 / 1 / 8 / 1 / 4），说明这一轮只动了正文文本与守卫件内文字，没有把任何一条判据改松。矩阵与逐项日志在 `/tmp/bat9/`（本机，`/tmp/bat9_matrix.txt` 是 17 行具名 rc 矩阵），**读数与日志不符时以日志为准**；矩阵的覆盖自证是同一段脚本里的"行数必须 =17"，少一行就打印 `!! 矩阵只有 N 行`。**终验顺序**：这一跑之后剩下的只有本文件与 `FIGURE_LIST.md` 的元数据写入及根副本 `cp -a` 同步，因此按上一轮同一口径，在最终字节上又重跑了会读文本的四条（第一 / 二 / 三条 + 第九条），日志在 `/tmp/bat9_final/`；最终字节的计数写在提交信息里而不是这一行——把它抄进本行就等于作废本行引用的那一跑。
- **（2026-09-24）第十条守卫 `check_incidents.py` 落地：把清单与时间线里"说了没人执行"的规矩装上门卫**：本节末尾"仍待"那条自问自答了三轮，这轮补齐。事实源只有两个——`ch05-数字清单.md` 开头写"构造原则：与时间线逐一对应""任何新增数字必须先回写本清单，再进正文"，1.2 节写"一律以本表为准"；此前**没有任何一条判据读过这两句话**。七条判据（N1 编号尾四位 ↔ 本行日期；N2 时间线阶段标题必须自带区间，段内 90 行日期不得越窗；N3 同案 17 个阶段窗口严格递增不重叠；N4 清单 12 件事故的日期必须落在本案时间线窗口内；N5 正文出现的任何 `字母-四位数字` 编号必须已登记（点名处逐条判，命中数由闸自己打印）；N6 每件事故正文至少点名一次；N7 编号与完整日期同写一行时须等于清单日期）。`--selftest` 九条对照全绿：变异 M1–M7 各自按机制报红（含"改一个编号会连带打红所有点名处"的级联，条数随语料漂移、属预期），两条正例（同窗口换另一天、编号配对的合法日期）必不报；另加一条**控制跑**——未变异的副本必须先报 0，否则整套对照都不成立。
- **（2026-09-24）三条候选判据被现网读数否决，所以闸比它看起来窄**：写闸前先把每个候选判据在真实字节上量一遍命中面与假阳性，三条没通过就**不做**，并写进件内注释。① 「点名事故时同行的钱数须等于清单损失估算」——13 处命中里 10 处合法（D-0114 的 15 分钟窗口、Z-0612 的回测三数在 3.2b 不在事故行、X-0719 那行顺带写了 KPI 百分数），假阳性 77%，弃。② 「事件名不带编号、与日期同现须一致」——从 ch03 读出 59 个事件名，他章复用这些名字时**同行带日期的命中 0 处**，做了就是一条恒绿装饰，弃。③ 「四案例数字禁止交叉引用」——正文确有合法的跨案对照（`ch35:315` 是给案例一的公开信、ch38 整章就是交叉启示），机械禁令会把合法件全打红，**这一轴至今无守卫**。「编号与日期同句」在时间线章内也量出两条合法形状（`2025-08-14` 的大会复盘行点名 D/E/F、`2025-02-14` 的 Y-0820 前奏行），所以 ch03 被排除在 N7 之外——**N7 今日的命中面是 0 行**，它的牙齿只由自检桩证明，别把这条算进"闸已经拦过什么"。
- **（2026-09-24）第十条守卫在立闸当轮就打红过两处，两处都是我自己写的字**：① 第九条渲染泄漏闸把新写的一处粗体报红 2 处——粗体的闭 `**` 紧跟在反引号包起的文件名之后、后面再接全角逗号 `，` 时，marked 不解析这组粗体（正是它自己登记的 R1 形状）。修法是把粗体重排到以汉字收尾的短语上，不是把 R1 放宽。② 第十条自己的 N7 在本文件的登记行上报红 3 次：描述一条判据的那行字，把事故编号和完整日期写进了同一行，而这正是 N7 禁止的形状。这不是误报——工程记录里写"编号＋日期"是在**说明规则**，不是在**断言事实**，而 N7 分不出这两者。取值方式：把 N7 的适用面从全站 md 收窄为 `manuscript/` 下的书稿正文（时间线章仍单独排除），闸内注释与本条同步记下这个取舍；为了变绿去改写那条记录行，是这一次没做的事。
- **（2026-09-25）全站图号撞号收口，第一条守卫加上第三条判据**：判据叫「图注编号全书唯一」。`FIGURE_LIST.md`「下一轮」第 2 条把这件事记成"前缀混用、语义清楚但不齐整"，量的结果比这严重——**107 条图注只有 102 个唯一编号**。ch02 与 ch04 各自把三张图编号成 `R-1/R-2/R-3`（甘特／角色负荷／分叉 对 博弈轴／小团队博弈轴／对话指纹），站点首页与定位章各有一对 `0-1/0-2`。也就是说读者手里的"见图 R-2"从来不指代任何一张确定的图，而这是**六张图共用三个编号**，不是排版不齐整。为什么两条旧判据全绿：引用解析拿的是 `set(全书编号)`，撞号在 set 里塌成一个，"引用可解析"照样成立；"图号递增"是**每文件局部**性质，两个文件各自递增互不相干。**教训写进 BOOK_SPEC §9**：递增是局部性质，可引用性是全局性质，所以唯一性判据必须按整副语料判，不能按文件判。改法（12 处图注与正文引用）：ch04 `R-`→`PS-`、ch03 `00-`→`TL-`、站点首页 `0-`→`HM-`，前缀由"这一章自己的名字"决定并列表进 BOOK_SPEC。**判据落地三步**：`check_figures.py` 新增撞号判据（报红时打印撞在哪几章的哪两张图，含图注标题）；`--selftest` 加一条**两文件** fixture（单文件 fixture 证不了跨文件这一半）；按还原事故形状量真件——把 ch04 改回 `R-` 后 `rc=1`、3 条撞号、唯一编号 104，还原后 `rc=0`、107 条 / 107 个唯一编号。**回归**：矩阵仍 9 行具名 rc；首轮 `/tmp/bat11/` 里第九条报红 2 处，红在本条登记行自己身上（粗体闭 `**` 前是 ASCII 引号、后是全角冒号，正是上面刚记过的 R1），改字后最终字节日志在 `/tmp/bat11_final/`；**读数与日志不符时以日志为准**，最终字节的计数只写进提交信息。
- **（2026-09-24）第十条守卫回归**：本轮没有动正文、主题与图，所以浏览器类闸按"量测被后续编辑作废"的口径只在最终字节上重跑会读文本与渲染的五条（第一 / 二 / 三 / 九 / 十条，其中四条各跑全量与 `--selftest` 两跑，第二条只跑全量）。首轮日志在 `/tmp/bat10/`，写入本行与上两条之后的最终字节日志在 `/tmp/bat10_final/`；两处矩阵各 9 行具名 rc，脚本自带"行数必须 =9"的覆盖自证（首轮这条自证按错的常量 10 报过 `!! 矩阵只有 9 行`，是脚本自己算错分母、不是漏跑）。**读数与日志不符时以日志为准**，最终字节的计数写在提交信息里而不是这一行——抄进本行就等于作废本行引用的那一跑。

- **（2026-09-25）全书唯一一处"空标题"补实，第三条守卫加"空节"判据**：这一轮不再从图与配色入手，而是把每个标题问一句"下面有能读的东西吗"。全站 73 个 md 扫下来只有一处为空：`ch33:165` 的 `### 3. 回滚决策矩阵（见 28.4）`——28.5 自己写着"可落地的四件"，第三件只有一条指针。代价不在版面上：附录 A 的 `P-28-4-01` 提示词要求 AI"基于回滚决策矩阵"给建议，读者与模型一起指向空处。补齐为八行矩阵（触发信号｜立即动作｜拍板人｜决策时限｜多端联动｜事后动作），数字全部沿用本章既有口径（端崩溃率 0.5%、核心接口错误率 1%、高风险 30 分钟 / 一般 10 分钟的观察窗口），新增的唯一一条例外授权（"到点无人决策即按回滚处理"）在正文里点名它是默认值、只在损失随时间扩大的链路上成立。**判据**：`check_markdown.py` 新增"小节子树不得为空"，口径按**子树**算（分组标题天然豁免、"标题 + 一段围栏"的提示词卡也豁免），补完那处之后全站命中 0；`--selftest` 从 4 条增至 8 条（标题闸 4 + 空节闸 4），真件红证明走"还原事故形状"：把该节改回指针标题 → `rc=1`、共 1 处结构问题，还原 → `rc=0`。**顺带记一条闸自己的缺口**：`STYLE_GUIDE` 里「事实与编号」与「事故编号」两节规则重叠，想删后一节时第三条守卫的"零丢小节"判据把它当编辑事故报红了——判据没有"这是有意删除"的记账口，所以本轮**没有放宽闸口**，改成给两节各自划界（上方管一致性、本节管写法）并互指。要么下一轮给这条判据加一个具名的删除台账，要么承认"任何标题删除都必须先复原"。**同一轮我又吃了同一个老错**：往本节末尾插这一条时，替换串以 `### 全量校验命令（可复核）` 作锚、新串里忘了把标题写回去，于是这条标题被吃掉。本节上方记着 2026-09-24 我犯过同一口饭（那次靠 `grep` 数围栏位置自查发现，没有依赖守卫）——这一次是"零丢小节"判据当场报红抓到的，补回标题后单跑归零。同类编辑事故第三次，第一次与第二次靠人，第三次靠闸，这正是"该不该扩判据"的答案来源。**回归**：九行具名 rc 矩阵（`/tmp/bat12/`）全 0，第四至第八条浏览器闸按"改了正文表格就要重看窄屏与字号"重跑全量（`/tmp/bat12_browser/`）也全 0——新矩阵是六列宽表，横向滚动暗示由 `index.html` 的表格渲染器无条件套 `.table-scroll`、再由补层挂 `.scroll-frame`，第七条枚举的正是 `.mermaid-block, .table-scroll` 两类容器，所以它一进全站清单就被这条判据看见，覆盖它的正是 B/D 两条变异；写入本段回归说明后的最终字节再跑一遍同一矩阵，日志在 `/tmp/bat12_final/`。**读数与日志不符时以日志为准**，最终字节的计数只写进提交信息。

- **（2026-09-25）第七条守卫补上覆盖率读数：把「表格被滚动层包住」从代码推断改成双口径等式**：上一段收尾时我写的是"第七条枚举的正是这两类容器，所以新表一进清单就被这条判据看见"——那句话在代码层是对的，但它不是一条读数：这条判据的对象数从来没有露面过，`--report` 只逐图打印字号，通过行只说"全绿"。六列宽表当时确实被 B/D 两条变异覆盖，可"覆盖了几页几张"没有任何数字能反驳我，这正是「指标可失明」的教科书形状——判据整条空跑，退出码照旧。补三层：① 探针在判溢出**之前**先按类别数容器，于是"枚举到几个"与"其中几个真的要滚"分成两个数；某档某类容器一个都不溢出是现状不是缺陷（桌面档全站 238 张表只有个位数需要滚），把 0 直接当红会把判据调歪。② 新增 markdown 侧独立口径 `markdown_tables()`（沿用图那套围栏嵌套规则，代码块里的管道行不算），与 DOM 侧 `.table-scroll` 数逐页**判相等**而不是判有无——`index.html` 的表格包裹是无条件的，漏一张就该红。③ 台账每档只按首个主题记一次（逐主题累加会把同一批容器乘上主题数，读数失去"全站几张宽表"的含义），并且红与绿两条路径都打印这一行读数。**新拒判有读者**：变异自检从 A–E 增至 A–F，F 把渲染器那层包裹摘掉，等式两边当场差起来。真件读数在 `/tmp/bat13_browser/`（第七条全量与变异自检各一条日志），本行不抄计数——抄进来就等于作废本行引用的那一跑。

- **（2026-09-25）覆盖率读数当天就抓到一条真洞：首页正文从来没进过浏览器类四道闸**：这条判据立起来的下一跑，两个口径就报出不相等——DOM 侧 `.mermaid-block` 全站数到 105 个，markdown 侧独立口径数到 107 张围栏。差的不是渲染器漏包，而是**分母少了一页**：`check_legibility.py` 的路由枚举走侧边栏 `.sidebar li a`，而首页 `#/` 不在侧边栏里；第五、六、七、八条闸都从同一份枚举出发，于是首页正文那两张图和它的一万四千余字从来没被量过。此前本文件与 `FIGURE_LIST.md` 里"全站 62 路由 × …"那句话，"全站"两个字一直是推断，不是读数。**取证**（一次性 CDP 探针）：落在 `#/` 时页面只有封面，可见正文两千余字、`<svg>` 0 个；真点封面上的「全书架构」入口揭幕之后，正文一万四千余字、图 2/2 渲染出 svg——同一条 URL 有两种"页面"，旧的四道闸量的都是前一种。**修法**（本轮只动第七条，另三条的欠账登记进「仍待作者/第二轮」）：路由枚举保留 `#/`；首页必须真点一次 CTA 才有正文，点击坐标由 `COVER_CTA_JS` 在 `section.cover a` 里按文本寻址，找不到就报红「正文无从揭幕，本页读数作废」并跳过该页读数（这条拒判在封面尚未挂载时真的命中过一次，是有牙齿的，不是装饰）；`md_file()` 把归一化后为空串的首页路由指回 `README.md` 而不是 `.md`；`--pages` 解析出 0 条路由即中止，防止"传了参数却量了空气"的假绿。**还欠三条**：第五、六、八条仍然跳过首页——要么补揭幕再量，要么把这三条打印与文档里"全站"两个字收回到"侧边栏可达的 61 页"。补齐之前，任何写着"全站"的浏览器读数都带着这个已知缺口。**改动外溢到了第三个读者，而且是它先报的红**：`check_render_leaks.py`（第九条）本来就 `from check_legibility import routes_two_ways`，我改枚举时只想着第七条自己要量首页，没有去数"还有谁 import 了这个函数"。第九条当场报红：`✗ [] 走到 0 个可见文本节点——覆盖集为空，不算通过`——它按新枚举走到了首页，看到的却是没揭幕的封面，而这条"覆盖集为空不算通过"的判据正是它自己立的。**这不是回归，是闸抓到了本闸口径的半成品。** 同一轮把揭幕那一步从第七条的采样循环里抽成 `dismiss_cover(b, base, path)`（找不到入口就返回一句可直接进 fails 的原因，调用方据此作废本页读数），第七、九两条共用。改后第九条从 61 页变 62 页：可见文本节点 15241→15348（首页那 107 个从此在账上）、渲染出 `<strong>` 2643→2668、泄漏仍 0。**同一条错第二次落地时，代价仍然要靠闸而不是靠我记得**——本文件上方早就记过一条"守卫的枚举清单会漏掉第三个读者"。**同一轮第四次吃同一个老错**：给 `check_legibility.py` 插入函数时，替换串以 `def routes_two_ways(...)` 那一行作锚、新串里忘了把它写回去，于是函数定义行被吃掉，`python3` 当场 SyntaxError。2026-09-24 那两次是 markdown 的小节标题（一次靠 `grep` 自查、一次靠零丢小节闸），这一次是 Python 的 `def`——**凡是锚在"某个结构的头一行"上的替换，做完必须先跑那个结构自己的语法/结构闸再继续**，这句写进本文件而不是只写进脑子。

- **（2026-09-25）把揭幕链接到第五、六、八条：一条真点顺手抓出一个读者天天踩的缺陷**：上一段留的欠账是"要么补揭幕、要么收回'全站'两个字"。选补，理由是这三条打印里"全站"是读者与模型判断覆盖面的唯一依据，收窄措辞等于把洞改写成口径。**接法**：`dismiss_cover(b, path)` 仍然只有一份（在 `check_legibility.py`），第五条在 `probe_route(..., dismissed=True)` 里对首页多跑一遍正文判据、第六条把 `""` 加进代表页、第八条把它加进 `MUT_PAGES` 与采样面。**新拒判各自配读者**：第六条变异 F、第七条变异 G、第八条变异 P10、第五条变异 M6——四条打的都是**入口**而不是渲染件（把封面那条「全书架构」改名），因为这一轮新增的事实不是"图渲染得对不对"，而是"读者到不到得了正文"。M6 还带子进程退出码复核（rc 必须为 1），沿用第五条既有口径：报红≠会拦。**真点抓到的东西不是闸的预期产出**：首页 CTA 原本写 `[全书架构](#/)`，Docsify 把以 `#` 开头的 href 当**封面页自己的锚点**编译，落地成 `#/?id=…`，随后 `$resetEvents` 拿 `#/` 去 `querySelector` —— 未捕获的 `SyntaxError: Failed to execute 'querySelector' on 'Document': '#/' is not a valid selector`。也就是说这条"进正文的唯一入口"一点就在控制台抛错，而它抛错之前封面也没收起：读者看到的是**点了一下没反应**。第五条的 `drain_events` 本来就能抓到 `Runtime.exceptionThrown`，抓不到是因为**从来没有人真点过它**——这正是 `location.hash=` 赋值冒充点击的代价，赋值那次跳页会连"入口能不能用"一起跳过。中途试过的 `#/README` 也是错的（`'#/readme' is not a valid selector`，封面照样不收起）；真修是写成路由链接形态 `[全书架构](README.md)`，与封面上另两条入口同形。落点判定因此从"必须等于 `#/`"改成 `same_landing()`（只比 `?` 之前的页面段，允许首页落在 `README`——锚点是那条 CTA 自带的，不是走错了页）。`index.html` 里那段"点封面链接后手动摘掉 `.show`"的兜底监听同时**退役**：它存在的唯一理由就是绕开上面那个不收起，入口修好后它就是死代码。**读数随口径一起动**：第八条的采样面从 366 页次涨到 372 页次、图 630/630 涨到 642/642（首页 2 张 × 三档 × 两主题），第七条 `.mermaid-block` 的分母从此与 markdown 侧独立口径对齐（105≠107 那笔差进零），第六条代表页 4→5，第五条打印行新增"其中封面揭幕后的正文 N 遍"。**最终字节的回归与计数只看日志**：文本九行 `/tmp/bat15_r29/`、浏览器六行 `/tmp/bat14_browser_r29/`，四条变异自检日志 `/tmp/mut_g{5,6,7,8}_*.txt`；读数与日志不符时以日志为准。**同一次改注释把第三条守卫的一个口径缺陷顶了出来**：它的"相对 HEAD 不丢小节"判据用 `heading_titles()` 逐行正则取标题，**不认识围栏**，于是 `FIGURE_LIST.md` 命令块里那五行 shell 注释被当成小节记账——改一次措辞就报五条"查无此人"的假红（本轮实测第一次就是这样红的）。修法是与空节闸共用同一份围栏感知口径 `heading_kinds()`；改前后标题读数 871→790，差的那 81 条全部落在 27 个文件的围栏里（提示词卡与代码示例内的 `#` 行），本来就不是读者能跳到的节。这条新口径有两条自带基线的 fixture 把住：围栏内注释改措辞必须不报、**同一段文本里**围栏外的真小节被吃掉必须报 1 条——正反面放在同一份文本，否则"不报"可能只是因为整段没被读。顺手把封面另外那条最显眼的链接也真点了一次做对照：**站名**（`section.cover .cover-main h1 a`，class=`anchor`，href `#/?id=ai-…`）真点之后封面**不收起、也不抛错**——它是 Docsify 给当前页标题生成的**同页锚点**，点它=滚到自己，这是正确行为不是洞；已删掉的那段兜底监听当年恰恰是在给这个锚点强行摘封面，属于把正常行为改坏。判据：封面上四条链接按 href 分两类，以 `#` 开头的走锚点分支，其余走路由分支——只有路由分支会收起封面。

- **（2026-09-25）"明亮一些"不是一条令牌方向，先量读者感觉到的"灰"压在几个面上**：纸感改版之后，浅档最压光的三层是页底 `#fbf8f2`、图版卡底 `#f5f1e7`、卷首位图上那条 `rgba(30,28,25,.22)` 的暗部压带。第一次提亮只动页底，四代表页 × 浅/深截图前后对比**肉眼读不出差别**——单动一层改不动整体印象，因为"灰"是三层叠出来的。第二次按三个面各动一刀，并且动完就把动了谁写进账：浅档 78 个令牌改 **32** 个（22 个非图版 + 10 个 `--c-plate*`），页底 `#fbf8f2 → #fffefb`、书桌 `#efeae0 → #f7f5f0`、图版纸 `#f5f1e7 → #fbf8f1`、压带 `.22 → .12`；深档 47 个覆盖令牌改 **21** 个（`--c-desk #121110 → #191713`、`--c-bg #1a1815 → #211e1a`）。"亮"由此是三处可核对的位移，不是一个形容词。
- **纸底一提亮，强调色只能往深走**（`--c-accent` 由 `#2f6154` 收到 `#2b7159`）：这一轮的"变暗"和"变亮"同时发生，不是手抖。第八条算的是**逐层 alpha 合成之后**的对比度，底色抬亮等于把前景的余量吃掉一块；实测 `accent on bg` 从 6.70:1 落到 5.77:1，最坏那一层（铜绿压在 `--c-accent-soft` 上）从 5.80:1 落到 **4.89:1**——换成 `#2b7159` 是把它拉回 4.5 地板之上的那一步，淡一档 `--c-accent-light` 同向从 `#6f9a8e` 收到 `#5e8f81`。同一名目在页底上的读数：`--c-text-3` 5.27→5.54:1、`--c-text-2` 9.57→10.06:1。**结论是一条口径**：改配色时"更亮"与"看得清"是一对反向的力，余量必须从前景回收，不能只报表面变了多少。
- **第二段没再动深档，理由是一个算出来的数**：当前深档页底 `#211e1a` 上 `--c-text-dim #948a79` 是 **4.88:1**、`--c-text-3 #9d9486` 是 5.54:1；把页底再抬到现 `--c-bg-soft` 那一档（`#2a2621`），dim 掉到 **4.42:1**，而第八条地板是 4.5:1。**再亮一档就得把 dim 文字调浅来还债，那等于"亮"换了个地方输**。所以这一轮的"为什么不改"和"改了什么"同权重登记：守卫的地板不只用来拒绝坏改动，也用来拒绝看起来更多的改动。
- **（2026-09-25）图版令牌一动，它有四类派生读者：前三类当场报红，第四类静默**：`--c-plate*` 之外另有三处在抄它——书稿 **160 条 mermaid 指令行**（43 个文件、**480 个取色**）、`index.html` 的 **16 项兜底表**（本轮改 11 项）、题图与位图两类图。前两类由第八条按机制打红（「不在图版令牌里」「兜底值 ≠ theme.css」），SVG 由引擎自己打红：`plate_engine --check` 在令牌一动之后当场报出 **9 张图 26 条板外色**，`--emit` 重发即归零。**位图一条红都没有**：`<img>` 对外只有一个盒子，DOM 量不到像素，"令牌改了而图没跟着重跑"在整个闸上不存在——这一轮真的把 14 张 webp 按新三锚重着色（复跑一次字节逐张不变，说明重着色到锚是幂等的），但那是人记着的，不是闸抓的。**补法不是想办法看像素，而是让写手自己落凭据**：`recolor_plate_art.py` 只在零不达标的那一跑之后写 `docs/assets/plate-art-anchor.json`（当时的三个锚值 + 重着色张数），第八条逐键判相等，缺回执 / 少键 / 键在 theme.css 查无 / 值不等 / 多未知键 / 张数不等六种形状都报红，另配变异 P11。**为什么不用像素距离**：试过——一张刚重着色完的图离**自己的**锚有 2.4–5.4 的逐通道距离（版画带渐变和抖动，"纸底"本来不是一个纯色），而停在旧锚的 HEAD 图离**新**锚只有 2.9–4.9，两个区间重叠，距离口径分不开"新图"与"旧图"，做出来既漏又误报。回执口径的代价也说清楚：它判的是"有没有按当前令牌重跑"，像素本身仍由脚本自己的冷调 / 暖调 / 墨线三条读数把住（`--check` 只量不改，改前改后各跑一次）。
- **（2026-09-25）变异载荷里写死的色值，会在改色那天把坏样本悄悄变回好样本**：第八条 `--mutate` 在新字节上跑出 **P4 存活**：`报红 486 条，命中「令牌一个都没变」0 条`。486 条说明闸在忙，0 条命中说明它忙的不是 P4 那件事。根因在样本自己：P4 的坏形状是"深档被更靠后的同名规则指回**浅档值**"，那段 `<style>` 里的四个色是提亮**之前**手抄的浅档值；浅档一动，它指回去的就不再是浅档，于是 `data-theme` 翻了、令牌也确实跟着翻——判据报"没坏"完全正确。**样本不再坏，就不是变异**。改法：载荷由 `parse_tokens(THEME.read_text())` 现取四个浅档令牌拼出来，取不到键即 `SystemExit`；P11 的锚串同样现拼，并加一条前置自检——"纸底"与"节点底"必须是两个不同色，否则那个坏样本不坏（自检取到空值或相等值必须让脚本停下，不然跑的就是一条装饰）。修完实测 P4 报红 6 条、命中「令牌一个都没变」6 条。
- **P11 首跑也"存活"，这次抓住的是判据自己的措辞**：红确实报了（`--c-plate 回执 #f1ece1 ≠ theme.css 的 #fbf8f1`），但命中针 0 条——针写的是「锚点回执」，而**值不等**这一支的措辞只有"回执"两个字，其余五支都带全名。机制在工作，记账口径没跟上；这种"变异存活"最容易换来的反应是去松判据，而该改的是措辞：**一条判据的每一支红话都要带上那根针**，否则下一次改文案会把一条真 RED 洗成假红。改后六支全含「锚点回执」，复跑 **P1–P11 全部命中、rc=0**。
- **（2026-09-25）本轮的改动面与回归顺序**：字节改动是 `theme.css`（浅档 32 / 深档 21 个令牌）+ `index.html` 兜底表 11 项 + 43 个 md 的 160 条指令行 + 9 张题图 SVG + 14 张位图 + 两个守卫件（`check_palette.py` 加一条判据与 P11、`recolor_plate_art.py` 加回执写手）+ 一张新落盘的回执。顺序按"量测之后的任何编辑作废该量测"走：五类派生件先全部改完并复算 → 再动守卫件 → 最后才写本文件与 `FIGURE_LIST.md`。**写在最后的那一支仍然被打红**：文本矩阵首轮 `09_leaks rc=1`，2 处泄漏全在本轮新写的这一段自己身上——粗体闭 `**` 紧跟在反引号包起的令牌之后、后面再接全角冒号（正是上方 2026-09-24 已登记过两次的 R1 形状），改法是把粗体收在汉字上、把令牌与色值移到括号里，而不是把 R1 放宽。这一类错我在立闸那天记过"同类编辑事故第三次，第一次与第二次靠人，第三次靠闸"；本轮是**渲染泄漏闸第四次在真实编辑上命中**，命中的仍然是我新写的那一行——**登记不等于免疫，只有闸在跑才算**。文本九行矩阵、浏览器六行矩阵与四条变异自检日志分别落在 `/tmp/bat15_*`、`/tmp/bat14_browser_*`、`/tmp/bat16_mut_*`（本机，跑完即随会话留存）；**读数与日志不符时以日志为准**，最终字节的计数只写进提交信息——抄进本行就等于作废本行引用的那一跑。"改过 `--c-plate*` 必须重跑 `plate_engine --emit` 与 `recolor_plate_art.py`"这条规矩住在「全量校验命令」那两行新增命令里，而不是住在散文里：住在命令里它会自己报红，住在散文里它要靠我记得。
- **（2026-09-25）提亮把"看得清"和"分得开"拆成两件事：对比度全绿的一族色，可以在图例里撞成一档**：提亮之后第八条的浏览器与静态口径都是 0 红，而图版分色族（书稿里真被 `fill:` 用到的那几档）在浅档上两两之间的**色差**没有任何一条判据在看——对比度只算亮度，两档只要都够亮就一律 1.0x:1 永远绿。补一条 CIELAB 判据：按**令牌名**枚举族成员（不是按面值，P12 就是打在这个区别上：成员若按面值派生，改值即掉出分母），族内两两 ΔE≥4.0、每一档离纸底 ΔE≥4.0。当前读数：8 档 × 浅档 = 28 对全过，最紧的一对 4.84（`--c-plate-gray` 与 `--c-plate-node`），最贴纸底的一档 5.54（`--c-plate-node`）。**余量必须打印**：地板是 4.0，贴着 4.0 过和宽裕地过不是一回事，而下一轮提亮只会往"更亮＝更贴纸底"走。两个分支各配一个坏样本（P12 同族两档撞在一起、P14 一档被深档覆盖到离纸底太近）；另有一条防豁免名单腐烂的 P13：角色色 `--c-plate-edge` 只许描边不许当底纹，一旦书稿把它 `fill` 了又没有同值档接管，就报「角色豁免」。
- **（2026-09-25）焦点指示器是第四个房间，前三个都量不到它：判据取像素，不取声明**：WCAG 1.4.11 的"非文本对比度 ≥3:1"整本书此前无人管——键盘用户唯一的定位手段就是那一圈环。立闸口径三条纪律：**（1）必须真按 Tab**（`el.focus()` 同样满足 `:focus-visible`，用它取数等于自证）；**（2）每一站取一次视口像素**（`deviceScaleFactor: 1`，CSS px 与像素 1:1），因为计算样式读得出"作者声明了什么"，读不出"这一圈有没有被祖先 `overflow` 裁掉、有没有被更上层盖住"；**（3）两把尺子互证**——几何侧算"控件留在自己祖先滚动口内的比例"，像素侧数"四边里几边真画出了环色"，二者独立，谁单独说话都不算。第一次真跑就抓出三处读者天天踩的洞：窄屏抽屉收起后**仍在 Tab 顺序里**（焦点跳到一个看不见的控件上，改法是 `visibility:hidden` 让它离开遍历）；焦点落在正文宽表/宽图右侧时 **Chrome 不做横向滚动**（浏览器只保证纵向可见，于是补 `focusin` 揭示：沿 `overflowX: auto|scroll` 的祖先把停靠点滚进口内）；`.mermaid-block`/`.table-scroll` 这类**可滚停靠点根本没有环**（它们浏览器给焦点但没有选择器覆盖，`outline` 停在 `auto`）。当前读数：8 次走查 × 8 站 = 64 站，环对邻边最紧一站 4.84:1（地板 3.0:1），最小可见占比 99%（地板 98%），每次走查 5.8–6.0s，溢出可滚的停靠点 1–2 站、Tab 落空 0 次。站数地板 `FOCUS_MIN_STOPS` 定 6 而不是 8：留出"重复一站"的余量，但 Tab 如果在同一个控件上转圈、或整片停靠点被摘掉，这一条立刻红。
- **（2026-09-25）焦点环的第一条真红，顺着像素挖出了一个全站级的口径错误：运行时注入的 `<style>` 是同特异度的第二个宿主**：走查跑到搜索框那一站报「环下侧邻边 #eee，1.60:1」。#eee 不在 theme.css 里——docsify 的搜索插件用 `Docsify.dom.style(...)` 在运行时**往 `<head>` 追加**一条样式表，它排在 `theme.css` 之后，**同特异度即赢**。于是 theme.css 里 `.search{…border-bottom…}`、`.search input{padding…font-size…}` 三条声明从来没有生效过，页面上画的是插件的 6px / #eee / .6em 与 16px。修法不是 `!important`，是给选择器加一个元素名（`div.search` 0,1,1 压过 `.search` 0,1,0）——**这个元素名是承重的，删掉它页面看起来一模一样**，所以必须换成判据：新增「生效对账」账本（`EFFECT_LEDGER`，当前 4 行：搜索容器下边框、输入框上边框/底/字色），逐页读**计算值**并与令牌面值对账，五种红话各有名字（没有读者／读出来是空／不是可比较的颜色／被别的宿主赢了／账本没翻开），变异 P17 走的正是"把 `div.search` 简化回 `.search`"这一支。这一条的通用教训写进判据而不只写进日志：**「文件里写了」与「页面算出来还是它」是两个事实**，凡是与运行时注入表竞争的声明，都要有一行账本。
- **（2026-09-25）同一个口径错误在源码里还留着一格：`meta theme-color` 是提亮前 `--c-desk` 的手抄件，而没有任何闸看它**：`index.html` 第 9 行写的是 `#efeae0`，现值 `#f7f5f0`。运行时 `applyTheme` 每次换档都会把 DOM 里这一格改写成 `ainseToken('--c-desk')`，**所以「页面看起来是对的」掩盖了「源码字节是旧的」这一层**：首屏渲染之前浏览器读的是源码值，标签栏/地址栏先闪一下上一版配色。这与位图派生件（P11）同形：令牌有抄件、抄件没有闸。补法刻意不写成"再加一行 if"——那正是清单会漏第三个读者的老路——而是**从文件派生**：把 `index.html` 里每一个 `#rgb/#rrggbb` 找出来（先剔掉 `//`、`/* */`、`<!-- -->` 三种注释：真文件的注释里正好写着 mermaid 默认主题的 `#eee #999 #333 #707070`，不剔注释的第二天就会有人为了过闸去放宽判据），落在"已登记宿主"字符区间之外的一律报「未登记」。当前读数：枚举到 17 处字面量、宿主 2 个在场（兜底表整块 + `meta theme-color`）、表外 0 处；两个分支各配一个坏样本（P18 抄件停在另一个令牌的值、P19 表外凭空多出一格）。
- **（2026-09-25）量具自己也要过堂：这一轮有三次"红"最后是探针的错，不是页面的错**。(1) 390 档报「真点 (195,499) 之后封面仍在」——查下来是同一条路由相邻两次读到 CTA 中心 y=499 与 y=751（差 252px，封面揭幕动画路上），改法是把"落定"写进量具（`cover_cta` 要求连续两次读数相同才点），并且**把"量具不稳"与"产品缺陷"分开报**，否则读者会去修一个不存在的洞。(2) 焦点判据的假阳对照第一次就红在干净页面上：PIL 取像素给 `#rrggbb`、计算样式给 `rgb()`，`rgb_of` 只解得动后者，于是"像素上只数到 0 侧"打在完全干净的记录上——两种写法都要认。(3) 悬停态探针报"字对底 1.38:1"：它把 `rgba(43,113,89,.12)` 的前三个数当成底色、把 alpha 丢了，真实合成底是 12% 强调色压在纸上，第八条的合成口径不会这么算。**悬停因此不是洞**：`a:hover` 确实换到 `--c-accent-hover` 并带着下边框，只是这一态此前从未被看过——量过、判过、记下来不再猜。另外 `ainseInitMermaid(dark)` 收了 `dark` 却从不使用它（图版配色走 `themeVariables` 现读令牌，不靠 mermaid 的具名主题），这个"看起来像 bug"的形状已用三行注释钉死，免得下一个读者（人或模型）再据此立一条假闸。
- **（2026-09-25）本轮变异口径从 P1–P11 涨到 P1–P19，其中三条是补出来的**：P15（环色不跟全站强调色）、P16（焦点环偏移为负）、P17（选择器被注入表赢）、P18/P19（抄件登记两个分支）。P16 的第一版预期是「像素上只数到 N 侧」，实测不成立：`outline-offset:-18px` 时环仍然画得出来，塌掉的是**邻边锚点**（落到控件自己的墨色上，报出 2.92:1 与 1.00:1）——那是判据在替作者解释几何。改法是给"偏移为负"一条具名拒判并 `continue`，让像素读数在这支之后不再被采信；而「像素上只数到」那一支今天在真页面上不犯，**只有合成记录走过**，这件事写在代码里而不是含糊过去。日志：`/tmp/mut_g8_r6.log`（19 条全部命中、rc=0），样本面绿在 `/tmp/pal_focus_r8.log` 与 `/tmp/pal_shot_r1.log`。
- **（2026-09-25）上一轮判过"不是洞"的悬停，这一轮量出了洞：四族导航项在指针下毫无反应**：上方那条「悬停因此不是洞」量的是普通正文链接（`a:hover` 覆盖得到的那一批），它只回答了"这一态有没有变"，没回答"这一态在哪些元素上变了"。这一轮把它做成第八条的第五支判据，口径两条都从现场派生：候选集 = 页面上真在场的 `a[href] / button / summary / [role=button]`；去重键 = 元素自己的静息计算样式签名（字色、合成底、四边按宽门控后的边框、描边、透明度、阴影、字号、字重、下划线、transform）。同签名必被同一批规则命中，所以每种签名只需真点一次鼠标。基线跑下来 54 种签名里有 4 种完全没回应：侧栏书名、顶栏当前项、侧栏当前章节、右侧目录当前项。根因不是缺样式，而是特异度相撞之后**靠书写顺序赢**：`.app-nav a.active` 与 `.app-nav a:hover` 同为 (0,2,1)，写在后面的 `.active` 吃掉悬停；`.sidebar ul li.active > a` 与 `.sidebar ul li a:hover` 同为 (0,2,3)，同理；书名那条则是全站 `a:hover` (0,1,1) 被 `.sidebar .app-name-link` (0,2,0) 压住，从来没有配套的 `:hover`。静态扫选择器发现不了任何一条：四条规则都在场、颜色都是令牌。修法是给"当前项"一条自己的悬停语言——整条往深一档（字色 → `--c-accent-hover`，带立条的连立条一起深），而不是把 `.active` 挪到 `:hover` 前面：调顺序会让当前项在悬停时改穿兄弟项的浅底，读起来像被取消选中。
  量具这一侧吃掉三个假案，全部记进件内以免重来。(1) 指针停靠点原定 (3,892)，那正好压在侧栏最后一条链接上，于是"静息签名"天生带悬停态——同一批元素在别的路由按静息态入键就报「签名漂移」，撤开指针后又必然不相等，两条都是量具自己造的。实测只有负坐标能把 `:hover` 链清成 0，于是"停靠成功"升级成一条自检。(2) 撤开指针 0.12s 就取签名，而 `theme.css` 最长 transition 是 .4s，首页 settle 之后仍有 7 条 `case-card` 过渡在跑——第一版的 83 条"仍停在悬停态"全是这么造出来的；改法等 `getAnimations()` 落定（实测本仓无循环动画，落定即为 0），不猜时长。(3) 描边一度跟着字色一起变：Chrome 在 `outline-style:none` 时仍把 `outline-width` 报成 `medium`，而 `outline-color` 的初值是 `currentColor`，于是"一条根本画不出来的描边换了颜色"被算成反馈。描边的可见性只能按 style 门控（宽度看不出来），边框才按宽度门控（`border-style:none` 时宽度计算值就是 0）。
  档位口径：1680 不是富裕而是必需——`.page-toc` 在 `min-width:1600px` 之前一直 `display:none`，只跑 1280/1440 的口径对整条右侧目录失明，目录当前项那条零反馈只有 1680 量得到；390 触屏档不量悬停（tap 之后粘滞的 `:hover` 不是反馈通道，键盘那一侧归焦点环走查）。两条新变异各打一支：P20 删掉书名那条 `:hover`（针「没有任何反馈」）、P21 把 hover 字色改成合法的 `--c-border`（针「字反而糊了」，实测浅档 1.17:1、深档 1.44:1）。判据的自证是七净七坏，其中两支假阳对照专门防"把看不见的改变当反馈"（宽 0 的边换色、同一条边换写法 `@3` → `@3.0`）。P21 的第一版报红正文里带着「变=字色/描边」，那正是上面第 (3) 条假案的现场证据，门控修好后只剩「变=字色」。日志：基线 `/tmp/hoverbase2.log`（四档 24/24/28/28 种签名、在场 6224/6225/6944/6944 个、判红 18 条），修复后 `/tmp/hoverafter.log`（同一分母、判红 0、换键跳过 0），两条变异 `/tmp/mut2021b.log`；**读数与日志不符时以日志为准**，最终字节的计数只写进提交信息——抄进本行就等于作废本行引用的那一跑。

### 逐章扩写第三轮（2026-09-25）：技术栈纵深、第十一条守卫落地、两处编造被抓

- **规模与口径**：27 个书稿文件、净增 5722 行、新增 138 个小节（两条命令自量：`git diff --numstat` 与 `git diff -U0 | grep -c "^+##"`）。全书技术图 107 → 148 张，每章区间由 2–3 张变成 2–5 张，七章顶到 §9 的上限——复核以 `check_figures.py` 自己打印的那行为准，本行的计数只说明这一轮的形状。
- **新增的技术栈一律带工具卡**：卡上写清"本机实跑（A）／官方文档逐字（B）／本书未跑（C，不出读数）"。本机没有的工具（go、protoc、nginx、redis-cli、psql、duckdb、docker、semgrep、trivy、buf、oasdiff、kubectl、helm、cosign、syft、promtool、graphviz、hypothesis、mutmut、tiktoken、ruff、mypy 等）一律不许出现"跑出来是……"那种句子——只有形状档／规范档，没有数字。
- **（2026-09-25）第十一条守卫 `check_replay.py` 落地：把"贴进来的读数必须是这段代码真打印的"变成机械判据**。做法是把每个 python 块里的 `print` 编译成行模板（字面段保留、插值段变 `.+`；多参 `print` 按默认单空格拼接；带 `sep=`/`end=` 的会改行形状，整块交人工），再要求紧邻 `text` 块里每一行非豁免读数落进任一模板。豁免三条各有名字：命令回显 `$ `、分节标记 `=== 标题`、退出码行。首版按"字面前缀"判，34 处假阳（循环里的 print 以变量开场），换成模板匹配后清零；多参 `print` 从"拒判"改成"按单空格拼"之后，交人工块由 27 降到 17、进入判据的读数行由 66 升到 125。**判据面与盲区面各由脚本自己打印，本行不抄计数**——`--selftest` 八支 fixture（含"整块交人工"那一支）、真产物变异 M1/M2 各自具名报红且还原后 `cmp` 相同，日志 `/tmp/mut_replay.log`。红零不等于全覆盖：那批交人工的块里的读数行至今没人判，这条缺口写在守卫自己的输出里。
- **（2026-09-25）对 A 档卡做五点抽检，抓出两处真编造，两处都是本轮新写的**：① ch26 §21.4d 贴了一行它自己那段脚本根本打印不出的读数——补上 `tail_after()` 重跑之后，页面里的表才是脚本打得出的那张；② ch27 §22.6g 声称"本机实跑"却既没附扫描器、数字也是旧的——重写为随书 `secscan.py` 全量清单加真读数，顺带量出一个真结论：**把这份读数粘进仓库再跑一次，命中数会变**（扫描器把 `docs/` 当语料，正文里粘的样例本身成了被扫对象），所以那一节现在明写"这是第 N 次跑的数，不是常数"。这条抽检不进守卫（"是否编造"判不了），但进了 `STYLE_GUIDE.md` 的"跑不了就不出卡"。
- **（2026-09-25）推送前的自检抓出台账自己造假：三行 B 档被登记成 A 档**。上一段抽检的是卡，这一轮抽检的是**卡的抄件**——`ch09` §4.5c 那张"全书点名的工具逐张卡在哪一章"的台账，把 OpenAPI 生成器、`protoc`、nginx 三行标成"本机实跑"，而这台机器一件都跑不起来：`command -v java`／`protoc`／`nginx`／`go` 全部无输出，`/usr/lib/jvm` 不存在，`~/.npm/_npx/` 只有 chrome-devtools-mcp 与 vite 两个包（实测 `ls` 结果，不是推断）。**发现它靠的不是再读一遍台账，而是把两个数据源摆在一起对**：卡正文写"文档逐字"、台账写"本机实跑"，同一工具的两个声明不可能同时成立。修法分三层：① 两张越权的卡（`ch14` 的 OpenAPI 卡先前写"本卡实测跑在 OpenJDK 17"、protoc 卡先前写"本卡报错全部在它上面跑出"，还贴了一行 `protoc --version → libprotoc 36.2` 的"本机读数"）改成显式 `> **档位声明**： **B 档 · 文档逐字，本书未在本机跑过。**` 并删掉那三句；② 台账整表按卡自述逐行重抄（49 行，档位列把降级理由写进格子而不是留个字母），口径声明新增一条固定点：**档位以卡内声明为唯一权威，台账是抄件，抄件与声明不符以卡为准并当场降档**；③ 在 `ch09` 台账下方把这次降级写成正文的一部分（点名三行与取证命令），而不是悄悄改掉三个字母——读者需要知道这张表曾经错过，否则"重抄过的表"和"改过的表"在版面上无法区分。**登记的缺口**：第十一条守卫量的是"打印 ↔ 围栏读数"逐行相等，它抓不到这一类编造，因为假读数与假代码本来自洽；能抓它的只有"声明 ↔ 抄件"这一对一致性，而这一对目前仍是人工对的，机检候选（从每张卡的 `档位声明` 行派生档位、与台账格子逐一判相等）留待下轮立项。**→ 同日已立为第十二条**，见下方同名条目。
- **（2026-09-25）第十二条守卫 `check_tier_ledger.py` 落地：把「卡内声明 ↔ 台账抄件」这一对一致性变成机检**，收掉上一条目末尾登记的那个缺口。判据四支：① 台账每一行的键，在落点那一章的文件里**有且只有一条**具名 `> **档位声明（工具名）**` 行（键按整串相等判，特意做成能抓前缀：台账写成「Kafka 分区」不许命中「Kafka 分区键与顺序语义」那条声明，这一支有 fixture）；② 声明所在的小节要与落点节号／卡名相符——节号要在某级标题开头，所以 `## 21.4` 之下 `### 工具落地卡：…真系统` 里的声明算落在 21.4·真系统；标题栈与声明都跳过围栏（示例块里的 `# 注释` 不是小节，块内照抄的声明不算本书的声明）；③ 档位标签**集合**相等（A／B／C／规范／形状五种，一张卡两半证据不同就两边都列，台账格子必须同样列全）；④ 反向闸：卡上声明了而台账没有的键报红——台账漏登记等于抄件比实物少。**立 producers 而不是只立判据**：`--print` 整列由卡派生（49 行全部可派生，派生不出来的行计数并拒绝出字母），所以"以卡为准"这条口径有个会拒绝执行的读者，而不只是红字里的一句话。**极性对照打在真产物上**：把 ch27 的 Semgrep 卡改成 A 档、把台账的 Trivy 格子改成 A 档（两个方向各一次），两次都 rc=1 且各自具名报红，还原后 `cmp` 字节相同；`--selftest` 十支 fixture（含正对照与假阳对照）全绿。落地时台账与卡**互有对方错的地方**，各按权威方向改：三格是台账抄漏另一半（JSON Schema、p50/p99、required checks 的卡都写明两半档位不同，格子只列了一档）→ 补格子；两行是卡用词漂移（10.5c/10.5d 的卡自称 B 档，而它们的小节标题与台账都写规范档）→ 改卡。**顺带把第九条打红一次**：上一轮写的 pytest 卡里那句报错原文用了转义反引号套反引号，Docsify 不吃，两处泄漏——这就是第九条存在的意义，改法是换成一条不带内嵌反引号 code span 的写法。
- **（2026-09-25）第九条的自检在矩阵里报了一次不该那样报的红，改的是它的自证而不是判据**：文本矩阵第十二轮 `09b_leaks_st rc=1`，报的是 R3／R4 两条**反例没报**——照字面读是"判据松了"。同一支脚本单独跑三次全 rc=0，且那一次它自己打印的节点数是 107（正常 321），说明实际发生的是**九条对照所在的采样页没加载完就被取样**：旧自证只有一条 `walked == 0` 的地板，节点少到 1／3 仍然继续判九条。两个缺陷同因——缺一份"我覆盖到了什么"的读数，于是环境问题穿着判据的衣服报红。修法两条，都不放宽判据：① 采样前先等九条对照的 `h3` 标题全部进 DOM（等的是**自己的 fixture**，不是 `sleep`）；② 新增具名读数「覆盖缺失：哪些标签没进采样面／走过多少节点」，归因不到标签同样算缺失，缺就单独报红并不再判那九条。这一支的极性用变异打过：把 `TAG_RE` 换成永不命中的正则，九条标签全部列进「覆盖缺失」且 rc=1（日志 `/tmp/g9_*.txt`、变异输出见本轮记录）。**教训写在这里而不是只写在脚本里**：报红要有正确的归因，一条只会说"没报"的自检等于把自己的环境问题转嫁成一次假的判据失效——下一轮就有人去"修松掉的判据"，把真判据改松。
- **（2026-09-25）第八条在终字节矩阵上红过一次，红的是取数时机而不是色板**：`08_palette rc=1`，三格 `div.search input` 的计算值各差 1（`--c-bg` #211e1a 读成 rgb(34, 31, 27)、`--c-text` #ece6da 读成 rgb(235, 229, 217)、`--c-border-strong` #59513f 的蓝通道 63→64），同一支闸单独重跑 rc=0。四条观测指向同一处：① 生效对账的参照物是**自定义属性**（换档那一帧即终值），被比对的是**真实属性**（theme.css 里 `transition: all .2s` 会让颜色跑一段过渡）；② 三格偏差的方向恰好是浅档→深档的插值途中（底色偏亮、字色偏暗）；③ 只有首页那一格红，而它是全站唯一要先真点揭幕、再等正文渲染的一页，取样落在动画帧上的窗口最长；④ 原始那一帧没能按需复现——这条如实记着：修的是窗口，不是复现。**改法不放宽判据**：取计算值之前等页面自己停（复用悬停那条等 `document.getAnimations()` 归零的等待，改名成共用的 `quiesce()`，原有三处调用点含义不变，这里接上第四处），停不下来就具名作废本页读数——「生效对账前 6s 内页面仍停在过渡/动画里（N 条在跑）——getComputedStyle 读到的不是终值，本页读数作废」。等之前先睡 0.12s：过渡要等下一次样式重算才登记进 `getAnimations()`，不等会把"还没开始"读成"已经停"。作废走 `continue`，页次与生效对账行数同进同退，覆盖集自证不会把这次作废算成"账本没翻开"。
- **（2026-09-25）给"取数时机"配一条会红的变异，顺手又抓出文档抄走的条数**：P23 在被读的那个元素上挂一条无限动画——色板、令牌、声明一个字没改，坏的只有时机。before/after 两跑走同一台变异机器：把那一步静默短路成恒真（等价于修之前的代码）时，坏样本 12 个页次的生效对账 0 红，这条形状对判据完全隐形；接上之后 12 页各自报出上面那句具名作废，而总红数反而从 29 降到 20——悬停走查不再拿插值误判成"回不去的悬停态"，那 29 条里本就有 9 条是量具自己造出来的假案（日志 `/tmp/p23_probe.log`）。**同一轮写文档时撞出第二件事**：两处命令行说明把第八条的变异条数抄成了一个区间，而清单在 P22 落地那天起就比它多一条——条数只住在 `MUTATIONS` 与 `--mutate` 的第一行，抄进文档等于再造一个会腐烂的事实源。新增一支判据：对象只取"活指令"（含 `check_palette.py --mutate` 的那一行），带日期的事故记录里那些区间是当天为真的快照、不在对象范围内（真树 9 处一律不红，写进 fixture 当假阳对照）；P24 打它自己那支，静态段 0.2s 报 1 条并打印"扫过 67 个 md"，极性另有对 HEAD 那棵树的复算背书（改书之前当场报 2 处，点名的正是那两格旧区间）。**写这一段本身又吃掉了一次相邻条目的加粗标题**（同类第五回），这回是改完自己顺着接缝看出来的：第三条守卫的"零丢小节"判据跑在修补之后，它没有机会报红，所以本行不能记成"靠闸抓到"——第五次里前四次靠人、只有一次靠闸，别把账记反。**这一支不覆盖什么**：静默只保证读到终值，不保证终值等于作者意图——后者归 P17 那一支（声明被运行时注入的第二宿主赢）判。
- **（2026-09-25）第二条守卫报的两条"死链"是闸错了，不是书错了**：那两处引用活在前例代码块里，`check_links.py` 把围栏内容当正文扫。改闸不改书：新增可见正文提取（围栏与行内码整段剔掉）、围栏未闭合就拒绝读数，另加 `--selftest` 五支 fixture（含四反引号嵌套那一支）＋端到端变异——正文坏链必红（`MUT_PROSE_RC=1`）、围栏内坏链必不红（`MUT_FENCE_RC=0`）。
- **（2026-09-25）本轮三处"源码看着合法、页面留一串字面星号"，全栽在同一个形状**：`**` 紧跟反引号开场（`**` 后面第一个字符是反引号）。第九条守卫抓到三处，改法一律把加粗挪到以汉字收尾的短语上；这一形连同两个已登记的兄弟形写进了 `STYLE_GUIDE.md`「加粗与引号」。
- **（2026-09-25）"图真画出来了"这句话重新量过**：第七条守卫本来就在每页验「md 围栏数 = 已渲染并量到字号的图数」，那一次量到的等式两侧都是 148（当日读数，现值以守卫打印的那一行为准）；另用一支独立探针把三口径并排对了一次（md 围栏 / DOM `.mermaid-block` 容器 / 其中有可用 viewBox 的 svg），并按路由收 `console.error` 与未捕获异常，日志 `/tmp/mm_render.log`。FIGURE_LIST 旧行那句"107/107 通过浏览器端 `mermaid.parse` 全量校验"是扩写前的一次性读数，新增的图它没量过，已按现口径改写——而且那句当时也没有对应守卫，是一句话挂在那儿没人执行。
- **（2026-09-25）内容纵深第二波：三处最该深却最浅的章补到同深，顺手量出"第一人称"这一轴画不出界**：并行三张工单各自只许改一章，收回来 3 文件 +658/−4、新增 31 个小节标题（其中一支是 33.6→33.7 的改号），全书技术图由 148 涨到 153（两条读数都按 `git diff --numstat`／`git diff -U0 | grep -c "^+##"`／`check_figures.py --print` 现量，不做事后追述）。三章的靶子不是字数：路线图章的周表只有"动作／产出／验收"，没有一条可 grep 的退出判据；工程栈章讲原则不讲栈自己的机械结构；收束章只做归纳不给对照。**交叉验证按证据纪律走**：新写的每条"某卡是 A 档／某判据行本机打印过"我都回到源头逐条核（21.4d 的 A 档自述、21.5b 的实跑、28.5b 的形状档、9.5h 的 C 档、`'USD' does not match '^CNY$'` 确在 9.5f 的读数块里），并把它引用的那条报红行原样复现了一遍——造一个撞号的临时件跑出 `图 R-1 撞号 2 处（…）→ ch02…、ch99-临时撞号试验…` 与 `EXIT=1`，与正文抄的那两行逐字相同，临时件随即删除（`ls docs/manuscript | grep -c ch99` → 0）。
- **（2026-09-25）子代理交稿带回来一处第一人称越界，这一轴没有闸，也不可能靠文本立闸**：新写的两段收束叙述用了「我」，而 `STYLE_GUIDE.md`「第一人称」只放行第八部分四案例章——第 33 章在第九部分，HEAD 版那一章的「我」计数是 0。改法是把那两句改成无主语句。**为什么不顺手补一支守卫**：先量了再判——非四案例章含「我」的行实测 132 处，其中绝大多数是合法宿主（角色卡的立场句、清单里的"我承诺"填空、Mermaid 标签上的对话、他人引语），要把"作者声"与"引语里的我"分开，界得靠语义；硬按行首或引号画界，第一版就会吃掉那 33 行"疑似其它"里的一半合法件。**判据先量过，量不出界就不立闸**——与"四案例数字禁止互相引用"那一轴同一处置：写进 STYLE_GUIDE、靠人核，本轮靠的正是人读。三案各自的数字归属逐格另核过一遍（矩阵每格只回指自己案例的 ch05 分节，四格的钱不相减不比大小）。
- **（2026-09-25）上一轮刚立的"计数只有一个宿主"，当天就在自己家抓到四处腐烂**：正文一涨，`FIGURE_LIST.md` 与 `DIAGNOSIS.md` 里那些"全书 148 张／全站 148 = 148／七章顶到上限／148 张 Mermaid"的活抄件当天全部失真（现值 153，顶到 §9 上限的章从七章变八章）。改法不是把数改对——那样下一轮又坏——是按同一口径改成指向打印它的命令：图数指向 `check_figures.py --print`（那三行读数已框进 `FIGURE_LIST.md`，注明是同一次运行的打印件），等式两侧的数指向 `check_legibility.py` 逐页自己打印的行；带日期的事故记录（"那一次量到两侧都是 148"）作为当天的快照保留。**这条判据自己的边界要写明白**：上一轮的静态判据对象是"含 `check_palette.py --mutate` 的那一行"，管不到图的总数、也管不到角色数——它只对第八条的 P 区间有效。剩下的计数抄件仍靠人核，且已列入文末"下一轮"。

### 全量校验命令（可复核）

```bash
python3 scripts/check_figures.py          # 第一条：图数 / 图注 / 图号 / 节点上限 / 悬空引用 / 无锚图
python3 scripts/check_figures.py --selftest  # 第一条的口径自检：五条已知答案 fixture + 覆盖集自证
python3 scripts/check_figures.py --print  # 输出 FIGURE_LIST 用的逐文件表
python3 scripts/check_links.py            # 第二条：本地引用死链扫描（**要先起服务**：python3 -m http.server 8080 --directory docs）
python3 scripts/check_markdown.py         # 第三条：围栏成对 + 正文裸围栏 + HTML 块吞语法 + 根/docs 副本一致 + 相对 HEAD 零丢小节 + 小节子树非空
python3 scripts/check_markdown.py --selftest  # 第三条的两道子闸自检：标题闸 6 条（删除必报 / 改名与重编号必不报 / 围栏里的 `#` 不算小节，且同段围栏外真小节被吃必报）+ 空节闸 4 条
python3 scripts/check_overlays.py         # 第四条：整屏遮罩的层叠语义闸（静态）
python3 scripts/check_interactions.py     # 第五条：命中测试 + 真实点击（62 路由 × 1280/390）
python3 scripts/check_interactions.py --mutate   # 第五条的变异自检：必须按机制报红、退出码 1
python3 scripts/check_mobile.py           # 第六条：390 窄屏几何闸（顶栏折行 / 图缩糊 / 居中溢出 / 横滚）
python3 scripts/check_mobile.py --mutate  # 第六条的变异自检 A–F（F＝封面入口改名，首页正文无从揭幕）
python3 scripts/check_mobile.py --report  # 逐图输出 viewBox / 渲染宽 / 缩放
python3 scripts/check_legibility.py       # 第七条：有效字号地板 + 滚动暗示覆盖率（1280/390 × 浅/深）；红绿都打印 [覆盖率读数] 一行；首页 `#/` 先真点「全书架构」揭幕再量正文
python3 scripts/check_legibility.py --mutate              # 第七条的变异自检 A–G（F＝渲染器不再给表格套 .table-scroll；G＝封面入口改名）
python3 scripts/check_legibility.py --report              # 逐图输出 自然字号 / 有效字号 / 缩放
python3 scripts/check_legibility.py --viewports 1280,1440,390 --themes light,dark --screenshot DIR
                                                          # 全量三档复核并逐图出截图（终验用这条）
python3 scripts/check_palette.py              # 第八条：token 纪律 + 逐层 alpha 合成后的对比度 + 分色族可辨性 + 焦点环 + 悬停可辨性（真起 Chrome、真点 #btn-theme、真点鼠标）
python3 scripts/check_palette.py --mutate     # 第八条的变异自检：每条各自要能被按机制打红。条数与针数由这条命令自己打印在第一行——上一版在此抄过一次条数，P22 落地当天它就少报一条，抄计数等于再造一个会腐烂的事实源；第八条从此对"md 里抄 P 区间"这个形状本身报红（判据见下方 2026-09-25 取数时机那一条）。举其要：P8＝往 @media print 塞字面量；P11＝位图锚点回执停在图版另一色；P15/P16＝焦点环的环色与偏移；P17＝声明的选择器被运行时注入的第二宿主赢；P18/P19＝index.html 的色抄件停在上版令牌 / 表外没人认领；P20/P21＝书名的 hover 被删 / hover 字色改成合法的边框灰；P22＝追加一条与现网同值的重复块（针「永不生效」）；P23＝在被读的那个元素上挂无限动画（针「读到的不是终值」）
# 第八条的口径自检没有独立开关：焦点环坏记录、抄件登记坏件、悬停判据的净与坏、P 区间的极性加两支假阳，都在 check_palette.py 那条的静态阶段常驻跑完并打印自证行；各支的支数由脚本自己数，本行不抄
python3 scripts/check_palette.py --report     # 逐页输出对比度最低的前 25 对 + 悬停台账整张（按在场元素数排序）
python3 scripts/recolor_plate_art.py --check  # 位图版画只量不改：冷调占比 / 暖调占比 / 墨线亮度（改前改后要对账，别在已改色的文件上重建基线）
python3 scripts/recolor_plate_art.py          # 把 docs/assets 的位图重着色到当前 --c-plate* 三锚，零不达标才落锚点回执（改配色必跑，否则第八条 P11 报红）
python3 scripts/check_render_leaks.py         # 第九条：渲染泄漏闸——源码写了 markdown 语法，页面必须真的变成样式（62 路由逐页走可见文本节点）
python3 scripts/check_render_leaks.py --selftest  # 第九条的对照自检：R1–R4 四种坏形状各一条反例 + 五条修好的正例，同页互证
python3 scripts/check_render_leaks.py --report    # 逐页输出走了多少节点、渲染出多少 <strong>/<em>/<code>
python3 scripts/check_incidents.py            # 第十条：事实一致性闸——编号↔日期↔时间线↔登记（纯静态，读清单与时间线）
python3 scripts/check_incidents.py --selftest # 第十条的对照自检：控制跑必 0 + 变异 M1–M7 各按机制报红 + 2 条正例必不报
python3 scripts/check_incidents.py --report   # 逐编号输出清单日期与正文点名次数，逐案输出阶段窗口
python3 scripts/check_replay.py               # 第十一条：读数可重放闸——书稿 python 块的 print 模板必须盖住紧邻 text 块的每一行
python3 scripts/check_replay.py --selftest    # 第十一条的桩件自检：八支已知答案 fixture（含"整块交人工"那一支）
python3 scripts/check_replay.py --verbose     # 逐对输出配了哪些模板、哪些行进豁免、哪些块整块交人工
python3 scripts/check_tier_ledger.py          # 第十二条：档位对账闸——§4.5c 台账每一行必须等于对应卡上那行具名档位声明
python3 scripts/check_tier_ledger.py --selftest   # 第十二条的桩件自检：十支 fixture（正对照＋六类坏件＋两支派生支＋一支围栏归属）
python3 scripts/check_tier_ledger.py --print      # 整列档位由卡派生（改档位的动作从这里开始，不许手填字母）
python3 scripts/accept_view.py                # 验收：真起 Chrome，代表页 × 1280/1440/390 × 浅/深逐格截图 + 现场读数（本机无 X 显示，只能 headless 驱动）
python3 scripts/plate_engine.py --check       # 题图引擎自检：板外色 / XML 解析失败 / 含 <text> 节点
```

### 仍待作者/第二轮

- 真实素材（PR / 事故 / 账单 / Beta 反馈）
- ~~文件名统一 `chNN-`~~（2026-09-24 已完成：42 文件 git mv + 全量引用重写，映射表见下）
- 出版级 PDF/EPUB（深色主题已以切换形式完成，不再待办）
- ~~宽幅流程图的观感打磨：个别横向流程图节点偏多，在正文列里等比缩小、字号偏小~~（2026-09-24 已收口为**硬地板**：不再靠"下一轮重排节点"，而是按每张图自己的 `viewBox` 与自然字号解出所需宽度，任意视口下有效字号 ≥11px，超出正文栏交给横滚；由第七条守卫 `check_legibility.py` 逐图把住，变异 A 证明这条地板会报红。节点偏多的图现在读得清了，但**重排/改向仍值得做**——那是版面审美，不再是可读性缺陷。）
- ~~窄屏滚动缺"可滚"的暗示~~（2026-09-24 已落地：`.scroll-frame` + 左右 `.scroll-veil` 渐隐与方向箭头，随 `scrollLeft` 实时更新，`pointer-events:none` 且被第五/七条守卫逐点命中测试与真点复覆盖；覆盖率从实测 0 到 352 处全挂号。因为改的是 `theme.css` 与 `index.html`，第五/六/七条守卫与截图复核已按承诺全部重跑，退出码见上。）
- ~~390 档浮动控件在滚动途中压住正文~~（2026-09-24 已收口：顶栏改两行网格、控件并入第一行，正文区命中测试 0 点落控件；改法与实测见上一节。**代价也记下**：窄屏顶栏从 60px 抬到 84px，390×844 的可视正文高度少掉 24px。）
- ~~跨文件叙事一致性仍无守卫~~（2026-09-24 已立第十条守卫 `check_incidents.py`——**它只收口了能量的那半轴**：编号↔日期自洽、时间线 90 行日期不越阶段窗、17 个阶段窗口递增不重叠、12 件事故日期落在本案时间线内、正文点名处逐条判登记（命中数由闸自己打印，不抄进本行）、每件事故正文至少点名一次；清单里那两句「先回写本清单，再进正文」与「与时间线逐一对应」从此有执行者。N7 那条判据的适用面也在这轮收窄成 `manuscript/` 下的书稿正文，理由见上方立闸条目）。**仍然没有守卫的是「四案例数字禁止交叉引用」这一轴**：正文确有合法跨案件（ch35:315 的公开信、ch38 整章交叉启示），机械禁令会误伤，量出后果后按现网读数否决；这一轴继续靠人，别声称有闸。另两条被否决的候选判据（同行钱数、事件名配日期）连命中面都是零，理由与读数记在本节上方的立闸条目里，不在这里重复。
- **还没出卡的工具**：Mythril / Slither（本机未装、无可核对判据行）与 oasdiff / buf——出卡前提是先拿到一手判据输出，拿不到就留在台账 C 档。
- ~~**浏览器类第五、六、八条仍然跳过首页正文**~~（2026-09-25 已补齐：揭幕链只有一份（`check_legibility.dismiss_cover`），第七、九、五、六、八条五条闸共用，"全站"两个字从此是读数不是推断。改法、四条新变异（第五 M6 / 第六 F / 第七 G / 第八 P10）与那条被真点抓出来的 Docsify 抛错，记在上方「把揭幕链接到第五、六、八条」一节；`FIGURE_LIST.md` 的"口径边界"注释同步改成逐条首页口径。）
- **回归矩阵的覆盖自证改为按行名集合，并发防护改为锁文件**（2026-09-25 已落在两支脚本上，这一条留作口径说明）：`/tmp/bat13_browser/` 那一份被一个 TaskStop 之后仍在跑的子孙进程追加过行，"行数 =6" 的自证先恰好放过、后又抓到 7 行——改成断言**排序后的行名集合**相等才算覆盖。第一版并发防护用 `pgrep -f "^bash /tmp/bat1…"` 并排掉自己的 PID，结果**第一跑就被自己拦死**：脚本在本会话里被 harness 包了一层同命令行的祖先进程，"排掉 $$" 排不掉那一层，抓到的 pid 事后 `ps` 已经没有了。两支矩阵（`/tmp/bat15.sh` 文本九行、`/tmp/bat14_browser.sh` 浏览器六行）现在都用 `flock -n` 拿锁文件。**顺带抓到一件与矩阵无关但同源的事**：8080 端口上挂着一个 44 分钟前被停掉的矩阵留下的 `http.server`（父进程已是 `systemd --user`），而第二条守卫的脚本自己会再 `&` 起一个同名服务——端口被占时那个后起的静默失败，检查却继续对着**残留的那个**服务跑。同一份文档目录所以读数没被污染，但"我起的服务没起来"这件事当时没有任何读数。已清掉残留并按 flock 重跑。

- **第十一条守卫有登记在案的盲区**：带 `sep=`/`end=` 的 print、循环里拼接出来的输出、f-string 以变量开场的块，整块交人工——这些块里的读数行进不了判据。缺口由脚本自己打印（配对数／进判据的行数／交人工的块数与行数三条一起出），别把"红零"读成"全部核过"。要把这块收进来，得先决定是给这些形状做行模板，还是逐块改写成可判的形状。
- ~~**本轮扩写的工具卡还没进全书台账**~~（2026-09-25 已补齐：`ch09` §4.5c 整表按**卡上自己声明的档位**逐行重抄，本轮新增的机制族（编译期路由、契约 diff、Burn-rate、静态扫描证据链等）全部登记，档位列把理由写进格子。**但补台账的过程本身抓出三行错账**（B 档被登成 A 档），取证、修法与新增的固定点"档位以卡内声明为唯一权威、台账是抄件"记在上方同名条目里。）~~**「卡内声明 ↔ 台账抄件」这一对没有守卫**~~（2026-09-25 已立第十二条 `check_tier_ledger.py`：四支判据＋`--print` 派生整列作为写侧生产者，正反面各在真产物上变异过一次。**它管不到的那一面仍然没有守卫，而且大概率永远判不了**：卡里那句「本机实跑」是否为真——第十一条量"打印↔围栏"、第十二条量"声明↔抄件"，两条都是自洽性检查，一段编造的读数只要代码与输出一起编，两道闸都会绿。所以"下一轮先确认它有没有第二个读者"这句话的答案是：有，`--print` 就是那个读者；而**没有读者的那一面是卡内证据的真实性**，它只能靠抽检，别声称有闸。）
- **案例四章与附录四章还没做第二轮纵深**：第三轮只动了理论章，第四轮（同日）补的是三处最该深却最浅的章——`ch02` 路线图、`ch09` 工程栈、`ch38` 收束章（206 行，按行数全书最薄的实质章）。**剩下的薄处按同一量法排队**：附录 J／K（277／245 行）至今没有一个技术栈归属，案例章要维持第一人称且不引入任何新数字。
- **`secscan.py` 这类"把仓库当语料"的工具读数不可粘第二次**：命中数会随正文里粘贴的样例本身增长（本轮实测一次），所以引用它的地方必须写明"第几次跑的数"。这不是守卫能判的，是写法纪律。
- **正文里还活着的计数抄件，这一轮只扫掉了图数那一族**：`FIGURE_LIST.md`／`DIAGNOSIS.md` 里"全书 148 张／全站 148 = 148／七章顶到上限"已在当天改成指向 `check_figures.py --print` 与 `check_legibility.py` 逐页打印的等式。**没扫掉的至少还有两类**：正文里"全书点名的 49 件工具""42 个 `chNN-` 文件"这类句子（宿主是 `check_tier_ledger.py --print` 与图清单自己打印的行），以及"第 N 条守卫"这种编号（没有打印宿主）。扩判据之前先给每一类指定宿主——**没有宿主的计数，删数不删句**，别改对了这一次等它下一次烂。
- **并行子代理扩写的收口顺序要写进流程**：本轮三张工单各自只许改一章，跨文件件（`FIGURE_LIST.md`、`STYLE_GUIDE.md`、`DIAGNOSIS.md`、根/docs 副本同步、两份矩阵、提交与推送）一律由主 agent 集中做。这个边界挡住了互相打红（第十二条逐行对账台账，多处并发改它必撞），但挡不住**交稿物里的口径越界**——那一处「我」是人读出来的（见上一条同日子条目），下一轮要在工单里把"该章是否允许第一人称"写成显式字段，并在合并后单独跑一遍第一人称与数字归属的人工抽读。

## 分章诊断表

| 章节 | 核心论点 | 证据强度 | 真实性风险 | 重复/矛盾 | 缺失素材 | 配图机会 | 建议动作 | 优先级 |
|---|---|---|---|---|---|---|---|---|
| 00 时间线 | 四案例 18 月并行时间线 | 中 | 低（已声明虚构） | 与案例章日期需锁死 | 可视化甘特 | **Mermaid gantt** | 配图 + 交叉校验日期 | P0 |
| 01 角色卡 | 32+ 角色立场与 KPI | 中 | 低 | 与正文口头禅偶有出入 | 关系图 | **mindmap/flowchart** | 配角色关系图 | P1 |
| 02 数字清单 | 唯一数字源 | 强 | 低 | 正文偶有近似表述 | 无 | 对照表即可 | 标注「正文引用规则」 | P1 |
| 第1章 | AI 原生≠AI 写代码；三层能力 | 强 | 低 | 与第4章工程栈有轻微重叠 | — | 三层能力对比图 | 补 Mermaid；删一句式重复 | P0 |
| 第2章 | 六失灵 + 介入点矩阵 | 强 | 低 | 与第24章幻觉章呼应（好） | — | **失灵模式图 + 介入流** | 双图；保持立场锚点 | P0 |
| 第3章 | AI 做可能/人做选择/人做负责 | 中 | 低 | 口号与第1/2章重复 | 责任边界图 | **sequenceDiagram** | 收紧重复，补时序图 | P0 |
| 第4章 | 四支柱工程栈 | 中 | 中（像清单） | 与治理章部分同构 | 架构全景 | **架构 flowchart** | 补全书总架构图 | P0 |
| 第5章 | 大规模考古画图 | 中 | 低 | 弱 | 图「本该有却无」 | 依赖盘点四图 | **必须配图** | P0 |
| 第6章 | 跨团队依赖 | 中 | 低 | 与第5章衔接 | 循环依赖示意 | **flowchart 循环** | 补依赖图 | P1 |
| 第7章 | AI 读懂祖传代码 | 强 | 低 | 幻觉点与第24章重叠 | 考古三法对比 | 对比图 | 表+1图 | P1 |
| 第8章 | 目标架构三层 | 中 | 中偏空 | 与第4章需分工 | 边界测出来 | 三层边界图 | 与第4章互链 | P1 |
| 第9章 | 契约先行 | 强 | 低 | 全书支柱 | 契约流水线 | **契约生成→审核→门禁** | 补流水线图 | P0 |
| 第10章 | 边界变测试 | 强 | 低 | 与第9章衔接好 | import-linter | 测试门禁图 | 补图 | P1 |
| 第11章 | 留缝 | 强 | 低 | 命题锚点 | 留缝五步 | **五步流程图** | 补图 | P0 |
| 第12章 | 数据日志分家 | 中 | 低 | 与第13章同属重构 | 三分家 | 数据流图 | 补图 | P1 |
| 第13章 | 单文件→router | 中 | 低 | 实践细 | 拆分路径 | 拆分前后对比 | 补图 | P2 |
| 第14章 | Python 立包 | 中 | 低 | 实践细 | 双跑迁移 | 迁移状态图 | 1图 | P2 |
| 第15章 | 前端设计令牌 | 中 | 低 | 与契约章呼应 | 令牌即契约 | 多端一致性图 | 1图 | P1 |
| 第16章 | 聊天独立成域 | 中 | 低 | 与风控钩子 | 域边界 | 域拆分图 | 1图 | P2 |
| 第17章 | 多端 BFF | 中 | 低 | 与第15章 | 契约共享 | BFF 架构图 | 1图 | P1 |
| 第18章 | 亿级流量 | 中 | 中（数字表演化） | 与电商案例峰值对齐 | 削峰 | 流量架构图 | 对齐数字清单 | P1 |
| 第19章 | 五层门禁 | 强 | 低 | 支柱 | — | **五层门禁图** | 必配 | P0 |
| 第20章 | 风控不可绕过 | 强 | 低 | 与第19章 | 三道防线 | 防线时序图 | 补图 | P0 |
| 第21章 | 对账灰度回滚 | 强 | 低 | 支柱 | 对账三层 | 对账闭环图 | 补图 | P0 |
| 第22章 | 安全合规公开仓库 | 中 | 中 | 稍工具化 | 四红线 | 红线矩阵 | 收紧 | P1 |
| 第23章 | 提示词工程归档 | 强 | 低 | 与提示词腐化 | 归档流水 | **prompt 生命周期** | 补图 | P0 |
| 第24章 | AI 评审与幻觉 | 强 | 低 | 回答第2/11/27章（好） | 幻觉三类型 | 幻觉分类图 | 补图 | P0 |
| 第25章 | ADR/RFC/令牌 | 中 | 中偏文档 | 与第15/23章 | 文档治理环 | 文档流图 | 补图+互链 | P1 |
| 第26章 | 组织治理 | 中 | 中偏空 | 与第2章矩阵 | 委员会运作 | 治理结构图 | 补图、加冲突场景 | P0 |
| 第27章 | 契约治理 | 强 | 低 | 与第9章 | 变更分级 | **变更分级状态图** | 补图 | P0 |
| 第28章 | 灰度发布 | 强 | 低 | 与第21章 | 回滚矩阵 | 灰度分层图 | 补图 | P0 |
| 案例一 | 300人电商契约先行 | 强 | 低 | 数字与清单一致 | — | **事故时间线** | 头图+时间线 | P0 |
| 案例二 | 交易所精度铁律 | 强 | 低 | — | — | 精度门禁图 | 头图+1图 | P0 |
| 案例三 | 量化四道闸门 | 强 | 低 | — | — | 闸门流程图 | 头图+1图 | P0 |
| 案例四 | 智能体权限矩阵 | 强 | 低 | — | — | 权限矩阵图 | 头图+1图 | P0 |
| 第33章 | 四案例交叉启示 | 强 | 低 | 收束正确 | 对照总图 | **四案例对照图** | 补总图 | P0 |
| 附录A | 50 条 prompt | 强 | 低 | 体量大 | 分类索引 | 目录图 | 加索引导航 | P2 |
| 附录I | 契约治理模板 | 强 | 低 | 可 fork | — | 模板结构图 | 1图 | P2 |

## 优先修复清单（按编辑部动作）

### P0 — 本阶段必须（已全部完成）

1. **全站接入 Mermaid**（已自托管 `vendor/mermaid.min.js`）。✔
2. **为全部理论/支柱/案例章补足 2–3 张 Mermaid**（实测 92 块，见 `FIGURE_LIST.md`）。✔
3. **重做封面与首页**：抽象头图 + 九部分卷首 + 阅读路径。✔
4. **案例章头图**（AI 抽象图，无文字）。✔
5. **写入 BOOK_SPEC / GLOSSARY / STYLE_GUIDE**（已完成）。✔
6. **导航与编号**：侧边栏分组清晰；文件名后续可重排（先保证链接不炸）。✔

### P1 — 第二轮

1. 理论章「空泛正确」段落收紧（最薄 13 章已扩写，第 4/8/26 章此前已补操作层）。✔
2. 外部引用核查表（DORA 等），标 `[待核实]`。✔（2026-09-24：17 条去重外链逐条实测，16 条直连 200、1 条经二次通道确认在线、0 死链；台账见 `public-evidence.md`「外链可达性台账」；oasdiff 已更正为规范组织路径）
3. 第 5 章「画图」章补齐本该存在的考古图（实测 3 张）。✔

### P2 — 打磨

1. 附录导航与搜索权重。
2. 章末遗留债务可视化。
3. ~~深色主题~~（已实现为深浅双主题切换，默认浅色不回退）。✔

## 结构问题（非内容）

| 问题 | 影响 | 动作 |
|---|---|---|
| ~~文件名序号与章号错位~~ | — | 已解决（2026-09-24）：42 文件按侧边栏阅读顺序统一为 `chNN-` 前缀，`git mv` 保留历史，全库引用重写后死链扫描 =0；映射表见下 |
| ~~零图片~~ | — | 已解决：封面 + 4 案例头图 + 9 部分卷首 + 全书 Mermaid 技术图（张数以 `check_figures.py` 打印的行为准，2026-09-25 起不再手抄） |
| ~~封面依赖 shields.io~~ | — | 已解决：全站零外链（实测无任何 cdn/unpkg/jsdelivr/shields 引用） |
| 根 `_navbar.md` 可能编码异常 | 导航乱码 | 重写 UTF-8（已随导航重做解决） |
| 无 GLOSSARY 引用 | 术语漂移 | 侧边栏与指南挂接 |


### 重命名映射存档（2026-09-24，旧名 → 新名）

| 旧文件名 | 新文件名 |
|---|---|
| `03-这本书是什么.md` | `ch01-这本书是什么.md` |
| `04-90天总路线图.md` | `ch02-90天总路线图.md` |
| `00-案例时间线.md` | `ch03-案例时间线.md` |
| `01-角色设定卡.md` | `ch04-角色设定卡.md` |
| `02-数字清单.md` | `ch05-数字清单.md` |
| `16-第1章-AI原生不是让AI写代码.md` | `ch06-第1章-AI原生不是让AI写代码.md` |
| `11-第2章-人在回路.md` | `ch07-第2章-人在回路.md` |
| `17-第3章-人机分工.md` | `ch08-第3章-人机分工.md` |
| `18-第4章-AI原生工程栈.md` | `ch09-第4章-AI原生工程栈.md` |
| `22-第5章-大规模考古画图.md` | `ch10-第5章-大规模考古画图.md` |
| `23-第6章-跨团队依赖分析.md` | `ch11-第6章-跨团队依赖分析.md` |
| `24-第7章-AI读懂祖传代码.md` | `ch12-第7章-AI读懂祖传代码.md` |
| `20-第8章-目标架构.md` | `ch13-第8章-目标架构.md` |
| `19-第9章-契约先行.md` | `ch14-第9章-契约先行.md` |
| `21-第10章-边界变测试.md` | `ch15-第10章-边界变测试.md` |
| `12-第11章-留缝.md` | `ch16-第11章-留缝.md` |
| `25-第12章-数据与日志分家.md` | `ch17-第12章-数据与日志分家.md` |
| `26-第13章-单文件API到router+nginx.md` | `ch18-第13章-单文件API到router+nginx.md` |
| `27-第14章-Python立包与迁移.md` | `ch19-第14章-Python立包与迁移.md` |
| `28-第15章-前端解耦多端设计系统.md` | `ch20-第15章-前端解耦多端设计系统.md` |
| `29-第16章-聊天功能.md` | `ch21-第16章-聊天功能.md` |
| `30-第17章-多端BFF.md` | `ch22-第17章-多端BFF.md` |
| `31-第18章-亿级流量.md` | `ch23-第18章-亿级流量.md` |
| `32-第19章-五层门禁.md` | `ch24-第19章-五层门禁.md` |
| `33-第20章-风控不可绕过.md` | `ch25-第20章-风控不可绕过.md` |
| `34-第21章-对账灰度回滚监控.md` | `ch26-第21章-对账灰度回滚监控.md` |
| `35-第22章-安全合规公开仓库.md` | `ch27-第22章-安全合规公开仓库.md` |
| `37-第23章-提示词工程与归档.md` | `ch28-第23章-提示词工程与归档.md` |
| `36-第24章-AI评审与幻觉处理.md` | `ch29-第24章-AI评审与幻觉处理.md` |
| `38-第25章-文档ADR-RFC-设计令牌治理.md` | `ch30-第25章-文档ADR-RFC-设计令牌治理.md` |
| `13-第26章-组织治理.md` | `ch31-第26章-组织治理.md` |
| `14-第27章-契约治理.md` | `ch32-第27章-契约治理.md` |
| `15-第28章-灰度发布.md` | `ch33-第28章-灰度发布.md` |
| `44-案例一-300人电商平台.md` | `ch34-案例一-300人电商平台.md` |
| `45-案例二-海星交易所.md` | `ch35-案例二-海星交易所.md` |
| `46-案例三-暗流资本.md` | `ch36-案例三-暗流资本.md` |
| `47-案例四-守夜人科技.md` | `ch37-案例四-守夜人科技.md` |
| `48-第33章-四案例交叉启示.md` | `ch38-第33章-四案例交叉启示.md` |
| `39-附录A-提示词库骨架.md` | `ch39-附录A-提示词库骨架.md` |
| `40-附录I-契约治理规范模板.md` | `ch40-附录I-契约治理规范模板.md` |
| `41-附录J-AI系统工程.md` | `ch41-附录J-AI系统工程.md` |
| `42-附录K-一线最小实践包.md` | `ch42-附录K-一线最小实践包.md` |

## 作者仍需补的真实性（AI 不能编）

- 任一真实项目的 commit / PR / 事故复盘片段（可脱敏）。
- 工具账单或效能前后对比（内部数据）。
- Beta 读者「哪里不真实」反馈。
- 公开引用的可点击来源（DORA 报告页、厂商 postmortem）。

在这些素材到位前，保持「自洽虚构脱敏示范」声明，不把案例写成纪实。
