# 第 28 章 大规模灰度与回滚：百万用户级发布

> **本章定位**：发布治理锚点——灰度分层、自动刹车、回滚决策矩阵、多端同步。
> **核心命题**：在 AI 让"上线"变得几近免费之后，"能撤回"成了发布真正的成本中心。

<figure class="chapter-plate">
  <img src="../assets/plates/ch33-spillway.svg" alt="第题图：溢洪道"/>
  <figcaption>题图 · 溢洪道：水位到了堰顶自己泄，不需要谁下令</figcaption>
</figure>

---

## 28.1 问题：回滚用了数小时，用户已经大量受损

在某电商平台的支付对账事故中（案例一），告警响起后，SRE 发现回滚脚本不存在——**AI 只生成了"前进"路径（迁移脚本），没有生成"后退"路径（回滚脚本）。** 从告警到完全回滚、差额停止增长，耗时数小时。在这段时间里，对账差额持续累计，影响了大量用户。

事后复盘时，一位 SRE 说了一句值得反复引用的话："**治理团队天天讲'人在回路'，回滚的时候，AI 能帮我回滚吗？它不能。它连一个回滚脚本都没给我。**"

这句话指向一个组织级的盲点：组织花了大量精力治理"生成"、治理"留缝"、治理"契约"——**唯独没有好好治理"出事之后怎么办"。** 而这次事故恰恰证明：在一个大规模平台上，**回滚的速度比生成的速度重要一万倍。** 生成慢一天，损失是延期；回滚慢一小时，损失是用户的钱。

这个教训在其他案例中以不同代价重演：交易所案例中，24/7 不可停机的约束让回滚变成了"降级到上一稳定版"而非传统意义上的停服回滚；量化基金案例中，链上合约部署不可逆，"回滚"意味着紧急撤出资金而非代码回退；安全初创案例中，智能体规则的"回滚"是恢复检测基准，而非代码级别的版本控制。

---

## 28.2 根因：组织治理了"怎么上线"，没治理"怎么撤回"

灰度/回滚失败有三条根因：

**① AI 只生成"前进路径"，不生成"后退路径"。** 它的目标函数是"让需求跑起来"，不是"让需求能撤掉"。于是 AI 生成的迁移、配置、灰度方案，默认都没有回滚位。**我们默认"上线"是终点，其实"能撤回"才是终点。**

**② 灰度没有"自动刹车"。** 灰度通常是"放出去一个比例，等人看"。看的人是谁？通常没人专门看。告警响了很久才被人注意到——因为灰度期间没有一个指标能"自动触发回滚"。**人盯监控是不可靠的，尤其在大促半夜。**

**③ 回滚决策权不清楚。** 出事的那一刻，谁能拍板回滚？SRE 能回滚但怕误回滚被追责；业务方能拍板但半夜找不到人；风控能叫停高风险链路但不熟悉发布链路。**于是最关键的最初几十分钟，常常耗在"谁有权回滚"上。**

三条归结成一句：**组织把发布当成了单向门（只能进），没当成双向门（能进能退）。** 而在 AI 介入后，"前进"变得太容易，"后退"的欠账就暴露得越狠。

---

## 28.3 AI 用法：AI 做配置，人定刹车，回滚脚本必须和迁移一起生成

```mermaid
flowchart LR
  MIG[迁移 / 变更包] --> ROLL[回滚脚本<br/>必须成对生成]
  MIG --> CAN1[灰度 1%]
  CAN1 --> METRIC{刹车指标}
  METRIC -->|健康| CAN2[灰度 10% / 50%]
  METRIC -->|越阈| STOP[自动刹车 + 回滚]
  CAN2 --> FULL[全量]
  STOP --> ROLL
  style STOP fill:#ffe3df,stroke:#a03b31,color:#1e1c19
  style ROLL fill:#f1ebde,stroke:#2f6154,color:#1e1c19
```

**图 28-1｜灰度与自动刹车** — 没有后退路径的生成物不让上线；人盯监控不可靠，刹车要自动。

图 28-1 里"回滚脚本"这张绿卡收着两支入向箭头：一支从迁移卡直连，是"成对生成"的前进义务；一支从红色刹车卡折回，是"越阈"分支的落点。把绿卡从图上抹掉，越阈分支就悬在半空——"没有后退路径的生成物不让上线"说的就是这一格不能缺。

灰度/回滚里，AI 的位置：

- **AI 可以生成灰度配置。** 比例、分桶、白名单，AI 能快速产出初版。
- **AI 不可以定"刹车阈值"。** 什么指标到什么值就自动回滚，这是业务+风控的判断，不是 AI 的。
- **AI 生成的任何迁移/变更，必须同时生成回滚脚本。** 只有前进路径、没有后退路径的生成物，不让上线。

最终机制叫"**前进必带后退**"：

```
任何变更 = 前进脚本 + 回滚脚本 + 刹车指标 + 回滚演练记录
缺任何一项 → 灰度审批不通过
```

**治理的意义，有时候就是把某个人的通宵，变成一条不让别人再通宵的规则。**

---

## 28.4 组织冲突模式：回滚权归 SRE 还是归业务

这是全书里和"谁签字"并列的最难的权力问题。

冲突的核心：**回滚快的收益（少损失）和回滚错的成本（误伤正常流量），该由谁权衡、由谁承担？**

SRE 要求"事故时可一键回滚，不需要业务批准"，业务方反对"你们误回滚，损失算谁的"。

分场景处理是最有效的妥协：

| 场景 | 决策权 | 理由 |
|------|--------|------|
| 资金/对账类异常 | SRE 可一键回滚，事后报备 | 损失随时间扩大，必须快 |
| 资金/对账类正常流量波动 | 业务拍板 | 误回滚成本高 |
| 端上白屏/崩溃 | 多端负责人可一键回滚 | 离端最近的人判断最快 |
| 非资金功能异常 | 业务 + SRE 联合，限时决策 | 平衡速度和误伤 |

**关键认知：回滚权不能笼统地说"归 SRE"或"归业务"，必须按"损失是否随时间扩大"来切。** 随时间扩大的（资金对账），权力给快的人；不随时间扩大的（功能小问题），权力给判断准的人。

---

## 四案例映射

| 案例 | 回滚/降级的典型困境 | "前进必带后退"的具体形态 | 回滚权分配的核心原则 |
|------|---------------------|------------------------|---------------------|
| 案例一·电商平台 | 对账事故无回滚脚本，耗时数小时 | 迁移脚本+回滚脚本+刹车指标+演练 | 按损失是否随时间扩大分配 |
| 案例二·加密交易所 | 24/7不可停机，无维护窗口 | 降级到上一稳定版+冻结提币 | 风控有一键降级权（资金存亡级） |
| 案例三·Web3 量化 | 合约部署不可逆，无法代码回退 | Kill switch紧急平仓+仓位冻结 | 首席风控手动kill，时间优先 |
| 案例四·安全初创 | 智能体规则回滚≠代码回滚 | 规则版本快照+一键恢复基准 | CTO手动kill+规则回滚 |

---

## 28.5 产出物：分层灰度方案 + 自动刹车 + 回滚决策矩阵 + 多端同步发布

可落地的四件：

### 1. 分层灰度方案

```
内部 → 1% → 5% → 20% → 50% → 100%
每档观察窗口：高风险链路 30 分钟，一般链路 10 分钟
每档刹车指标：错误率、对账差额、端崩溃率、核心转化
```

这张档位表本质上是一台状态机——每一档都挂着同样的出口规则：

```mermaid
stateDiagram-v2
  [*] --> 待发布: 前进 + 回滚脚本齐备
  待发布 --> 档位观察中: 进入当前档
  档位观察中 --> 档位观察中: 指标健康 · 推进下一档
  档位观察中 --> 暂停待决: 次要指标异常 · 软刹车
  暂停待决 --> 档位观察中: 人决策继续推进
  暂停待决 --> 已回滚: 人决策放弃
  档位观察中 --> 已回滚: 硬指标越阈 · 自动刹车
  档位观察中 --> 全量完成: 走完 100% 档
  已回滚 --> [*]: 限时复盘 · 修订后重走
  全量完成 --> [*]
```

**图 28-2｜灰度批次推进状态机** — 灰度不是一把推到底的滑块，是一台状态机：每档过观察窗口才推进，软刹车停在当前档等人决策，硬刹车直接回滚并强制限时复盘。

图 28-2 的要紧处在"暂停待决"与"档位观察中"的分工：软刹车只把变更停进"暂停待决"，它的两条出边都写着人的决定——推进或放弃，权还给人；硬刹车那条"硬指标越阈"的转移则从观察状态直达"已回滚"，全图没有给它留任何等一等出口。"停一下等人"与"不等人"的差别，就是这两条边的差别。

### 2. 自动刹车

- **硬刹车（自动回滚）**：对账差额 > 阈值、端崩溃率 > 0.5%、核心接口错误率 > 1%。触发即回滚，不等人。
- **软刹车（告警 + 暂停灰度）**：次要指标异常，暂停在当前比例，等人决策。

硬刹车从触发到复盘，是一条不依赖任何人守夜的时序链：

```mermaid
sequenceDiagram
  participant C as 灰度批次
  participant M as 刹车指标监控
  participant S as SRE
  participant T as 复盘会
  C->>M: 每档上报错误率 · 对账差额 · 端崩溃率
  M->>C: 硬指标越阈 · 自动刹车触发
  C->>C: 执行回滚脚本 · 冻结在上一稳定档
  C-->>S: 回滚完成通知
  S->>T: 事后报备 · 限时复盘
  T->>T: 定位根因 · 评估其他端是否同步回滚
  T-->>C: 修订通过后重新灰度
```

**图 28-3｜自动刹车→回滚→复盘时序** — 硬指标越阈即自动回滚、不等人；回滚完成后人只做两件事：限时复盘、评估其他端是否同步回滚。

图 28-3 前三步没有 SRE 的位置：上报、越阈触发、执行回滚并冻结在上一稳定档，全部发生在灰度批次与监控两条泳道之间；SRE 露第一面已是那条"回滚完成通知"的虚线——人出场的时间点被画在刹车生效之后，这件事本身就是这张图的结论。

### 3. 回滚决策矩阵

28.4 给的是原则（按"损失是否随时间扩大"切权），这一张把它落成能贴进 runbook 的表：每一行自带触发、动作、拍板人、时限与多端联动，值班的人半夜不必重新推理。附录 A 的 `P-28-4-01` 提示词消费的就是这张表——没有它，那条提示词让 AI"基于回滚决策矩阵给建议"时指向一个空标题。

| 触发信号 | 立即动作 | 拍板人 | 决策时限 | 其他端是否联动 | 事后动作 |
|---|---|---|---|---|---|
| 硬刹车：对账差额越阈（资金/对账链路） | 执行回滚脚本，冻结在上一稳定档 | 值班 SRE 单人，事后报备 | 0，不等人 | 联动：契约未变则各端同批回滚 | 差额清零确认 + 限时复盘 |
| 硬刹车：端崩溃率 > 0.5% | 仅该端回滚上一版本，其余端保持 | 该端负责人（离端最近的人） | 0，不等人 | 不联动：端侧问题不外溢 | 崩溃栈归因 |
| 硬刹车：核心接口错误率 > 1% | 回滚该发布批次 | 值班 SRE，无需业务批准 | 0，不等人 | 联动：调用该接口的端一起评估 | 根因 + 门禁补一条 |
| 软刹车：核心转化等次要指标异常 | 暂停在当前档，先不回滚 | 业务方拍板 | 一个观察窗口（高风险 30 分钟 / 一般 10 分钟），到点无人决策即按回滚处理 | 评估：同契约的端一起停在当前档 | 记入复盘，无论最终走哪条 |
| 资金/对账链路的正常流量波动 | 不回滚，先查证 | 业务方 | 一个观察窗口 | 不联动 | 误报也要留痕 |
| 不可停机形态（案例二：24/7 交易） | 降级到上一稳定版 + 冻结提币 | 风控一键降级（资金存亡级） | 0，不等人 | 联动：交易主链路一起降 | 资金存亡级复审 |
| 不可逆形态（案例三：链上合约） | Kill switch 平仓 + 仓位冻结 | 首席风控手动 | 0，不等人 | 不联动：链上没有"端" | 撤资路径审计 |
| 规则型发布（案例四：智能体规则） | 恢复规则版本快照到基准 | CTO 手动 kill + 规则回滚 | 0，不等人 | 不联动：单端产品 | 规则基准修订 |

三处需要按你自己的链路改：软刹车那行的两个观察窗口是本书的默认值（沿用本节第 1 件那张档位表的窗口，不是通用常数），硬刹车的三个阈值取自上面「自动刹车」的口径，"到点无人决策即按回滚处理"是一条例外授权——它把"没人拍板"从默认不动作改成默认回滚，只在损失随时间扩大的链路上成立。最后一列不许省：**事后动作落不到 ADR 或门禁，同一场事故会在下一个灰度周期重演**，这正是 28.2 根因③ 的正面写法。

### 4. 多端同步发布

- **契约一致的变更**：多端同批灰度。
- **端差异变更**：按端排期，但必须同步更新契约。
- **回滚同步**：任何一个端回滚，触发评估"其他端是否需要同步回滚"。

> **代价声明**：这套方案上线后，灰度平均时长会显著增加（因为每档观察窗口变长、刹车指标变多）。业务方会抱怨“上线变慢了”——但用“上线变慢”换“回滚变快、事故变小”，在大规模平台上这笔账没人能质疑。

---

## 28.5b 渐进交付实现件：Argo Rollouts 的 steps 与 analysis 判据（形状档，本书未跑）

> **档位声明（Argo Rollouts 的 steps 与 analysis）**：**形状档 · 未运行，本节开头已写明本机既没有 kubectl 也没有 Argo Rollouts（两者 `command -v` 均无输出）——`Rollout` 与 `AnalysisTemplate` 两块 YAML 按公开 CRD 形状书写，全节一个字节的运行输出都不贴。**

28.5 第 1 件那张档位表和“自动刹车”四个字，写在 runbook 里就是半夜没人执行的散文。渐进交付工具做的机器化翻译，是把档位变成声明式工件、把刹车变成表达式。**本机没有 kubectl 也没有 Argo Rollouts，本节只给 YAML 形状、一个字节的运行输出都不贴（R2 证据纪律）**，字段按公开 CRD 形状书写：

```yaml
# 形状档（未跑）：档位=steps 的机器形态；刹车=analysis 的 failureCondition
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: checkout                 # 占位名
spec:
  strategy:
    canary:
      trafficRouting:
        istio:
          virtualService: { name: checkout-vs, routes: [primary] }
      steps:
        - setWeight: 5           # 第 1 档：5% 权重（注意：是权重不是流量，见 28.5c）
        - pause: { duration: 30m }   # 高风险链路观察窗口，取自 28.5 第 1 件默认值
        - analysis:
            templates:
              - templateName: checkout-brakes
        - setWeight: 20
        - pause: { duration: 10m }
---
apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: checkout-brakes
spec:
  metrics:
    - name: canary-error-rate
      interval: 5m
      failureCondition: result > 0.01     # 阈值人定（28.3：AI 不定刹车）
      failureLimit: 2                     # 容忍越阈次数——两窗语义的落点，见 28.5f
      provider:
        prometheus:
          address: http://prometheus.example.internal   # 占位符
          query: |
            sum(rate(http_requests_total{status=~"5..", version="canary"}[2m]))
            /
            sum(rate(http_requests_total{version="canary"}[2m]))
```

三个静默失效面：其一，观察窗口短于指标滞后——指标还没爬上来，analysis 判的是“健康”的假象；其二，`failureCondition` 用绝对计数而不是比率——晚高峰流量自然涨起，刹车自己被误踩；其三，`trafficRouting` 没真正挂上——steps 全走完，用户流量从没进过 canary。还有一个组织面的失效：**工具不会改变 28.4 的权力问题。自动刹车执行的是你预先授予的回滚权——授权是谁签的、误刹车谁担，仍然归那张决策矩阵管，不归 YAML 管。**

---

## 28.5c 权重不等于流量：连接粘滞的三处来源

`setWeight: 5` 声明的是**新请求的路由决策**，不是“线上 5% 的流量此刻正在新版本上”。三处把它拉偏的来源：

- **长连接**：WebSocket、gRPC streaming 这类连接建立那一刻就定终身，之后不再重新分流（聊天域的连接语义见[第 16 章](./ch21-第16章-聊天功能.md)）；灰度开始后老实例上的旧连接会一直旧下去。
- **多路复用与连接池**：HTTP/2 一条连接跑很多请求，Envoy 按新连接决策的次数远少于请求数；上游池子保持住了，旧后端就还吃着真实流量。
- **客户端缓存**：端上的 DNS 缓存与 keepalive，让“已经回滚”在服务端生效、在客户端还要再活一会儿——这正是 28.5 第 4 件“回滚同步评估”要评估端的原因。

三条纪律：推进档位的依据是 canary 实例**实际收到的请求占比**（PromQL 形状见 28.5f，`version="canary"` 那一路的分母读数），永远不是配置里的权重数字；连接型服务给出连接最长寿命上限、到期强制重平衡（量级取“你的最长会话加观察窗口”，示意是小时级，不是通用常数）；低峰时段 canary 的样本本来就薄，**薄样本上的“健康”读数没有推进权**——这一格和第 24 章评测集最小样本量是同一个统计常识的两次出场。

---

## 28.5d Feature flag targeting 的决策链（图 28-4）

flag 是灰度之外的第二道独立闸：代码已经 100% 部署，flag 决定谁真的用得上。它和 28.5 第 1 件的版本灰度互不取消——版本管“哪台机器跑新代码”，flag 管“哪个人看到新行为”。决策链必须短、全、且每一步可审计，先给链再给图：

```python
def serve_flag(user, env, flag):
    if env.kill_switch.on(flag): return False        # ① 全局关：一票否决位
    if user.id in flag.blocklist: return False       # ② 黑名单先于白名单：拒绝权优先于豁免权
    if user.id in flag.allowlist: return True        # ③ 内部吃狗粮名册
    if not user.matches(flag.segment): return False  # ④ 定向段：地区 / 套餐 / 风控分层
    # ⑤ 按百分比推进的放量子集：盐按 flag 隔离，桶按用户稳定
    return bucket(user.id, salt=flag.salt) < flag.rollout_percent
```

两个细节各挡一类事故：**桶必须是账号 id 的稳定哈希**（随机数会让同一用户两次刷新劈里啪啦换体验，客服与复现全失效），**salt 必须一 flag 一换**（全平台共用一个盐，所有 flag 命中同一批用户——多个灰度绑成同一个风险源，一炸全炸）。先给链再给图，因为图上的落点顺序就是代码的 return 顺序：

```mermaid
flowchart LR
  Q[一次flag判定请求] --> K{全局kill switch}
  K -->|开| OFF[返回旧行为]
  K -->|关| BL{命中黑名单}
  BL -->|是| OFF
  BL -->|否| AL{白名单内部名册}
  AL -->|是| ON[返回新行为]
  AL -->|否| SEG{定向段规则}
  SEG -->|不符| OFF
  SEG -->|符| PCT{稳定哈希放量}
  PCT -->|桶号在比例内| ON
  PCT -->|不在| OFF
  style OFF fill:#f1ebde,stroke:#2f6154,color:#1e1c19
  style ON fill:#e2f3df,stroke:#3e7247,color:#1e1c19
```

**图 28-4｜flag targeting 决策链** — kill switch 与黑名单排在豁免名单之前：flag 系统里拒绝权必须永远比放行权多一个前置位；放量站最后，因为只有前面四关都过了，比例才有意义。

图 28-4 与 28.5b 的关系要说破：Rollout 的 steps 换的是**实例**，flag 换的是**人群**，刹车指标要能分辨这两层（按 version 切还是按 flag 曝光切），否则“canary 健康”与“flag 人群健康”会互相冒名。

---

## 28.5e 影子流量与“影子写”的危险边界（图 28-5）

影子流量是灰度家族里最激进的一种：生产请求复制一份打给新版本，**响应用户的一直是老版本**，新版本只参与对比。读路径上这是赚的——查询无副作用，复制品跑出的差异就是免费的正确性证据。写路径上它多半是债：影子请求触达的是有外部后果的函数——支付真的扣款、库存真的扣减、消息真的发出去，这些副作用**不跟着响应一起被丢弃**。新版本正常路径写一遍、影子路径又写一遍，用户看到的就是双份现实。先给边界图：

```mermaid
flowchart LR
  REQ[生产请求镜像] --> KIND{读还是写}
  KIND -->|读| NEW[新版本执行]
  NEW --> CMP[响应只进对比器]
  CMP --> USE[差异报告喂灰度决策]
  KIND -->|写| SD{三问 外部副作用 幂等可逆 下游知情}
  SD -->|有一问答不上| DROP[禁止影子写 只采差分日志]
  SD -->|全答得上| MARK[影子标记贯穿全链路]
  MARK --> STO[落影子表与影子主题]
  style DROP fill:#ffe3df,stroke:#a03b31,color:#1e1c19
  style MARK fill:#fcefd3,stroke:#9d6127,color:#1e1c19
  style USE fill:#e2f3df,stroke:#3e7247,color:#1e1c19
```

**图 28-5｜影子读放行、影子写三问** — 读可以影子是因为它没有外部后果；写的边界由三个问题把守：有没有外部副作用、副作用幂等可逆吗、下游知道自己参演彩排吗。

三问里任何一问答不上或答“不知道”，就进图 28-5 的红色出口。真要让写路径也参与彩排，只剩一条诚实的路：所有下游实现真正的 `dry_run` 语义，且影子标记一路贯穿——从入口到最外层的第三方调用都认识这个标记。**这是工程项目，不是配置开关**：标记漏传一环，彩排就有一环来真的。这也是 28.2 根因① 的镜像面：AI 生成“影子对比”这种读代码很快，但“标记全链路贯穿”里每一个下游系统的所有者点头，机器给不了。

---

## 28.5f 自动刹车的可抄表达式：从 PromQL 到两窗判定器

刹车指标的查询形状（B 档，PromQL 函数与匹配符语义按规范书写；标签名以你的埋点为准，本书未跑任何 Prometheus 实例）：

```promql
# 比率型刹车：分子分母都限定在 canary 那一路
sum(rate(http_requests_total{job="checkout", status=~"5..", version="canary"}[2m]))
/
sum(rate(http_requests_total{job="checkout", version="canary"}[2m]))

# 分布型刹车：延迟分位，窗口至少覆盖两个采集步长
histogram_quantile(0.99, sum by (le) (rate(http_request_duration_seconds_bucket{version="canary"}[5m])))
```

两条口径各自挡一类假刹车：分母也必须限 `version="canary"`，否则稳定版自己的错误会喂进 canary 的刹车读数；`rate` 的窗口短于采集加评估滞后时，读数永远比事故慢半拍——这就是 28.5b 静默失效面第一格的数学形态。

然后是判定本身。“越阈即回滚”其实是个坏规则：单窗尖刺太容易被噪声喂出来，误刹车多了，人会去拔刹车的线。28.5b 模板里那个 `failureLimit: 2` 背后是“连续两窗超阈”语义。这段判定器拿真数据形状跑给你看——**本机实跑，纯标准库；输入序列是示意数据，统计对象是判定逻辑本身，不是任何线上指标**：

```python
THRESHOLD = 0.01                                   # 示意阈值：对应 28.5 硬刹车口径
WINDOWS = [0.004, 0.016, 0.008, 0.012, 0.018, 0.006, 0.002]   # 示意输入：逐窗错误率
streak = 0
for i, v in enumerate(WINDOWS, 1):
    streak = streak + 1 if v > THRESHOLD else 0
    print(f"W{i} err={v:.3%} streak={streak} -> "
          + ("ROLLBACK" if streak >= 2 else "OBSERVE"))
```

```text
W1 err=0.400% streak=0 -> OBSERVE
W2 err=1.600% streak=1 -> OBSERVE
W3 err=0.800% streak=0 -> OBSERVE
W4 err=1.200% streak=1 -> OBSERVE
W5 err=1.800% streak=2 -> ROLLBACK
W6 err=0.600% streak=0 -> OBSERVE
W7 err=0.200% streak=0 -> OBSERVE
```

这段真实轨迹里有两个读数值得停一下：W2 与 W4 都是单窗越阈，判定器不动作——**尖刺与趋势的差别，就是让一窗与杀两窗的差别**；W5 触发 ROLLBACK 后 W6 归零，因为回滚已经执行，这是轨迹的结尾不是判定的仁慈。代价也要摊开说：两窗语义把最坏暴露时间放大成两个观察窗口，所以窗口长度与阈值按链路风险等级反着定——资金链路宁可窗短、两窗，一般功能宁可窗长、一窗即杀也可以。**判定状态必须按发布批次清零**：把上一批的 `streak` 带进新一批，等于允许上一批的越阈触发这一批的回滚——这是这段十行代码唯一真正的坑。

---

## 28.5g 灰度机制的生效证据清点：每套闸各自挂着哪份产物

28.3 那条「前进必带后退」的公式列了四件，28.5b 到 28.5f 给其中几件造出了实现件。这一节把它们逐条翻成证据面：每条机制在生产上该留下什么可翻的东西、这条证据怎么读、没有产物时它退回哪种原形。表内读数全部来自本章前文，不新增任何读数；三态（有产物／产物可造假／无产物）的判据取自 28.3 那句「缺任何一项 → 灰度审批不通过」——四件里今天能被机检回答的只有成对性那一半，剩下的面全靠这张清点兜着。

| 机制（出处） | 生效产物 | 三态 | 这条证据怎么读 | 没有产物时退回什么样子 |
|---|---|---|---|---|
| 放量档位（28.5 第 1 件「内部 → 1% → 5% → 20% → 50% → 100%」） | 档位与观察窗口落进 steps 并进仓库走评审（28.5b 那段 YAML：`setWeight: 5`、`pause: { duration: 30m }`） | 有产物 | 比时间戳的先后：改档位的变更必须早于第一次真实推进；档位表与 steps 序列两处对不上，即有一处是散文 | 退回 28.5b 开头点破的那句原判——写在 runbook 里「半夜没人执行的散文」 |
| 权重与真实流量（28.5c：`setWeight` 声明的是新请求的路由决策，不是此刻的流量占比） | canary 那一路实际收到的请求占比（28.5f 比率型查询里 `version="canary"` 那个分母） | 产物可造假 | 逐档把「声明权重」与「实际占比」两列并排对差；只交出一列的报表就是自证 | 「5% 权重」冒充「5% 流量」，长连接、连接池、客户端缓存（28.5c 的三处来源）把这格差值一路带进回滚之后 |
| 硬刹车（28.5 第 2 件三条口径：对账差额、端崩溃率 > 0.5%、核心接口错误率 > 1%） | `failureCondition` 与 `failureLimit` 两个字段进了 YAML（28.5b），外加一条真走过 ROLLBACK 的批次轨迹 | 产物可造假——全表最险的一格 | 每一次改 `failureLimit` 或阈值都要对回 28.5 第 3 件决策矩阵的某一行；对不上行的改动就是 28.5f 说的那根「被人拔掉的刹车的线」。28.5f 那段轨迹是示意数据，不是任何线上系统的产物，不能当这一格的证据 | 退回 28.2 根因②：告警响了很久才被人注意到 |
| 软刹车（28.5 第 2 件＋图 28-2 的「暂停待决」状态） | 「停在当前档多久、到点谁拍的板」的计时记录；时限锚现成（28.5 第 3 件：一个观察窗口，高风险 30 分钟／一般 10 分钟） | 无产物 | 问一句：上一批软刹车停了几个窗口、有没有一条到点执行「即按回滚处理」的记录 | 「暂停」退化成「遗忘」：状态机那条回「档位观察中」的边没有时限锚，等于允许无限停在灰度里 |
| 回滚脚本成对性（28.3 四件套第二件） | 第 21 章 21.5b 那条 required check 的绿灯（按命名约定双向查孤儿） | 有产物，但只答「有没有」 | 绿灯的口径写在 21.5b 自己身上：「成对不等于能跑」——把绿读成「跑得通」是越权读数 | 退回 28.1 那一刻：脚本不存在，从告警到差额停止耗时数小时 |
| 回滚演练记录（28.3 四件套第四件＋28.6 最后一条勾选项） | 无产物——全章没有一格 YAML 或表列承载它；第 21 章 21.6 清单里那行「回滚脚本存在且演练过」靠人打勾 | 无产物 | 一条合格的记录要同时给出批次号、执行时间、以及 28.5 第 3 件首行那句「差额清零确认」——三者缺一即只是勾选，不是产物 | 反模式第 7 条：真出事时才发现回滚脚本跑不通。灰度特有的暗面正落在这格——**一份从没跑过的演练记录比没有记录更挡审批**，它让四件套看起来齐了 |
| feature flag targeting（28.5d 那段判定链） | flag 配置里的 kill switch 位、黑白名单、以及一 flag 一换的 salt | 产物可造假 | 每条 flag 单问一句「生命周期里有没有一条被关掉的记录」；salt 是否逐 flag 隔离要能从配置核出，看命名不算 | **一个从没被真的关掉过的 flag 与一个不存在的 flag 等效**：28.5d 第①关那个 return 没被走过，就没有任何产物替它作证 |
| 影子写三问（28.5e：外部副作用、幂等可逆、下游知情） | 三问的书面答案＋下游认得影子标记的名册（`dry_run` 语义逐环落地） | 无产物（本章给了三问，没给答案的容器） | 名册要能列出哪一环的下游签收过这个标记；答案写在会议纪要里而落不进名册，等于没有答案 | 图 28-5 的红色出口被绕过：新版本正常写一遍、影子再写一遍，用户看到双份现实 |

三态各点名一次之后要说破：最该盯的不是「无产物」那三行，是「产物可造假」那三行。无产物会当场挡住审批，造假产物会当场通过审批。硬刹车那行的造假最贵，因为它伪造的不是安全而是速度——`failureLimit` 被人一路往小里改，每一次都只动一个整数、每一次都能自圆其说，而图上那条「硬指标越阈直达已回滚」的边（图 28-2）已经悄悄换了执行者。这五列咬合的地方在于：缺「三态」这一列，清点就退化成一堆绿灯的清单，而 28.6 检查清单那七条勾选项正是全章最容易产出假绿的地方；缺「怎么读」这一列，21.5b 的成对绿灯会被拿去替「演练过」作证，四件套里两件并成一件；缺「没有产物时怎样」这一列，本节就是在建议读者删掉那些看不见的机制——而 28.2 三条根因里最少被提起的那条（灰度没有自动刹车），恰恰是以「配置里看起来有」的形态活着的。附录 A 的 `P-28-4-01` 消费 28.5 第 3 件那张表，本节这张表消费的是它的证据面：提示词让 AI 基于矩阵给建议之前，得先知道矩阵每一行的产物态。

把这八行摊回 28.3 那条公式，四件套只有一半能自证：前进脚本与回滚脚本这一半有机检（表里第 5 行），刹车指标这一半有产物但产物可造假（第 3 行），回滚演练记录这一半干脆没有容器（第 6 行）。所以「前进必带后退」这八个字今天的真身价是：**四件里两件能当场验、一件要人读、一件全靠自觉**。清点要跑起来也只有三步，而且都不花钱：每次灰度评审逐行点一次名；点不出产物的行，要么补产物，要么把这条机制降级成明示的假设——降级必须写在评审记录里，因为不写，下一场评审它仍会以「已有」的姿态出现（第 4 行那个没有计时记录的软刹车正是这样活到今天）；降级之后如果这条机制还是从没被触发过，就按后面 28.5i 的腐烂信号来读它，而不是当作健康。

---

## 28.5h 灰度决策点表：哪条读数越线触发哪个动作、谁拍、多久复核

图 28-2 管档位之间自动怎么走，图 28-3 管硬刹车触发之后的时序，这一节管自动动作之上必须有人拍板的那半。读数全部现成：28.5 第 1 件的档位与窗口、第 2 件的三条硬口径、第 3 件的时限锚、28.5b 的两个字段、28.5c 的占比判据、28.5f 的两窗语义。签署方全部沿用本章已经出场的人，不新增人物，不新增读数：值班 SRE（第 3 件里那行「单人，事后报备」）、业务方（28.4 表里拍板正常波动的一方）、该端负责人与多端负责人（离端最近的人）、风控与首席风控（案例二、案例三那两行）、CTO（案例四那行）、复盘会（图 28-3 最后那条泳道）、治理团队（28.1 那位说出「回滚的时候 AI 能帮我回滚吗」的人所在的桌子）。

| 读数（出处） | 上会判据（什么形状算越线） | 触发后的动作 | 签署方 | 时限锚 |
|---|---|---|---|---|
| 端崩溃率（28.5 第 2 件口径 0.5%） | 单条硬指标越过该口径，且按 28.5f 连续两窗成立；单窗尖刺不上会——W2 与 W4 那种形状判定器不动作 | 仅该端回滚上一版本，其余端保持（28.5 第 3 件第 2 行的「不联动：端侧问题不外溢」） | 该端负责人 | 0，不等人（第 3 件同一行） |
| 核心接口错误率（28.5 第 2 件口径 1%／28.5b 与 28.5f 的 `result > 0.01`） | 比率型读数分母限 `version="canary"`（28.5f 第一条口径），连续两窗越阈即形状成立 | 回滚该发布批次；调用该接口的端一起评估（第 3 件第 3 行的联动语义） | 值班 SRE，无需业务批准 | 0，不等人 |
| canary 实际占比与 `setWeight` 声明的差（28.5c 三条纪律之首） | 两条读数在同一档持续两窗对不上——形状是「声明未动、占比不动」，不是占比越某个数 | 冻结当前档：先修分流与连接寿命，再谈推进；这一格永不作为「可以推进」的依据上会 | 值班 SRE 提请，复盘会核（图 28-3 的「评估其他端是否同步回滚」是同一张桌子） | 一个观察窗口（高风险 30 分钟／一般 10 分钟） |
| 低峰段 canary 的分母厚度（28.5c：薄样本上的「健康」读数没有推进权） | 分母薄而判定为绿，两件事同现即命中——判据形状是「小分母配绿灯」；本节不敢给它一个数字阈值，这份欠账与 28.8 第一条那句「没有理论依据，是经验值」是同一种 | 停在当前档；要不要为该档加时由业务方定 | 业务方 | 一个观察窗口 |
| `failureCondition` 或 `failureLimit` 被改动（28.5b 两个字段） | 变更评审的 diff 里出现这两个字段之一即上会，不看改动方向、不看来由 | 阈值维持人定（28.3：AI 不可以定刹车阈值）；改后仍要能对回决策矩阵的一行，否则按 28.5g 的「拔线」形状处理 | 风控一票（28.6 第 3 条：高风险相关的，风控有没有一票） | 生效之前——事后报备在这一行不成立 |
| 误报留痕的走向（28.5 第 3 件「误报也要留痕」那一行＋28.5f「误刹车多了，人会去拔刹车的线」） | 留痕条目在涨而硬刹车零触发——形状是「只有软刹车在响」，说明阈值钉错了方向而非运气好 | 阈值回到 28.8 那个经验口径重钉（历史 P99 错误率的若干倍），重钉后逐行回填第 3 件 | 业务方与值班 SRE 联合（28.4「非资金功能异常」那一行的切权形状） | 下一次复盘会（第 3 件事后动作的「限时复盘」） |
| 演练记录与审批四件套的差额（28.3 公式＋28.5g 的无产物三行） | 四件里任何一件在评审时点不出产物——形状是「勾打了但翻不到记录」，不是「记录不好看」 | 灰度审批不通过（28.3 原话）；补齐演练那件才放行 | 治理团队 | 大促前（28.6 最后一条勾选项） |
| 某一个端已经回滚（28.5 第 4 件「任何一个端回滚」那条） | 出现第 3 件里任一「不联动」行而契约未变——形状是「端间版本代差加同一份契约」，两者同现即上会 | 触发其他端的同步回滚评估（第 3 件「联动」列的既有语义，本表不新增判据） | 多端负责人 | 一个观察窗口（与第 3 件软刹车那行同一时限锚；那条「到点无人决策即按回滚处理」的例外授权在这一行不自动适用——它只在损失随时间扩大的链路上成立，28.4 的切权原则在这里要再判一次） |

本表有意不收案例二、三、四那三行：风控一键降级、首席风控手动 kill、CTO 手动 kill 加规则回滚，在第 3 件矩阵里已经把时限钉成「0，不等人」，动作本身就是回滚——它们没有「上会」这一层，把它们抄进这张表会把「谁来拍」重新写成讨论题，正是 28.4 那个权力问题最忌讳的写法。

### 这条决策链的形状（逐岔口，不放图）

本章配图额度已满（图 28-1 到图 28-5），这条链改用文字把岔口逐个走一遍——每个岔口只标它在图上本来会落在哪张已存在的图里，读者顺着旧图就能复核形状。

**岔口一，读数越线还是没越线。** 没越线不上会，安静随下个周期归档（图 28-2 那条自环「指标健康 · 推进下一档」）。这一支什么都不拦，而全链的速度全靠它：图 28-2 的自环边是灰度真正的默认状态。

**岔口零，这条读数的分母是谁的。** 表里前两行共用同一个前置判据：分母必须限在 canary 那一路（28.5f 第一条口径）。稳定版自己的错误喂进 canary 的读数时，越线是别人的事——这一岔不拦任何回滚，它拦的是把别人的事故当自己那一档的证据用。

**岔口二，单窗还是连续两窗。** 28.5f 的 W2 与 W4 都越了 0.01，判定器不动作；W5 才 ROLLBACK。这一岔是两窗语义的权力含义：越线一次就召集人上会，等于把 28.2 根因② 请回来——用「人在环上」的名义重建了人盯监控。

**岔口三，硬指标还是次要指标。** 硬指标这一支没有岔口：图 28-3 前三步（上报、越阈触发、执行回滚并冻结在上一稳定档）没有人的泳道，值班 SRE 在这里只是事后报备的签名者。只有次要指标才进图 28-2 那个「暂停待决」，它两条出边都写着人的决定——推进或放弃，权还给人。

**岔口四，改阈值还是改契约。** 改 `failureCondition` 的数值或 `failureLimit` 是改阈值，走风控那一票就够；改档位序列（28.5 第 1 件那六档）、改权重口径（28.5c 那条「以实际占比推进」）、改 flag 的定向段（28.5d 第④关）都是改契约——必须回 28.5 第 3 件重签对应那一行。这一岔唯一要防的动作是「用调参的名义改契约」，而它恰好最容易藏在改动字数最少的那一类 diff 里。

**岔口五，到点有没有人。** 第 3 件那行「到点无人决策即按回滚处理」是全链最反直觉的一支：它把「没人」从默认不动作改成默认回滚。这一支只在损失随时间扩大的链路上成立，判据仍是 28.4 那句切权原则——资金对账给快的人，功能小问题给判断准的人。

**回边，动作生效后还要再量一次。** 28.5f 那句「判定状态必须按发布批次清零」是这条回边的技术形态：图 28-2 从「已回滚」指回「限时复盘 · 修订后重走」，图 28-3 最后那条「修订通过后重新灰度」是它的时序形态。链上没有终点站——一次拍板不是这条链的终点，而是下一次被量的起点。

剩下的三列各有各的复发方式。缺「出处」这一列，表里的数就成了无母体的常数：0.5% 与 1% 那两个口径一旦被抄进别的场合，28.8 说得很清楚——它们是经验值，没有理论依据，而经验值最怕的就是被当成通用常数用到另一条链路上。缺「上会判据」这一列，本表与 28.5 第 3 件那张矩阵就重复了：矩阵管半夜照着执行，本表管评审席上要不要把这件事端上桌，两者若都只写触发条件不写形状，值班的手感就会在评审室里重新变成阈值。缺「签署方」这一列，最贵的病会原样复发：28.4 那个「回滚快的收益与回滚错的成本由谁承担」的权力问题，不会因为多了一张表而自己消失，它只会挪到会后再吵一次。

五列里最容易被省掉的是「时限锚」，而省掉它复发的病本章已经写过：28.4 说得很直白——最关键的最初几十分钟，常常耗在「谁有权回滚」上。有读数、有判据、有动作、有名字，就是没有到点之后怎么办，于是软刹车那条自环变成无限等待。反过来说，时限锚这一列不能自己活：它必须和第 3 件矩阵里的同一行同值，两处不一致时以矩阵为准，因为矩阵是 runbook 里被半夜翻开的原件，而本表是它的对账副本。

---

## 28.5i 腐烂判据：灰度这套机制自己烂掉之前，先出现的是什么形状

实现件齐了，最后一问最不留情：这套闸会不会自己烂。灰度的失败形态从来不是「没做过」，而是「还在做，但做的事和桌上的读数已经对不上」——每档照常推进、每个 flag 照常放量，只是没人再核对声明与生产是不是同一件事。量尺就用 28.5 到 28.5f 造出来的那些闸，一层一条信号：档位（28.5 第 1 件与 28.5b）、刹车（28.5b 与 28.5f）、分流（28.5c）、flag（28.5d）、影子（28.5e），再加 28.3 四件套里最虚的那件——演练。证据面全是前文产物，不新增读数。

1. **档位层——两处档位声明开始分家。** 28.5 第 1 件那张档位表（六档序列配 30／10 分钟两个窗口）与 28.5b 的 steps 序列各自演化：档位表被业务评审改过而 YAML 没跟着走，或反过来只改了 `pause` 的时长。形状是「同一场发布里有两套档位」，读到任一套都不算证据；它比没改更坏，因为两套都能引用本章前面的纪律给自己背书。
2. **刹车层——长期不响，而窗口与阈值一个也没动过。** 28.5b 点过三个静默失效面，这里只认它们的腐烂形状：观察窗口短于指标滞后（28.5f 那句「读数永远比事故慢半拍」是同一件事的数学形态）、`failureCondition` 被换回绝对计数（晚高峰流量自然涨起，刹车自己误踩）、`trafficRouting` 没真正挂上（steps 全走完而 canary 分母读数全程为零）。三种都能长得像「这套机制很健康」，而 28.5g 硬刹车那一行给的反查动作是唯一的分辨法：改动有没有对回决策矩阵的一行。
3. **分流层——推进依据悄悄回到配置数字。** 28.5c 三条纪律之首是「推进档位的依据是 canary 实际收到的请求占比，永远不是配置里的权重数字」。腐烂不从改纪律开始，从报表开始：某次评审的表格里只剩 `setWeight` 那一列，占比那一列因为某次看板改版就没再出现。此后长连接与连接池（28.5c 前两处来源）造成的劈叉没有任何读数会主动报警——它只在回滚之后现形，而那时已经算在 28.5 第 4 件「回滚同步评估」的账上。
4. **flag 层——盐不再逐 flag 隔离，或 kill switch 那一支从没被走过。** 28.5d 写死了两条纪律：桶按账号 id 稳定、salt 一 flag 一换，理由是全平台共用一个盐会把多个灰度绑成同一个风险源，一炸全炸。这一层的腐烂有两个方向：盐被回收成公共值（形状是多个 flag 的命中人群完全重合），或者 flag 长期停在 100% 且生命周期内没有一条关闭记录——后一种和「flag 不存在」等效，却比不存在多占一次审批。
5. **影子层——标记在某个新接的下游那里不认得。** 28.5e 的原话是「标记漏传一环，彩排就有一环来真的」。腐烂不需要谁改影子逻辑，只需要链路上多接一个下游而它从没被问过认不认这个标记。这一条的对照物就是 28.5g 影子写那一行点名的无产物格：没有名册，就永远不知道哪一环已经在来真的。
6. **演练层——最初那次成为唯一那次。** 第 21 章 21.5b 说成对性管不着「能不能跑」，兜底是演练；28.6 最后一条勾选项要求大促前做一次完整回滚演练。腐烂形状不是演练失败，而是演练记录多年不更新而发布照旧全绿——28.1 那次「回滚脚本不存在」和「回滚脚本从没跑过」在事故现场给出的是同一个耗时数小时，只有证据面上前者挡得住审批、后者挡不住。

### 反事实的形状：如果这些判据当时在场，桌上会先看到什么

把本章出现过的失效形状放回判据桌上，归因就从「人不够警觉」换成「缺了哪一格可对账」：

| 那次的形状（本章出处） | 若有判据，桌上先看到的 | 现在该做的动作 |
|---|---|---|
| 案例一·电商：告警响起才发现没有回滚脚本，耗时数小时（28.1） | 四件套缺一在评审时就撞 28.3 那句「缺任何一项 → 灰度审批不通过」；第 21 章 21.5b 的机检会在发布 PR 上把那条迁移标成孤儿 | 成对性挂 required check，演练记录单列一格，不许「有文件」替「跑过」作证 |
| 半夜告警响很久才被人注意（28.2 根因②，反模式第 2 条） | 软刹车停在当前档而无人计时——28.5g 里那个无产物格正是它的形状 | 把时限锚落进批次记录，到点按第 3 件那行「即按回滚处理」执行 |
| 单窗尖刺误刹（28.5f 的 W2 与 W4） | 只有软刹车在响、留痕条目在涨而硬刹车零触发 | 阈值回 28.8 的历史 P99 若干倍口径重钉，重钉后逐行回填第 3 件 |
| 权重与真实流量劈叉（28.5c 三处来源） | 「canary 健康」这一行下面挂的是配置声明而不是实际占比 | 推进依据换成 28.5f 那个分母读数，两列并排上报 |
| 多个 flag 绑成同一个风险源（28.5d 同盐的形状） | 不同 flag 的命中人群完全重合，而事故一次波及全部在跑的灰度 | 盐逐 flag 隔离并在配置里可核，纳入 28.5g 第 7 行的读法 |
| 不可停机与不可逆形态（案例二 24/7 交易、案例三链上合约、案例四规则回滚） | 第 3 件矩阵最后那一列没有承接件——三行的事后动作分别是「资金存亡级复审」「撤资路径审计」「规则基准修订」，落不到 ADR 或门禁就无人复核 | 事后动作与 ADR 编号同批登记；这是 28.2 根因③ 的正面写法，不是新增要求 |

复审的最小环可以直接跑：每次灰度评审先过这六条信号，命中任一条就列入该会议程第一项；改档位、改阈值、改 flag 定向的唯一入场券是一次对账过的读数——也就是 28.5c 那条「以实际占比推进」，和 28.5f 那个两窗判定。这个环有三个盯点。其一，六条信号里没有一条是事故本身，六条全是腐烂的形状——灰度工程的坏状态从不以出事现身，它先以「看起来很绿」现身（28.5f 单窗尖刺那一格，与第 18 章 18.6g 那张把两个窗口的 p99 直接平均就贴进总表的报表是同一族病）。其二，「对账过的读数」是全环唯一认机器不认签名的关口：阈值改动可以签齐所有名字，但没有对回决策矩阵那一行就不许进生效列，这是 28.5g 硬刹车那格的机制化。其三，环没有终点站——废止一条档位、关掉一个 flag，要连同它的产物一起归档；那些产物是下次「尺子坏了」（信号第三条那种形状）时唯一还能翻的对照，没人翻它，环照样烂。

最后记一笔诚实账：这一节的六条信号自己就得先上 28.5g 那张证据清点。六条里能挂在真产物上的只有两条——刹车层那格有 28.5b 的两个字段可 diff，演练层那格有第 21 章 21.5b 的 required check 可跑；档位、分流、flag、影子四层给的全是「怎么读现有产物」的姿势，而姿势本身没有产物。本节也没敢给薄样本那条任何数字阈值（28.5h 第 4 行已经把这份欠账交回 28.8），全节不贴任何新的运行输出——它和 28.5b 一样只交形状。所以「防腐烂」这一节此刻在本章的三态里就是「产物可造假」那一态：它能被抄进 runbook、能被勾在清单上、能在评审会上念得很响，而它拦住的东西要等下一次有人真的去对那两个字段、那份名册时才算被拦住。灰度机制真正的健康度不体现在某次发布有多平静：一条刹车从没被误触发过、一个 flag 从没被关过、演练记录停在最早那一页——这三格读数一样干净，而 28.5f 已经给过唯一的解释口径：单窗越阈不动作是判定器的功劳，前提是判定器真的在跑。没跑过的干净不是好，是没人拿它验过自己。

---

## 28.6 检查清单

- [ ] 你的每次发布，是不是都带着回滚脚本？还是只有前进路径？
- [ ] 你的灰度，有没有"自动刹车"指标？还是靠人盯监控？
- [ ] 自动刹车的阈值，是谁定的？业务还是风控？高风险相关的，风控有没有一票？
- [ ] 出事那一刻，谁有权回滚？这个权力是按"损失是否随时间扩大"切的吗？
- [ ] 回滚之后，有没有限时复盘的硬要求？
- [ ] 一个端回滚了，其他端要不要同步回滚，有没有评估机制？
- [ ] 你的灰度方案，有没有在大促前做过一次完整的回滚演练？

---

## 28.7 反模式

| # | 反模式 | 后果 |
|---|--------|------|
| 1 | 只生成前进脚本，不生成回滚脚本 | 数小时手写回滚 |
| 2 | 灰度靠人盯监控 | 半夜没人盯，告警响很久才发现 |
| 3 | 所有回滚都要业务批准 | 黄金时间耗在找人上 |
| 4 | 回滚权笼统归某一个角色 | 要么误回滚，要么回不动 |
| 5 | 一个端回滚不评估其他端 | 端上不一致，二次事故 |
| 6 | 灰度没有"档"的概念 | 要么不发，要么一把梭 100% |
| 7 | 没有回滚演练 | 真出事时才发现回滚脚本跑不通 |

---

## 28.8 遗留问题

- **自动刹车的阈值怎么定？** 定低了误回滚，定高了漏事故。通常用历史 P99 错误率的若干倍做硬刹车阈值，但这没有理论依据，是经验值。**这个领域缺乏工程化的方法论。**
- **多端回滚的同步评估，能不能自动化？** 现在是一个端回滚后人工评估其他端。能不能让系统自动判断？技术上可行，但尚未普及。
- **AI 能不能帮着回滚？** AI 能帮着定位、能帮着生成回滚脚本，但"该不该回滚"这个决策必须人拍。因为回滚是对业务的否定，否定这件事，AI 没有资格——它不为后果负责。

---

> **本章的一句话**：在 AI 让"上线"变得几近免费之后，**「能撤回」成了发布真正的成本中心**。一个成熟的平台，衡量它的标准不是"一天能发多少次"，而是"出事时多久能撤回来"。**生成得快是本事，回滚得快才是底气。前者 AI 给得了，后者，永远得人来兜。**
